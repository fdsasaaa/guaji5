# 12_方案资金目标分类与自适应路径协议

版本：V1.0-FUNDING-OBJECTIVES  
状态：正式候选规则，合并后生效

## 1. 目的

资金路径不再由固定倍投模板先行决定。标准顺序必须是：

> 资金目标决定方案类型；玩法经济决定回收能力；本金决定有效深度；软件能力决定执行结构。

正式资金目标仅分为：

- `DURABLE`：耐久型；
- `RECOVERY_PROFIT`：回利型；
- `AUTO_COMPARE`：用户未明确时，同时生成前两类候选后再选择。

直线倍投和高级倍投只是执行工具，不是方案类型。

## 2. 耐久型

目标是低暴露、抗连续不中、主动降压和尾部可控，不要求任意一期命中都回收此前全部亏损。

必须说明：

- 最高倍数；
- 10、20、30、40、50期及需要时更深的连续不中压力；
- 尾部是1倍自锁、停止还是冷却；
- 长期利润来源是选号优势、返点、选择性入场、组合来源，还是尚未建立；
- 未完成样本外验证时，不得声称长期盈利、稳定盈利或保证盈利。

耐久型只能改善资金生存，不得把倍投本身描述为正期望来源。

## 3. 回利型

目标是在机器计算出的每一个有效档位内，首次命中后均满足：

```text
命中净结果
= 本期中奖总返奖
+ 本期实际返点
- 本期投注
- 此前累计净亏损
```

并且：

```text
命中净结果 >= 目标微利
```

回利型不得预设固定赔率、固定期数或固定倍投。允许：

- 长时间平倍后递进；
- 阶梯、等比、前缓后快；
- 涨跌递进；
- 有条件的状态循环；
- 直线、高级或混合执行。

循环必须具有明确事件、退出条件、封顶和尾部规则；禁止机械复制短模板凑期数。

最大有效期数必须同时受以下条件约束：

- 总本金及允许使用比例；
- 最低投注单位；
- 单期投注上限；
- 最大允许倍数；
- 平台派奖上限；
- 每档命中后的目标微利。

任何一档不满足回利条件，该档及其后续不得标记为有效回利档。

## 4. 自动口述路由

执行：

```bash
python tools/resolve_funding_objective.py --request "<用户口述>"
```

路由规则：

- 明确出现“耐久型”等词：`DURABLE`；
- 明确出现“回利型、连本带利、中一期盈利”等词：`RECOVERY_PROFIT`；
- 未明确或同时出现两类目标：`AUTO_COMPARE`。

`AUTO_COMPARE`不得由AI直接猜选，必须生成两类候选并按有效深度、最大暴露、命中后结果和软件证据比较。

## 5. 标准证据

每个标准方案必须生成：

```text
controller/runs/<RUN_ID>/funding_objective.json
```

模板：

```text
controller/templates/funding_objective.template.json
```

机器校验：

```bash
python tools/validate_funding_objective.py \
  --evidence controller/runs/<RUN_ID>/funding_objective.json
```

配置校验：

```bash
python tools/validate_funding_objective.py \
  --config controller/funding_objectives.json
```

仓库自检：

```bash
python tools/validate_funding_objective.py --self-test
python tools/test_resolve_funding_objective.py
```

## 6. 交付要求

方案交付必须明确：

- 资金目标类型；
- 为什么选择直线或高级倍投；
- 资金路径不是从模板套入，而是如何由经济参数反推；
- 耐久型的尾部边界；
- 回利型的逐档回利表和有效深度；
- 全链不中损失和至少命中一次概率；
- 实际软件证据等级。

一句总原则：

> 先算玩法再定路径，先算本金再定期数；允许灵活递进，禁止机械套表。
