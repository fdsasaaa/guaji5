# 05D_PPT技术来源与号码证据协议

版本：V3.9.5-SOURCE-GATE-1  
状态：强制执行  
适用范围：所有彩票方案、挂机实验、技术验证和对应讲解PPT。

---

## 1. 观众首先要知道“这些号码为什么出现”

PPT的任务不是向用户解释系统怎样拼装文件，而是让视频观众理解：

1. 研究了什么历史现象；
2. 使用了哪一种真实分析角度；
3. 读取了哪段数据、哪个位置和多大窗口；
4. 号码或状态怎样一步步算出来；
5. 为什么最终保留这些号码；
6. 软件接下来怎样真实执行这套规则。

凡是页面直接展示具体号码、号码组、杀码、胆码、冷热组、遗漏组或轮换组，前面必须已经给出可复现的来源说明。

---

## 2. 分析角度正式源

技术来源优先从以下正式源选择：

- `02A_彩票开奖历史分析体系与方案路由.md`；
- `分析角度索引.jsonl`；
- `02_技术原子与组合规则.md`；
- `01_软件格式与已验证执行规则.md`。

允许使用的典型角度包括但不限于：

- 定位冷热、短中长窗口冷热、加权冷热；
- 当前遗漏、平均遗漏、遗漏倍数、回补；
- 重号、邻号、隔期重现、历史召回；
- 长龙、单跳、双跳、跳幅和游程；
- 和值、跨度、单双、大小、012路；
- 位置交换、位置关系、位置转移；
- 条件频率、转移矩阵、随机基准和样本外验证。

不能先随意确定一组数字，再倒推“热码防守”“冷码回补”等故事。

---

## 3. 号码来源六要素

每一组对外展示的号码必须在内部冻结以下六项：

1. **数据来源**：文件、期号范围、总期数；
2. **观察对象**：玩法、位置、属性或组合；
3. **分析角度**：对应角度ID或明确自然名称；
4. **计算过程**：频次、遗漏、排序、阈值或映射规则；
5. **选取规则**：取前几名、并列怎样处理、排除条件；
6. **最终结果**：冻结号码及其逐个依据。

缺少任意一项，不得进入正式PPT和方案交付。

---

## 4. PPT必须展示到什么程度

PPT不需要堆满工程统计，但必须让普通观众能够复算主要结论。

推荐表达：

```text
我们先看最近20期个位。
数字4出现5次、6出现4次、8出现4次，排在前3位。
所以第一局使用4、6、8，验证短期热号继续出现的表现。
```

或者：

```text
数字0当前遗漏12期，历史平均遗漏约4期；
数字1当前遗漏9期，数字2当前遗漏8期。
本局取遗漏最高的3个数字，验证长遗漏号码是否会回补。
```

禁止用下列表述替代真实来源：

- “这是一组测试码”；
- “系统预设了四组号码”；
- “更严谨地说，这不是自动生成”；
- “为了方便演示，我们选择了……”；
- “号码只是示例，但先按它运行”。

这些话只能暴露方案没有真实技术来源，不属于客户讲解内容。

---

## 5. 技术来源页是正文核心页

PRE_RUN_SETUP阶段的推荐讲解顺序调整为：

```text
研究问题
→ 历史数据中观察到的现象
→ 号码/状态的具体计算来源
→ 投注和切换规则
→ 完整运行案例
→ 挂机与判定方法
```

当页面出现四组具体号码时，至少要有一页专门回答“这四组号码怎样得来”。不能只写“热码防守、温码接力、冷码回补”这类标签。

---

## 6. 分析逻辑与软件能力必须一致

必须区分两层：

- **选号来源**：历史数据怎样生成号码；
- **软件执行**：TXT和软件怎样使用这些号码。

如果软件只能接收冻结号码，PPT应明确：

> 号码在挂机前按照某个固定历史窗口计算并冻结，运行期间不自动更新。

但仍必须讲清冻结号码真实来自哪些数据和计算，不能把它降级成“预设测试码”。

如果PPT声称软件会动态计算冷热、遗漏或迁移，底层TXT必须真的具备对应更新能力。否则强制失败。

---

## 7. 对照组不得混淆技术来源

随机出号、固定三码和安慰剂只能作为对照组。PPT必须先完整讲清主技术，再单独说明对照组的作用。

禁止把随机出号和主技术并列包装成方案核心组成，也禁止让观众误以为随机号码同样来自冷热、遗漏或趋势分析。

---

## 8. 内部号码证据包

每个正式方案必须在GitHub批次记录中保存 `号码来源证据`，至少包括：

```json
{
  "数据范围": "",
  "分析角度ID": [],
  "分析角度名称": [],
  "观察位置": "",
  "窗口": [],
  "原始统计": {},
  "排序或阈值规则": "",
  "并列处理": "",
  "冻结号码": [],
  "软件字段映射": "",
  "运行中是否更新": false
}
```

PPT数字、TXT数字与该证据包必须完全一致。

---

## 9. 人工讲解语言要求

对外页面只保留观众有用的信息：现象、计算、规则、例子和实验问题。

以下内容进入GitHub内部记录，不得成为主讲文案：

- 本次纠错说明；
- AI为什么这样生成；
- 工程字段和校验状态；
- “更严谨的说法”；
- “给用户看的/给客户看的”；
- 模板、版本、生产协议和内部决策过程。

页面应像一个熟悉玩法的人在解释自己的实验，而不是系统向操作者解释文件结构。

---

## 10. 强制失败状态

- `PPT_NUMBER_SOURCE_MISSING`：具体号码没有历史数据来源；
- `PPT_NUMBER_CALCULATION_MISSING`：没有展示关键计算或排名依据；
- `PPT_NUMBER_REASON_NOT_REPRODUCIBLE`：观众无法按规则复算；
- `PPT_ANALYSIS_ANGLE_UNREGISTERED`：技术来源未对应正式分析角度；
- `PPT_STORY_BACKFILLED_AFTER_NUMBERS`：先定号码后编冷热/遗漏故事；
- `PPT_ANALYSIS_EXECUTION_MISMATCH`：PPT宣称的动态逻辑与TXT能力不一致；
- `PPT_CONTROL_GROUP_CONFUSED_WITH_MAIN`：随机对照被包装成主玩法；
- `PPT_INTERNAL_CORRECTION_LANGUAGE_VISIBLE`：纠错或工程说明出现在客户页面；
- `PPT_NUMBER_EVIDENCE_MISMATCH`：PPT、TXT与内部证据包数字不一致。

任一状态触发，禁止构建正式交付ZIP。