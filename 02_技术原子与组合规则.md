# 02_技术原子与组合规则

版本：V3.8.0  
作用：把可用技术拆成可查重、可组合、可冻结的原子；软件字段仍以01模块为准。

---

## 2.1 三层分类

### 一级分类

软件实际识别的策略入口，例如：

- 定位胆；
- 开某投某；
- 定码轮换；
- 高级定码轮换；
- 冷热温出号；
- 足迹出号；
- 重号侦测；
- 组合方案出号；
- 组合方案轮投；
- 已验证的直选/组选分类。

一级分类决定TXT字段集合，不得任意改名。

### 二级分类

一级分类内部的玩法与位置，例如：

- 万、千、百、十、个；
- 前二、后二、前三、中三、后三、五星；
- 龙虎位置对；
- 正投/反投；
- 单式/复式。

### 三级分类

必要的技术条件，例如：

- 统计周期；
- 冷/热/温阈值；
- 遗漏长度；
- 重号类型；
- 触发序列；
- 换号规则；
- 停止条件。

三级条件用于实验，不允许无限叠加以追求历史收益。

---

## 2.2 技术原子标准字段

每个原子至少记录：

```text
atom_id=
名称=
所属一级分类=
用途=出号/杀号/监控/编排/资金/停止
自然语言定义=
软件字段模板来源=
合法参数范围=
与哪些原子兼容=
与哪些原子冲突=
证据主张=
证据等级=
已知风险=
```

没有软件字段模板来源的原子可以作为E0研究想法，但不得直接生成正式TXT。

---

## 2.3 当前已知技术原子族

| 原子族 | 作用 | 默认证据边界 |
|---|---|---|
| 定位胆/固定取码 | 单位置或多位置号码选择 | 仅在01列明组合内正式 |
| 开某投某 | 根据开出状态选择投注 | 对应模板内使用 |
| 定码轮换 | 固定集合按换号条件轮换 | 格式可用，行为按具体设置分层 |
| 高级定码轮换 | 多局结构轮换 | 只用已验证字段 |
| 冷热温出号 | 按窗口频率分组 | 技术有效性需E4-E7 |
| 足迹出号 | 按历史足迹生成号码 | GUI字段E2，行为需核对 |
| 重号侦测 | 观察重复号码 | 部分GUI字段E2 |
| 投注监控 | 触发前虚拟观察 | 三模式概念已核对，交互待测 |
| 换号 | 中/挂/连续/累计后换号 | 枚举映射已核对，重置待测 |
| 正集/反集 | 投所选集合或补集 | GUI映射已核对，补集算法待测 |
| 组合方案出号 | 多子弹出号编排 | 枪格式E2，行为待测 |
| 组合方案轮投 | 多局多子弹编排 | 枪格式E2，行为待测 |
| 普通倍投 | 倍率序列 | 格式E2，效果不提高命中 |
| 高级倍投 | 9字段多局资金路径 | 格式E2，具体跳转逐项核对 |
| 盈亏/时间控制 | 停止、跳转或时段 | 多数只允许测试 |

---

## 2.4 枪弹组合规则

1. 一个普通一级分类TXT是一个子弹。
2. 枪只负责编排，不重新定义子弹内部逻辑。
3. 需要混合多个一级分类时，优先拆为多个子弹。
4. 一个枪引用的每个子弹必须有唯一显示名。
5. 枪与子弹同目录。
6. 修改子弹名称必须同步枪引用和方案指纹。
7. 枪本身也拥有ID、版本、指纹和证据等级。
8. 枪的导入证据不能替代子弹行为证据。
9. 顶部GUI轮投是额外全局状态，必须进入实验指纹和验证条件。
10. 不勾选顶部轮投时，多子弹并投会叠加风险，验证器必须按总注数计算。

---

## 2.5 组合合法性矩阵

组合前逐项判断：

| 检查 | 允许 |
|---|---|
| 每个原子都有同一级分类已验证字段模板 | 是 |
| 不同一级分类拆成独立子弹 | 是 |
| 枪只引用显示名 | 是 |
| test_only原子进入正式方案 | 否 |
| unknown字段与verified字段混合 | 否 |
| 通过换名字掩盖相同核心假设 | 否 |
| 用更多设置增加必要触发条件 | 条件允许 |
| 为提高历史收益不断加例外 | 否 |
| 参数超过主要5个 | 默认否，须说明 |
| 判断步骤超过3层 | 默认否，须说明 |
| 技术原子超过3项 | 默认否，须说明 |

复杂度数字为暂定默认，不是效果最优证明。

---

## 2.6 方案最小表达

每个候选必须能用1至3句话回答：

1. 观察什么状态；
2. 满足什么条件后投什么；
3. 何时换号、停止或重置。

无法用普通语言解释，通常说明：

- 原子过多；
- 条件层级过深；
- 存在后验修补；
- 软件实现和研究假设混在一起。

---

## 2.7 技术组合驱动流程

1. 从训练区描述状态，不看样本外。
2. 选择1至3个兼容原子。
3. 选择位置、周期和方向。
4. 选择枪弹结构。
5. 选择平倍或受控资金路径。
6. 提炼一个可证伪问题。
7. 生成指纹并查重。
8. 冻结。
9. 再进行验证。

技术组合用于提出假设，不代表数据中必然存在规律。

---

## 2.8 问题驱动流程

可使用的问题：

- 热号是否继续热；
- 冷号是否回补；
- 杀号是否比选号稳定；
- 短周期是否优于长周期；
- 连续状态后顺势还是反向；
- 两种矛盾技术谁更接近随机；
- 监控后入场是否只是赌徒谬误；
- 轮投是否只是改变风险分布。

先固定问题，再选最少的原子验证，不能为了视频冲突感增加无关条件。

---

## 2.9 新颖度维度

新方案至少改变两个核心维度，优先三个：

- 核心技术关系；
- 核心假设；
- 验证问题；
- 周期结构；
- 位置结构；
- 方向；
- 枪弹关系；
- 轮投关系；
- 投注/停止逻辑。

只改名称、排序、位置或少量参数，不构成新方案。

---

## 2.10 参数边界

参数分三类：

1. 软件必要参数：保证格式和运行；
2. 实验参数：周期、阈值、触发长度；
3. 内容参数：样本量、本金、视频展示长度。

实验参数必须在冻结前确定。  
软件必要参数不能根据收益随意更改。  
内容参数不能反过来改变实验结果。

---

## 2.11 高级倍投的技术定位

高级倍投是资金路径原子，不是选号原子：

- 不改变中奖概率；
- 必须与号码逻辑分开评估；
- 号码方案和资金方案最好做2×2对照：
  - 技术+平倍；
  - 技术+高级倍投；
  - 随机+平倍；
  - 随机+同高级倍投。
- 若优势只来自高风险倍数而非号码逻辑，结论必须明确。

---

## 2.12 原子库扩充纪律

新增原子时必须：

1. 给出自然语言定义；
2. 给出软件字段来源；
3. 标注E0-E7；
4. 做最小导入探针；
5. 做单变量运行测试；
6. 记录失败；
7. 更新兼容矩阵；
8. 更新JSONL中的核心技术规范名；
9. 不与旧原子换名重复；
10. 不因一段历史盈利直接升级为正式原子。

---

## 2.13 当前禁止的伪创新

- 同一冷热逻辑只换窗口；
- 同一监控逻辑只换01长度；
- 同一五号码组只换位置；
- 同一枪弹结构只换子弹排序；
- 同一方案换视频标题；
- 同一倍投只换模式名称；
- 同一假设增加一个无关过滤器；
- 为达到收益目标逐次增加条件。

这些通常判为R2或R3。

---

## 2.14 原子与方案指纹

推荐规范串：

```text
FP=一级类别|核心技术|位置|周期|方向|枪弹结构|轮投结构|投注规则|停止条件|核心假设
```

指纹必须使用规范化名称：

- 中文全角标点转ASCII；
- 同义词归一；
- 位置按固定顺序；
- 周期写明单位；
- 枪内子弹名排序规则固定；
- 不把结果分类写进生成指纹的核心部分，以免同一逻辑因结果不同被误认为新方案。

结果分类作为附加字段保存。

---

## 2.15 方案套优先规则

### 2.15.1 默认单位

正式输出默认单位从“单个方案TXT”升级为“方案套文件夹”。

方案套不是简单把多个TXT放在一起，必须具有一个可以解释的整体实验结构。每个子方案至少承担以下角色之一：

- 主假设；
- 对立假设；
- 不同时间尺度；
- 不同位置分工；
- 随机或简单基准；
- 风险对照；
- 备用/观察，不参与正式投注。

只有名称或参数轻微不同、没有独立实验职责的多个TXT，不算高质量方案套。

### 2.15.2 单TXT例外

以下情况允许只生成一个TXT：

- 格式探针；
- GUI行为探针；
- 单字段验证；
- 用户明确要求单方案；
- 作为已有方案套的补充子弹。

例外必须标记 `delivery_type=probe_or_single`，不能悄悄把单TXT当作完整交付。

### 2.15.3 套装最低文件

```text
/方案套ID_名称/
  B001_子方案...
  B002_子方案...
  可选G001或L001枪文件
  可选GJBTScheme/高级倍投配置
  README_先看这个.md
  软件导入与运行核对记录.md
  套装冻结记录.md
  方案记忆_建议追加块.md
```

---

## 2.16 方案套六种标准结构

1. **对立假设套**：顺势、反转、随机基准；
2. **多尺度套**：短窗、中窗、长窗；
3. **位置分工套**：不同位置承担不同逻辑；
4. **多技术低相关套**：不同分析体系互补；
5. **资金路径对照套**：同一号码逻辑的平倍与高级倍投，只用于比较，不默认同时实投；
6. **枪弹编排套**：组合出号或组合轮投，真实行为必须按证据等级限制表达。

若同一逻辑只更换3、5、7期参数，应明确称为“参数敏感性套”，不能伪装成三个独立技术。

---

## 2.17 轮投、并投与枪编排的选择

### 默认选择轮投的情形

- 子方案高度相关；
- 号码或位置大量重叠；
- 多条倍投会同时加压；
- 总资金暴露尚未精确计算；
- 主要目标是验证套装整体路径；
- 枪真实行为未达到E3。

### 允许全部并投的最低条件

必须同时满足：

- 每个子方案的实际注数已知；
- 重叠注单可检测；
- 总成本和最坏暴露已计算；
- 子方案相关性和同步连错风险已评估；
- 并投本身是冻结实验条件；
- 赔率下的盈亏平衡命中率已计算；
- 用户明确知道并投不是“分散”，可能只是风险叠加。

轮投和并投是两个独立策略版本，不能用同一结果互相替代。

---

## 2.18 投注设计复杂度分级

### L0 平倍

适合：

- 软件行为尚未核对；
- 实际赔率或注数未知；
- 首次历史验证；
- 需要隔离号码逻辑。

### L1 简单有限普通倍投

例如平缓、封顶、有限局数序列。必须给出最大倍数和一轮总倍数。

### L2 压力释放型普通倍投

允许非单调序列，例如升压后主动降压。不能仅因历史连错长度定制。

### L3 C/D型高级倍投状态路径

使用已知9字段结构，通过中后ID、挂后ID和有限倍率形成状态路径。具体跳转行为未达到E3时只能测试。

### L4 非对称高级状态机

深层中奖后逐级降压、不同状态转移等。只有具体跳转、重置、监控和多方案交互达到对应证据后才允许正式使用。

### L5 自适应资金状态机

按实时注数、赔率、累计回撤或剩余预算动态调整。当前软件若没有已验证字段支持，只能作为外部研究设计，禁止伪造TXT能力。

复杂度由任务需要决定。高级版本必须能够解释每个状态为什么存在，不能为“看起来高级”增加局数。

---

## 2.19 高级倍投设计输入

设计前必须尽量取得：

```text
单注金额=
每期实际注数范围=
实际赔率或返奖=
本金=
最大允许回撤=
最长连错历史分布=
随机基准最长连错分布=
是否多方案并投=
子方案重叠程度=
软件中后/挂后跳转证据=
```

缺少赔率或实际注数时，不允许声称某条倍投能够回本。

实际单期暴露应按：

```text
单期总暴露 = 当局倍数 × 实际注数 × 单注金额
```

可变注数方案不能只比较倍率。

---

## 2.20 高级倍投风险表

每个普通或高级倍投至少输出：

- 局数；
- 倍数序列；
- 中后路径；
- 挂后路径；
- 最大倍数；
- 一轮倍率总和；
- 连续全挂累计暴露；
- 95%和99%随机连错压力；
- 深层中奖后净损益；
- 是否能真正回收历史亏损；
- 熔断或强制复位点；
- 与平倍的2×2对照；
- 多方案同时到达高倍的最坏情况。

禁止默认使用无限马丁、无上限几何递增或依赖爆仓前必中的路径。



---

## 2.21 V3.8.0功能覆盖与组合案例接入

技术原子选择前新增两项检索：

1. `功能覆盖索引.jsonl`：判断该功能是高频、低覆盖、未使用还是被软件证据阻塞；
2. `方案组合案例索引.jsonl`：判断该原子曾经怎样与其他原子形成完整方案，哪些结构可复用、哪些效果仍未证实。

选择纪律：

- 低覆盖功能只获得“值得检查”的资格，不自动进入方案；
- 一个候选的所有原子必须共同服务于同一验证问题；
- 不能为增加功能数量而突破3个核心技术默认上限；
- 已经高频使用的功能仍可使用，但必须产生新的关系、问题或执行结构；
- 软件行为证据不足时，优先生成最小探针，不直接叠加资金路径。

固定原则：长期充分覆盖，单次合理克制。

---

## 2.22 V3.9.0资金路径四路评审

每个正式号码逻辑冻结后，必须比较：

1. 平倍基准；
2. 有限普通倍投；
3. 压力释放型普通倍投；
4. 高级状态路径。

最终仍可选择平倍，但必须写明另外三路为何不适合。赔率和实际注数未知时，非平倍只能作为有限测试候选，不能声称回本。

允许输出“平倍基准版 + 一个资金路径实验版”，但不得默认同时真实投注。

## 2.23 V3.9.0功能组合预算

推荐单个正式方案的功能预算：

```text
1个号码/映射核心
+ 0—1个监控或换号状态
+ 1个执行编排
+ 1个资金路径
+ 1个停止/风险规则
```

这是默认上限，不是机械配额。功能存在不等于必须启用；但每批必须在八层能力审议中明确考虑。
