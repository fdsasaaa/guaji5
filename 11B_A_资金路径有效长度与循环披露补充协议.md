# 11B-A 资金路径有效长度与循环披露补充协议

版本：V3.9.8-PSEUDO-LONG-GUARD-1  
状态：正式流程规则  
作用：禁止用短倍投序列重复展开，冒充几十期独立资金设计。

---

## 11B-A.1 核心判定

资金序列的三个长度必须分开：

```text
有效独立长度 effective_design_length
= 机器识别后的最短规范化序列长度

总运行期数 execution_horizon
= 实际计划运行到停止的总期数

最小重复周期 minimum_repeat_period
= 能够完整重复还原输入序列的最短块长度
```

例如：

```text
1,1,2,2,3,3,5,5,3,2,1,1
```

重复5次后虽然输入了60个数，但机器必须判定：

```text
最小重复周期=12
有效独立长度=12
总运行期数=60
结构=12期循环路径×5轮
```

不得称为“60期独立路径”“60阶高级倍投”或“完整设计60期”。

---

## 11B-A.2 规范化存储

资金路径只能使用两种结构：

### FINITE

有限独立路径。`canonical_sequence` 保存全部独立步骤，停止期不得超过序列长度。

### CYCLIC

循环路径。`canonical_sequence` 只能保存一轮最短规范序列，循环次数单独记录。

示例：

```text
canonical_sequence=[1,1,2,2,3,3,5,5,3,2,1,1]
sequence_mode=CYCLIC
effective_design_length=12
execution_horizon=60
cycle_limit=5
design_claim=12期循环路径×5轮
```

禁止把同一12步复制5次后写入规范序列。发现展开式重复块时必须失败，而不是自动包装成长路径。

平投也是循环结构。50期都投1倍，不是50步独立设计，而是1期恒定循环×50轮。

---

## 11B-A.3 强制字段

每条资金候选必须保存：

```text
sequence_mode=FINITE/CYCLIC
canonical_sequence=[]
minimum_repeat_period=
effective_design_length=
claimed_design_length=
execution_horizon=
declared_stop_period=
design_claim=
cycle_limit=
cross_cycle_checkpoints=
```

固定约束：

- `effective_design_length` 必须等于规范序列长度；
- `claimed_design_length` 必须等于有效独立长度；
- `execution_horizon` 必须等于声明停止期；
- `FINITE` 的停止期不得超过有效独立长度；
- `CYCLIC` 的总运行期数必须大于单轮长度；
- 循环结构必须准确显示“X期循环路径×Y轮”；
- 检测到更短完整重复块时，当前序列不是规范序列，必须退回导演阶段。

---

## 11B-A.4 跨轮压力与回收

循环路径不能只核算第一轮。必须逐轮记录：

```text
cycle_number=第几轮
losses_at_end=本轮结束时累计连挂
cumulative_outlay=从第一期开始的累计投入
remaining_bankroll=冻结本金减累计投入
next_period_stake=下一轮或下一步投入
hit_net_after_cycle=此时下一期命中后的实际净结果
recovery_complete=是否覆盖此前所有轮次累计亏损
```

第二轮的回收计算必须包含第一轮全部亏损；第三轮必须包含前两轮全部亏损。不得每轮重新归零后声称“本轮命中可回本”。

达到停止期后：

- 不再追加投注；
- `next_period_stake=0`；
- 后续50期压力检查保持累计投入不变；
- 不得假设停止后还有一次命中用于回收。

---

## 11B-A.5 机器拒绝条件

以下任一情况直接失败：

- 长序列包含可完整还原的更短重复块；
- 把12期循环写成60期独立路径；
- `claimed_design_length` 大于有效独立长度；
- 循环路径保存了多轮展开序列；
- 缺少逐轮跨轮压力表；
- 第二轮以后累计投入从零重新计算；
- 跨轮命中净结果未扣除之前轮次亏损；
- 有限路径停止期超过独立序列长度；
- 50期压力测试在已停止后仍继续追加投注。

对应失败状态：

- `FUNDING_SEQUENCE_STRUCTURE_MISSING`
- `EXPANDED_REPEAT_BLOCK`
- `EFFECTIVE_DESIGN_LENGTH_MISMATCH`
- `PATH_LENGTH_CLAIM_OVERSTATED`
- `CYCLE_DISCLOSURE_INVALID`
- `CROSS_CYCLE_STRESS_INCOMPLETE`
- `BANKROLL_SEQUENCE_MISMATCH`
- `POST_STOP_STAKE_DETECTED`

---

## 11B-A.6 表达规则

对外可使用：

- “12期循环路径，最多运行5轮”；
- “42期有限独立路径”；
- “第42期硬停止，50期压力表中42期后不再增加投入”。

禁止使用：

- “复制五轮后的60期独立倍投”；
- “看起来有60个数字，所以是60阶设计”；
- “每轮单独回本，因此跨轮也能回本”。

固定原则：

> 序列写得长，不等于设计得深。有效长度由最小重复周期和规范化结构决定，不由输入数字个数决定。
