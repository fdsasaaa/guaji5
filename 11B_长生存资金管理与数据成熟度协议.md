# 11B_长生存资金管理与数据成熟度协议

版本：V3.9.7-BANKROLL-LONGEVITY-1  
状态：正式流程规则  
作用：把资金管理从“附加倍数表”升级为与选号、触发和执行同等级的独立设计与机器验收域。

---

## 11B.1 目标边界

本协议追求的是：

- 在冻结本金和风险上限内延长资金生存期；
- 根据具体玩法的注数、成本、赔率、命中结构和软件能力设计资金路径；
- 明确连续挂单后的累计投入、剩余本金、下一期投入和命中回收能力；
- 量化资金断裂概率、最大回撤和恢复周期；
- 让每批方案在选号逻辑和资金管理逻辑上都具备可解释的实质差异。

本协议不允许把“能追几十期”表述为“不容易亏损”的证明。倍投只能改变盈亏分布和资金断裂时间，不能改变彩票底层数学期望。

---

## 11B.2 默认资金假设

用户未另行指定时，标准方案任务采用：

```text
默认本金=5000元
最低有效投注=0.1元
币种=CNY
```

用户明确指定其他本金时，允许覆盖5000元，但必须记录 `bankroll_source=USER_SPECIFIED`。任何路径中的单期投入不得低于软件最低有效投注。

不得把0.01元、0.001元等软件不可执行金额用于制造虚假的超长生存期。

---

## 11B.3 玩法与资金路径必须联合设计

资金管理不得在选号方案完成后机械追加。导演必须先冻结：

- 玩法和位置；
- 每期实际注数；
- 单注金额与单期成本；
- 奖金或返奖模型；
- 理论命中率及其来源；
- 历史命中率；
- 软件支持的直线倍投、高级倍投、状态跳转、复位和封顶能力。

然后为同一方案形成至少四条资金候选：

1. `FLAT`：平投基准；
2. `LIMITED_LINEAR`：有限、分段、非无限递增的直线倍投；
3. `PRESSURE_RELEASE`：先升压、后主动降压的非单调路径；
4. `ADVANCED_STATE`：根据连挂、命中、累计亏损、剩余本金、模拟/真实状态或时间条件切换的状态路径。

四条路径必须是为当前玩法重新计算出来的，禁止跨玩法复制同一倍数表后只更改名称。

---

## 11B.4 路径多样性与创新控制

每批资金路径至少改变两个实质维度，优先改变三个：

- 起投方式；
- 阶段长度；
- 加注触发；
- 增长幅度；
- 中后复位；
- 挂后降压；
- 模拟与真实切换；
- 单期封顶；
- 累计投入封顶；
- 回撤熔断；
- 盈利锁定；
- 重新启动条件。

每套正式方案默认采用“一项选号创新 + 一项资金创新 + 最多一项辅助状态创新”。禁止为了看起来高级而同时堆叠多项首次启用功能。

方案指纹必须同时包含号码、触发、执行、资金、停止和时间/模拟六维。资金路径的状态机、序列、封顶和复位只要实质相同，就视为同一资金指纹。

---

## 11B.5 长连挂确定性压力测试

每条正式候选至少检查：

```text
连续挂10期
连续挂20期
连续挂30期
连续挂40期
连续挂50期
```

每个检查点必须计算：

```text
cumulative_outlay=累计投入
remaining_bankroll=剩余本金
next_period_stake=下一期投入
hit_net_after_streak=下一期命中后的净结果
recovery_complete=是否真正完成亏损回收
survives=剩余本金是否还能支付下一期
```

`remaining_bankroll` 必须等于冻结本金减累计投入。只要下一期无法支付，`survives` 必须为false。

方案可以只选择10、20、30、40或50期中的某个检查点作为正式停止期，但该停止期必须在压力表中存在且 `survives=true`。

---

## 11B.6 命中回收计算

任何“命中后连本带利”“一期回收”“恢复本金”的表述，必须有明确公式：

```text
命中净结果
= 本期毛返奖
- 本期投注成本
- 命中前累计未回收亏损
```

若结果小于0，`recovery_complete` 必须为false。若赔率、注数、返点或奖金模型无法确认，只能比较资金暴露，禁止宣称能够回本。

允许某条路径的目标不是一次完全回收，而是：

- 分阶段回收；
- 命中后先降压；
- 保留本金安全垫；
- 牺牲部分回收速度以降低资金断裂概率。

“高级”不等于倍数更大，也不等于必须一次回本。

---

## 11B.7 四层验证

每个正式方案必须同时完成：

1. **理论概率**：给出玩法理论命中率及组合或规则来源；
2. **确定性压力测试**：计算10/20/30/40/50期连挂；
3. **随机压力模拟**：至少10000条路径，冻结随机种子，报告资金断裂概率和回撤分布；
4. **历史验证**：使用当前可用历史数据，但结论强度受数据成熟度限制。

随机模拟可使用理论命中率的伯努利近似，也可使用经审计的经验重采样。必须记录模型假设，不能把模拟结果包装为未来盈利证明。

---

## 11B.8 数据成熟度分层

### QUICK_EXPERIMENT：1—999期

用途：流程验证、候选初筛、号码来源复算和PPT实验说明。  
禁止：长期安全、稳定性或真实极端连挂结论。

当前约200期历史数据属于本层。系统升级可以完整实施，但历史回测只能标记为快速实验。

### DEVELOPING_SAMPLE：1000—4999期

必须划分训练区和验证区；允许开始比较不同参数的稳定性，但仍不能证明长期有效。

### RESEARCH_SAMPLE：5000—9999期

除训练/验证外，必须做滚动窗口验证；允许进行大样本重新校准和再生成，但必须保留过拟合限制。

### LARGE_SAMPLE：10000期及以上

必须保留方案冻结前不可查看的封存样本外区，并做滚动验证。只有通过封存样本外和随机压力测试后，才允许使用“大样本验证通过”表述；仍不得使用稳赚、必赚或长期不亏承诺。

数据达到1000、5000、10000和30000期时，应触发重新校准、重新生成候选和旧新版本对照，不得只围绕历史赢家做逆向拼装。

---

## 11B.9 选择标准

资金路径评分必须至少考虑：

- 与玩法命中结构的匹配度；
- 软件可执行证据；
- 5000元或用户指定本金下的生存期；
- 最大单期投入；
- 累计投入；
- 最大回撤；
- 资金断裂概率；
- 命中后的回收能力；
- 与最近方案的差异度；
- 视频讲解价值；
- 状态复杂度和误设置风险。

默认选择最高分且满足软件证据、停止期生存和风险预算的路径。低分路径仍被选择时，必须有可核对的覆盖债务或研究价值理由。

---

## 11B.10 强制证据文件

每个标准方案任务必须在运行证据目录生成：

```text
controller/runs/<RUN_ID>/bankroll_stress.json
```

正式模板：

```text
controller/templates/bankroll_stress.template.json
```

机器配置：

```text
controller/bankroll_stress.json
```

验证命令：

```bash
python tools/validate_bankroll_stress.py \
  --evidence controller/runs/<RUN_ID>/bankroll_stress.json
```

仓库CI必须执行：

```bash
python tools/validate_bankroll_stress.py --self-test --scan-runs
python tools/test_bankroll_stress_gates.py
```

`bankroll_stress.json` 未通过前，不得冻结 `design_contract.json`，不得生成正式TXT和PPT。

---

## 11B.11 PPT与对外交付边界

本协议不增加对外文件数量。标准交付仍保持原有三项内容放在同一个完整交付ZIP中：

- 可导入方案文件夹；
- 人工讲解型PPT；
- YouTube发布参考。

完整资金计算、模拟参数和淘汰记录保存在GitHub运行证据中。PPT只需清楚说明：

- 建议本金；
- 最低投注与实际单期成本；
- 选中的资金路径；
- 最大追期或停止条件；
- 关键压力结果；
- 该结果不等于未来收益保证。

禁止为了披露内部审计而额外生成独立技术报告或长篇说明文件。

---

## 11B.12 固定失败状态

- `BANKROLL_STRESS_MISSING`
- `BANKROLL_ASSUMPTIONS_INVALID`
- `DATA_MATURITY_MISMATCH`
- `FUNDING_CANDIDATES_INCOMPLETE`
- `LOSS_STREAK_STRESS_INCOMPLETE`
- `RECOVERY_MATH_UNPROVEN`
- `SIMULATION_INCOMPLETE`
- `HISTORICAL_VALIDATION_OVERCLAIM`
- `SELECTED_PATH_EXCEEDS_BANKROLL`
- `SOFTWARE_EXECUTION_UNPROVEN`

任何失败必须退回导演或合同阶段，禁止通过修改PPT措辞掩盖底层资金问题。
