# 12_双期条件触发与自动化边界协议

版本：V3.9.1

## 12.1 适用范围

凡是方案条件需要同时读取最近两期或多期开奖号码，例如临号、连号、重号、跨期和值、跨度变化、两期差值等，都属于跨期条件触发。

## 12.2 硬规则

1. 不得把双期条件简化成单期高级开某投某，除非能证明两者完全等价。
2. 同一个上一期号码如果可能对应多个不同前置状态，必须判定为单期映射不可无损表达。
3. 未找到已验证自动模板时，允许交付人工触发执行包、外部脚本触发表、PPT和统计报告。
4. 不得为了交付TXT而扩大号码集合、删除触发条件或改写原方案含义。
5. 必须明确当前是否已经触发，未触发时不得让用户立即投。

## 12.3 B391经验

个位临号和值3码五期计划需要同时读取最近两期个位。当前高级开某投某只能稳定表达上一期触发数字映射，因此本批不生成伪自动TXT。

该规则优先级高于“必须生成可导入TXT”的一般倾向。
