# 规则与触发判定

1. 每期结束后只看个位。
2. 取最近两期个位 a、b。
3. 如果 |a-b|=1，则触发。
4. 基数 = (a+b) 的个位数。
5. 投注号码 = 基数-1、基数、基数+1，按0—9环形处理。
6. 连续执行5期；命中后是否提前停止，由执行者自行选择，统计记录必须区分“命中即停”和“固定跑满”。

注意：这个方法的每期自然命中率是3/10=30%。5期内自然至少命中一次约为83.19%。所以“多数5期内会出”不是优势证明，必须和随机3码对照。
