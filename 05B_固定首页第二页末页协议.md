# 05B_固定首页、第二页与末页协议

版本：V3.9.3-FIXED-PAGES-2  
状态：强制执行  
更新时间：2026-08-02  
优先级：本协议只负责固定页位置、画面、复制方式和封面对比度；与 `05A_方案讲解PPT生产协议.md`、历史验收文本或旧固定页规则冲突时，本协议在固定页范围内优先。

---

## 5B.1 本次固定母版

所有正式项目PPT统一使用用户提供的 `cp讲解模板(2).pptx` 三页视觉作为唯一母版来源：

1. 用户模板第1页：未来所有PPT第1页的固定黑色金属背景；
2. 用户模板第2页：未来所有PPT固定第2页；
3. 用户模板第3页：未来所有PPT固定最后一页。

项目标题、短副标题和研究边界仍可在第一页动态生成；正文仍按项目内容动态生成，但必须从第三页开始。第二页和最后一页不属于动态正文，不得重写、重排或美化。

---

## 5B.2 唯一正式资源与物化方式

固定页采用“GitHub内置源数据 + 可重复物化器”保存。源数据位于：

```text
assets/ppt/fixed_pages/source/
├─ cover.*.b64
├─ second.*.b64
└─ end.*.b64
```

运行：

```bash
python tools/materialize_ppt_fixed_pages.py
```

生成：

```text
assets/ppt/fixed_pages/
├─ 首页背景图谱.png
├─ 固定第二页_原页画面.png
├─ 固定最后一页_画面.png
├─ 固定首页模板.pptx
├─ 固定第二页_平台与联系.pptx
├─ 固定最后一页_标准版.pptx
├─ PPT固定首页第二页末页模板_V3.9.3.pptx
└─ PPT固定页资源清单.json
```

源数据、物化器、运行资源或哈希缺失时，任务必须失败并报告 `PPT_FIXED_ASSET_MISSING_OR_CORRUPT`，不得依靠聊天记忆重画，不得用“风格接近”的页面顶替。

---

## 5B.3 第一页：固定背景，动态文字

第一页必须使用：

```text
assets/ppt/fixed_pages/首页背景图谱.png
```

硬性要求：

- 必须写入真正的PPT背景层，例如OOXML中的 `p:bg / a:blipFill`；
- 禁止把背景作为普通可选中、可拖动、可双击调整的全页图片；
- 在PowerPoint编辑模式中，双击页面不得选中背景图；
- 不得改变背景纹理方向、金属密度、色调和明暗关系；
- 允许叠加项目标题、短副标题和一行研究边界，保留原项目内容表达能力；
- 首页禁止黑色或近黑色文字和效果；
- 首页文字必须显式使用白色、浅灰、金色或其他与黑色背景形成足够对比的浅色；
- 禁止依赖主题默认字体颜色，因为主题默认色可能回落为黑色；
- 标题仍按05A执行：准确优先，原则上不超过10字、最多12字；
- 封面不得承载固定第二页内容、长段正文或复杂结果。

生成器使用 `fixed.add_text()` 在固定封面添加文字时，若颜色亮度低于安全阈值，必须直接报错 `PPT_COVER_DARK_TEXT_FORBIDDEN`。

---

## 5B.4 第二页：用户模板原页冻结

所有正式PPT第二页必须使用：

```text
assets/ppt/fixed_pages/固定第二页_平台与联系.pptx
```

第二页屏幕内容必须与用户模板第二页完全一致。

执行方式：把用户模板第二页物化为整页固定背景，以消除字体替换、元素漂移和程序重排风险。第二页不得叠加任何其他屏幕内容，包括但不限于：

- 动态标题、副标题、页码或项目名称；
- 新增文本框、图标、图片、二维码、角标或水印；
- 透明超链接遮罩或其他不可见形状；
- 自动风险提示、备注提示、页眉页脚；
- 对原页文字进行修正、替换、删减、重新断行或重新配色。

屏幕内容以用户提供的原页为准，不再执行旧版“重新设计平台页”“替换原文”或“按禁用词重写”的机制。演讲者备注可以存在，但不得改变屏幕画面。

第二页位置必须严格为第2页。正文动态内容从第3页开始。

---

## 5B.5 动态正文与隐藏附录

页面顺序固定为：

```text
第1页：固定黑色金属背景 + 动态浅色封面文字
第2页：用户模板固定第二页，原样冻结
第3页起：项目正文、案例、数据、风险、建议与结论
结论之后：隐藏技术附录（按需）
最后一页：用户模板固定第三页，原样冻结
```

05A中的“简单技术主讲通常7至9页”只计算动态项目正文，不把固定第二页和固定最后一页计入正文数量；封面单独计算。

隐藏附录必须位于主体结论之后、固定最后一页之前，并保持隐藏状态。

---

## 5B.6 最后一页：用户模板第三页冻结

所有正式PPT最后一张正常播放页面必须使用：

```text
assets/ppt/fixed_pages/固定最后一页_标准版.pptx
```

最后一页屏幕内容必须与用户模板第三页完全一致。

硬性要求：

- 保持原有蓝金实验室视觉、文字、号码球、网址、Telegram和排版；
- 不得重新排版、改色、裁切、换图、替换文字或新增标识；
- 不得添加透明链接遮罩、二维码、页码或任何可见/不可见屏幕对象；
- 不得在其后追加普通播放页面；
- 隐藏技术附录只能放在该页之前；
- 允许添加简短演讲者备注，但不得改变屏幕画面。

---

## 5B.7 生成器兼容规则

现有生成器继续使用以下稳定接口：

```python
fixed.add_cover(prs, cover_png)
fixed.build_second_slide(prs)
fixed.add_end(prs, end_png)
```

接口语义已冻结：

- `add_cover`：创建真正背景层，并启用封面深色文字拦截；
- `build_second_slide`：只创建固定第二页背景和备注，不创建任何屏幕形状；
- `add_end`：只创建固定最后一页背景和备注，不创建超链接遮罩或其他形状。

不得绕过这些接口重新调用旧的程序化第二页布局。

---

## 5B.8 自动校验

必须按顺序运行：

```bash
python tools/materialize_ppt_fixed_pages.py
python tools/validate_ppt_fixed_pages.py
python tools/build_current_delivery.py
python tools/validate_ppt_fixed_pages.py --final-ppt 'dist/B393_SET_001_delivery/*_人工讲解型PPT.pptx'
```

至少检查：

- 所有正式资源存在且SHA-256与清单一致；
- 第一页使用真正图片背景层，不存在普通全页图片；
- 最终PPT首页所有文字显式使用高对比浅色；
- 首页不存在黑色或近黑色文字、描边、阴影或其他文字效果；
- 第二页严格位于第2页；
- 第二页背景图片哈希与用户原页物化画面一致；
- 第二页屏幕形状数量为0，不存在额外内容或透明遮罩；
- 最后一页背景图片哈希与固定结尾画面一致；
- 最后一页屏幕形状数量为0，并且确实为最后一页；
- 三页固定模板严格为封面、第二页、末页三页顺序；
- 当前交付PPT也通过上述结构检查，而不是只校验模板文件。

失败状态包括：

- `PPT_COVER_BACKGROUND_NOT_EMBEDDED`
- `PPT_COVER_DARK_TEXT_FORBIDDEN`
- `PPT_COVER_DARK_EFFECT_FORBIDDEN`
- `PPT_FIXED_SECOND_PAGE_MISSING`
- `PPT_FIXED_SECOND_PAGE_WRONG_POSITION`
- `PPT_FIXED_SECOND_PAGE_EXTRA_CONTENT`
- `PPT_FIXED_SECOND_PAGE_HASH_MISMATCH`
- `PPT_FIXED_END_PAGE_MISSING`
- `PPT_FIXED_END_PAGE_NOT_LAST`
- `PPT_FIXED_END_PAGE_EXTRA_CONTENT`
- `PPT_FIXED_END_PAGE_HASH_MISMATCH`
- `PPT_FIXED_ASSET_MISSING_OR_CORRUPT`

---

## 5B.9 最终原则

固定页不是“参考风格”，而是不可近似替代的正式组件。

> 第一页保留动态内容，但所有文字和效果必须避开黑色；第二页必须是用户原页且不得加内容；最后一页必须是用户原页且必须收尾。
