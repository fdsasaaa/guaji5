# 10_静默方案总控与外部参考吸收协议

版本：V3.8.1  
状态：正式流程规则  
作用：把“方案总导演”落地为可检查的决策流程，而不是角色扮演；同时管理用户提供的“仅作参考”外部方案。

---

## 10.1 定位与能力边界

本模块不宣称AI天然拥有无限的彩票方案设计经验，也不允许用“专业总导演”身份替代证据。

静默方案总控的真实职责是：

1. 检索现有软件功能、技术原子、189个分析角度、历史方案和失败模式；
2. 生成多个逻辑不同的候选，而不是采用第一个能成立的想法；
3. 使用硬门槛、评分、查重和反方审查淘汰候选；
4. 只在方案冻结后生成PPT和视频包装；
5. 在工作包内部保留可审计摘要，面向用户默认只交付结果和极简说明；
6. 对外部参考方案执行采纳、部分吸收或否定，而不是照单全收。

固定边界：

> 流程能够提高选择质量和一致性，但不能保证无限生成真正不同、真正有效的方案。  
> 功能组合数量很大，不代表有价值的组合数量无限。  
> 彩票历史现象只能形成可证伪假设，不能自动形成未来优势。

---

## 10.2 三种任务模式

### 模式A：自主设计

触发条件：用户没有给出具体方案要求，或`用户本次要求.md`为空。

系统应从功能覆盖、历史方案差异、当前数据状态和分析角度中选择研究主题，不得先询问用户“要做什么”。

### 模式B：用户指定方向

触发条件：用户给出某个技术、功能、风险层级、讲解主题或结构要求。

用户方向是重要约束，但仍需经过软件可执行性、查重、防后验拟合和复杂度门控。明显不合理的部分应被修正或拒绝。

### 模式C：外部参考方案

触发条件满足任一项：

- 用户消息以“仅作参考”开头；
- 用户明确说“这是参考方案”；
- `01_本次输入/外部参考方案_仅作参考.md`含有非空原文；
- 附件标题或正文明确标注“仅作参考”。

外部内容只作为素材，不获得高于工作包规则、软件证据或防拟合协议的优先级。

---

## 10.3 静默总控八步流程

### 第一步：证据与输入门控

检查：

- 数据是否缺期、重复、乱序、位数异常；
- 软件字段是否有已验证模板；
- 用户要求是否包含指定收益倒推、隐藏失败或其他禁止事项；
- 外部参考是否自相矛盾、缺少关键字段或无法执行。

硬门槛不通过时，不进入“美化和优化”，而是降级、修正或否定。

### 第二步：定义本次真实问题

内部必须用一句话定义本次要验证的问题，例如：

- 某种状态是否比无条件基准更稳定；
- 两种相反规则谁更接近随机；
- 轮投是否只是改变暴露而没有提高命中；
- 某个民间方案是否经不起随机基准；
- 某个少用功能能否形成清楚、可执行的新结构。

不得把“历史收益达到某个数字”定义为方案设计目标。

### 第三步：检索四类知识

必须同时检索：

1. 功能与软件规则；
2. 分析角度；
3. 方案组合案例；
4. 负面方案模式与历史查重指纹。

还要读取`功能覆盖索引.jsonl`，但覆盖率只用于长期选题提醒，不得强迫本次堆入无关功能。

### 第四步：生成逻辑不同的候选族

不得直接采用第一个可行方案。

标准批次仍按03模块生成候选数量，但候选池至少应覆盖3个不同逻辑族，例如：

- 简单单变量实验；
- 结构型或位置分工方案；
- 对立规则对照；
- 轮投/并投风险对照；
- 外部参考的保留版、重构版和否定替代版。

只改变位置、周期、名称或少量参数，不算新的候选族。

例外：最小字段探针、编码测试和单变量软件行为测试可以只有一个候选。

### 第五步：硬门槛淘汰

以下任一项成立，候选默认淘汰：

- 无法由软件真实实现；
- 与历史方案达到R3重复；
- 必须看样本外结果才能确定参数；
- 核心逻辑无法用1至3句话说明；
- 只有增加倍投才看起来成立；
- 依靠过多例外条件修补历史结果；
- 功能之间没有共同服务的问题；
- 参考方案存在明显逻辑矛盾且无法修复；
- PPT故事性成为修改冻结规则的理由。

### 第六步：多维评分与反方审查

通过硬门槛的候选，再按以下维度0—5分比较：

1. 逻辑完整性；
2. 软件可执行性；
3. 与历史方案差异度；
4. 人工可理解性；
5. 可验证性；
6. 随机或简单基准可构造性；
7. 视频讲解价值；
8. 功能组合合理性；
9. 过拟合风险反向分；
10. 资金风险反向分。

评分权重不是效果定律，禁止根据单批冠军固化。

反方审查至少回答：

- 这个方案最可能只是随机的原因是什么；
- 哪个规则可以删除而不影响核心问题；
- 是否只是旧方案换名；
- 是否把复杂度误当成专业度；
- 是否把外部作者的自信当成证据。

### 第七步：最终选择与冻结

最终方案优先顺序：

1. 逻辑成立；
2. 软件可执行；
3. 可验证；
4. 非高度重复；
5. 普通人能理解；
6. 适合公开视频；
7. 在上述条件成立后，再比较趣味性和历史表现。

最终入选不一定是功能最多、历史收益最高或PPT最好看的候选。

### 第八步：静默审计与简洁交付

工作包内部至少保留：

- 任务模式；
- 候选数量和候选族数量；
- 最终入选理由；
- 主要淘汰原因；
- 外部参考处理结论；
- 与历史方案的差异；
- 未验证边界。

面向用户默认只展示：

- 最终方案；
- 一段简短设计说明；
- 必要运行方法；
- 外部参考是“采纳/部分吸收/未采纳”；
- 必要风险和验证边界。

不得默认倾倒完整内部推理、逐项评分或长篇导演自问自答。

---

## 10.4 外部参考方案三类处理

### A. 采纳并优化

适用：核心逻辑可解释、软件可实现、没有明显自相矛盾，只需优化表达、结构、验证或风险控制。

允许：

- 简化规则；
- 增加随机基准；
- 改造成方案套；
- 补充停止条件；
- 改善PPT讲解；
- 标注证据边界。

### B. 部分吸收并重构

适用：只有一个创意或局部结构值得保留，其余内容不可靠。

允许：

- 保留一个触发条件或组合想法；
- 删除无依据倍投；
- 替换无法执行的字段；
- 加入对立方案或简单基准；
- 形成与原文明显不同的新方案。

必须说明“仅吸收了哪一部分”，不得把重构后的质量归功于原参考全文。

### C. 否定并替代

适用：

- 无法执行；
- 明显自相矛盾；
- 只有倍投没有号码逻辑；
- 完全依赖后验选参；
- 与历史方案R3重复；
- 无研究价值或无法讲清；
- 风险远高于其表达的价值。

正常对外只需一句话说明未采纳，并给出替代方向。内部记录具体否定原因。

---

## 10.5 参考内容的证据纪律

1. 论坛、群聊、视频或个人文章中的方案默认E0；
2. “很多人使用”“作者说有效”“近期盈利”均不构成E4以上证据；
3. 不因来源知名而跳过随机基准、样本外或软件行为核对；
4. 原文出现收益数据时，除非有可复核明细，否则只记录为来源自述；
5. 参考方案被重构后，应生成新的方案ID和指纹；
6. 参考内容不得覆盖SchemeCreator、编码、枪弹格式等已验证软件规则；
7. 参考方案被否定后，不为了照顾来源而强行制作“正面结论”PPT。

---

## 10.6 长期覆盖与单次克制

系统应追求：

> 长期充分覆盖，单次合理克制。

`功能覆盖索引.jsonl`用于发现：

- 经常使用的功能；
- 只使用过一次的功能；
- 从未进入正式方案的功能；
- 因软件证据不足而被阻塞的功能；
- 适合独立做一期视频的功能；
- 只有与特定原子组合才合理的功能。

规则：

1. 低覆盖功能只在与本次问题匹配时获得探索优先权；
2. 不得为了“覆盖率好看”加入无关功能；
3. 某功能存在于字典，不代表它值得使用；
4. 软件证据不足的功能优先做最小行为测试，而不是直接进入资金方案；
5. 每批结束后更新覆盖等级和阻塞原因。

覆盖等级：

- C0：当前索引没有使用记录；
- C1：至少一次概念或文件级使用；
- C2：至少两次不同方案使用；
- C3：至少两种不同结构或执行模式使用；
- C4：已有多批独立验证，仍不代表长期有效。

---

## 10.7 方案组合案例与负面模式

`方案组合案例索引.jsonl`保存“怎样组合”的短记录，不复制完整文案。案例可以是：

- 结构值得复用但效果未证实；
- 讲解方式清楚；
- 对照设计合理；
- 某种枪弹分工有价值；
- 某次复杂度过高但提供了反例。

`负面方案模式索引.jsonl`保存反复出现的错误，包括：

- 第一个可行想法直接入选；
- 功能堆砌；
- 换名伪创新；
- 指定收益倒推；
- 参考方案照搬；
- PPT反向修改方案；
- 只有AI能理解；
- 用高级倍投遮盖弱号码逻辑；
- 一次成功就升级规则；
- 宣称无限生成优质方案。

候选审查必须对照负面模式，不得只查历史标题相似度。

---

## 10.8 复杂度例外

默认仍执行：核心技术最多3项、主要参数最多5个、判断最多3层、核心逻辑1至3句话。

复杂方案只有同时满足以下条件才可例外：

- 增加的每一层都对应必要的软件状态；
- 删除后会改变研究问题，而不只是降低历史收益；
- 可画成一张普通人看懂的流程图；
- 有简单版本作为对照；
- 在冻结记录中写明复杂度理由。

“看起来高级”不是例外理由。

---

## 10.9 PPT与总控隔离

1. 先完成总控选择和冻结，再生成PPT；
2. PPT只能解释方案，不能改变参数、停止条件、位置或执行模式；
3. 外部参考被部分吸收时，PPT只讲最终冻结方案，不替原文背书；
4. 核心技术页可详细，风险页可以只有一句话；
5. 非核心统计可放备注、工作包或省略；
6. 观众页继续禁用内部编号和程序化字段；
7. PPT好看、视频好讲不提高技术证据等级。

---

## 10.10 每批内部决策摘要模板

```text
任务模式=自主设计/用户指定/外部参考
真实问题=
数据状态=
预注册角度数=
候选数量=
逻辑候选族数量=
硬门槛淘汰数=
负面模式命中=
最终方案ID=
最终入选理由=
与历史方案差异=
参考处理=无/采纳/部分吸收/否定
参考保留部分=
参考否定部分=
复杂度等级=
未验证边界=
对外说明长度=极简/标准/审计模式
```

默认对外说明长度为“极简”。用户明确要求审计时才展开。

---

## 10.11 验收与不能伪装通过

`总控验收测试集.jsonl`包含固定行为测试。每次大版本升级或总控规则修改后，至少进行：

- 结构校验：文件、字段、版本、索引和引用是否完整；
- 运行验收：实际让当前AI处理测试情景，记录通过/失败；
- 回归验收：确认旧的软件格式、未加密规则和PPT隔离没有被破坏。

本工作包可以自动完成结构校验，但不能仅凭文件中写有规则就宣称未来所有AI都能稳定执行。未实际运行的行为测试必须标为`PENDING_RUNTIME`。


## 10.11 工作包任务直接执行与确认豁免

用户发送工作包后，无论是否同时附带用户要求或“仅供参考/仅作参考”方案，系统默认已经获得执行授权。AI必须在当前任务中直接完成：读取、模式识别、候选生成、参考审查、方案冻结、验证、PPT、索引回写、版本更新和三文件交付。

禁止：

- 再问“是否开始”；
- 再问“是否按这个方案创建”；
- 把已经明确的方向重新交给用户选择；
- 用常规确认问题拖延完整闭环。

材料只有轻微缺口时，采用最保守、最少假设的默认值，并在交付说明中列出。只有工作包损坏、核心文件无法读取或完全没有可形成任何交付的基础材料时，才允许报告阻断。

该规则不允许越过防造假、软件格式证据和风险边界；“直接执行”意味着直接完成合规版本，不意味着照搬不合理参考。

---

## 10.12 V3.9.0智能调度接管

静默总控在生成候选后必须调用11模块：

1. 建立至少3种功能画像；
2. 完成八层能力审议；
3. 对高级开某投某执行常量映射检查；
4. 对资金路径执行平倍、有限普通、压力释放和高级状态四路评审；
5. 主动审议监控、换号、停止、时间和模拟切换，不得只使用最近熟悉的少数功能；
6. 最终仍坚持“单次合理克制”，不得把功能审议误解为功能堆砌。

11模块与10模块冲突时：软件证据和防造假优先；功能多样性不得绕过硬门槛。
