# 03_思路生成与防后验拟合协议

版本：V3.8.0  
目标：先形成可证伪候选，再验证；不围绕结果反向拼方案。

---

## 3.1 原V3.3.0思路生成、分区、候选、查重与冻结规则（原意完整保留）

# 3. 双引擎思路生成系统

思路生成不得只依赖一种方式。系统同时保留两条路线。

## 3.1 技术组合驱动引擎（主要路线）

顺序：

**观察训练区数据状态  
→ 从字典选择技术原子  
→ 形成合理组合  
→ 再反向提炼验证问题和视频选题**

适合：

- 充分使用规则字典；
- 扩大组合空间；
- 产生新结构；
- 避免长期围绕少数问题打转。

默认组合规模：

- 一级类别：1～3个；
- 核心技术：1～3项；
- 三级技术：仅在必要时加入；
- 主要参数：3～5个以内；
- 判断步骤：最多3层；
- 位置、周期、冷热、遗漏、轮投等辅助条件：只保留真正必要的部分。

## 3.2 问题驱动引擎（辅助路线）

顺序：

**提出观众容易理解的问题  
→ 选择能够验证该问题的技术  
→ 生成冻结方案**

适合：

- 热号是否会继续热；
- 冷号是否更容易回补；
- 杀号是否比选号稳定；
- 短周期是否优于长周期；
- 连续状态后顺势或反向哪个更合理；
- 两种相互矛盾的技术谁更有效。

优点：

- 易懂；
- 易形成标题；
- 自带冲突；
- 适合系列化内容。

限制：

- 不能作为唯一生成核心；
- 不能反复围绕同类问题换名字；
- 不能因为某个问题容易做视频，就忽略字典中的其他技术空间。

## 3.3 默认候选池配比

为保证技术多样性与内容可读性，默认可采用：

- 约70%：技术组合驱动；
- 约30%：问题驱动。

该比例只是默认工作值，不是统计定律。用户可按频道阶段调整。

---
# 4. 数据分区与防后验拟合协议

这是整个系统的最高优先级规则之一。

## 4.1 必须先分区，后生成

收到完整历史数据后，先按时间顺序划分：

- **训练区**：允许观察，用于描述状态和生成候选；
- **验证区**：用于初步筛选；
- **样本外区**：方案冻结后才允许查看；
- **未来跟踪区**：发布后新增的数据，作为真正的前向检验。

建议默认比例：

- 训练区：50%～60%；
- 验证区：20%～25%；
- 样本外区：20%～25%。

如数据量很小，应减少结论强度，而不是强行拆分并夸大结果。

## 4.2 禁止行为

禁止：

- 先查看全部结果，再挑最匹配的技术；
- 为达到指定盈利率反复调整同一方案；
- 根据失败结果不断增加例外条件；
- 偷换周期、位置、参数或停止条件而不升版本；
- 只保留成功版本，删除失败版本；
- 把样本内优化结果称为“技术有效”；
- 用一次短期盈利证明长期优势。

## 4.3 盈利范围的正确用途

允许：

> 先独立生成并冻结候选池  
> → 统一验证  
> → 再按真实收益分类和筛选内容

不允许：

> 先要求盈利20%  
> → 再反复修改规则  
> → 直到历史回测达到20%

固定原则：

> **可以按收益筛选方案，但不能按收益拼凑方案。**

---
# 5. 候选方案生成协议

## 5.1 默认候选数量

根据任务规模生成：

- 快速实验：10～15个；
- 标准批次：20～30个；
- 大型研究：30～50个。

不得只生成一个方案后围绕它反复修改。  
候选池必须包含不同技术逻辑，而不是同一逻辑的参数微调。

## 5.2 每个候选必须包含

- 候选ID；
- 核心假设；
- 一级类别；
- 二级/三级技术；
- 位置范围；
- 统计周期；
- 选号或杀号方向；
- 枪/子弹结构；
- 轮投或组合结构；
- 投注规则；
- 停止条件；
- 参数；
- 设计理由；
- 预期验证的问题；
- 与历史方案的差异；
- 初步过拟合风险；
- 初步复杂度评分。

## 5.3 复杂度上限

公开视频候选默认限制：

- 一级类别最多3个；
- 核心技术最多3项；
- 主要参数最多5个；
- 判断步骤最多3层；
- 核心逻辑必须能用1～3句话解释；
- 必须能够画成一张简单流程图；
- 不允许使用无法用普通语言说明的复杂数学结构；
- 不允许为了改善历史表现不断增加例外条件。

固定原则：

> **规则简单、理由清楚、结果意外、验证完整，比复杂更有价值。**

## 5.4 数据观察的正确表达

AI可以描述训练区现象，例如：

- 某些位置近期冷热差异扩大；
- 某类遗漏暂时集中；
- 重号比例阶段性升高；
- 不同位置出现相反状态；
- 短周期与长周期判断冲突。

但不得直接说：

- 因此下一期必然出现某号码；
- 因此这个技术一定有效；
- 当前状态证明存在稳定规律。

数据状态只用于提出“值得验证的假设”，不是结论。

---
# 6. 方案指纹与语义查重协议

每套方案必须生成标准化“方案指纹”。

## 6.1 指纹字段

至少包括：

- 一级类别组合；
- 核心技术组合；
- 位置范围；
- 统计周期；
- 选号/杀号方向；
- 单方案/组合方案；
- 枪/子弹结构；
- 出号/轮投结构；
- 投注与资金规则；
- 参数区间；
- 停止条件；
- 核心假设；
- 验证问题；
- 结果分类。

推荐标准串：

```text
FP=一级类别|核心技术|位置|周期|方向|枪弹结构|轮投结构|投注规则|停止条件|核心假设
```

## 6.2 重复判定

以下情况视为重复或高度重复：

- 只修改方案名称；
- 只改变字段排序；
- 只更换无关紧要的参数；
- 只把万位改成千位，但验证逻辑完全相同；
- 换一种话术表达同一个核心假设；
- 枪结构不同，但内部子弹逻辑高度相同；
- 结果不同，但方案生成逻辑相同。

## 6.3 新方案最低变化要求

新方案至少应改变两个核心维度，优先改变三个：

- 核心技术关系；
- 验证问题；
- 周期结构；
- 位置结构；
- 枪弹或轮投关系；
- 投注/停止逻辑。

重复技术允许再次使用，但组合关系、验证问题或结构必须产生实质变化。

## 6.4 重复等级

- **R0：无明显重复**，可直接生成；
- **R1：部分技术重复**，但假设或结构明显不同，可生成并说明差异；
- **R2：高度相似**，默认禁止，除非用户明确要求做复现实验；
- **R3：核心逻辑相同**，禁止换名重复生成。

当候选池无法满足新颖度要求时，AI应明确告诉用户“当前字典的有效组合空间正在收窄”，并建议新增技术原子或改变验证维度，而不是制造伪创新。

---
# 7. 方案冻结器

候选方案通过查重后，立即冻结。

冻结字段：

- 方案ID；
- 版本号；
- 生成时间；
- 使用的数据范围；
- 完整规则；
- 所有参数；
- 投注与停止条件；
- 枪弹与轮投结构；
- 预设验证指标；
- 样本外区间；
- 随机基准规则；
- 文件哈希或文本指纹（条件允许时）。

冻结后：

- 不允许根据回测结果直接修改；
- 任何修改都必须生成新版本；
- 新版本必须记录修改原因；
- 原版本必须保留；
- 不允许删除失败记录；
- 不允许把多个失败迭代合并伪装成一个成功方案。

版本示例：

```text
EXP-20260731-001-A-V1
EXP-20260731-001-A-V2
```

V2必须视为新的独立候选，并重新进行完整验证。

---

---

## 3.2 样本外数据消耗规则

样本外不是一个永久标签，而是一种“尚未参与决策”的状态。

只要某个样本外窗口被用于以下任一行为，就视为已经消耗：

- 选择哪个方案；
- 淘汰哪个方案；
- 修改参数；
- 修改位置、周期、方向、触发条件或停止条件；
- 判断哪个候选最好；
- 决定是否公开视频；
- 决定视频用失败、普通或盈利案例；
- 人或AI查看结果后影响后续设计；
- 用于确认某种新默认值。

被消耗后：

1. 该窗口可以保留为历史参考；
2. 不得在后续批次继续称为“独立样本外”；
3. 后续独立验证只能使用：
   - 新增的后续开奖；
   - 尚未查看的新滚动窗口；
   - 预先登记的另一独立数据段；
4. 反复查看几十次的窗口，必须标记为“研究已用数据”。

---

## 3.3 样本外窗口账本

每个窗口必须记录：

```text
oos_window_id=
数据起止=
首次冻结时间=
首次查看时间=
consumed=true/false
consumed_at=
consumed_by=用户/AI/程序
consumed_reason=
影响的方案ID=
independence_status=独立/已消耗/历史参考
```

规则：

- 窗口未登记前不得称为独立样本外；
- 方案冻结时间必须早于首次查看时间；
- 同一方案V2若受V1样本外结果启发，V2不能复用该窗口；
- 同批候选统一查看同一窗口后，该窗口对整个批次均已消耗；
- 样本外结果进入视频选片，也算消耗。

---

## 3.4 候选池透明度账本

每个公开视频方案必须记录：

```text
批次ID=
本批候选总数=
不同核心逻辑数量=
同一核心逻辑版本数=
当前方案收益排名=
当前方案风险排名=
是否经过收益筛选=
用户是否看过其他版本结果=
AI是否看过其他版本结果=
失败版本数量=
当前方案为何被选中=
是否属于冠军展示=
是否同时公开失败/普通对照=
```

禁止：

- 生成100个候选，只说“发现一个盈利方案”；
- 把同一逻辑的50个参数版本说成50种技术；
- 隐藏失败版本；
- 只展示收益冠军而不披露候选规模；
- 把收益排名包装成独立统计证据。

---

## 3.5 候选池生成纪律

候选池应优先包含不同核心逻辑，而不是参数网格：

- 10候选测试要求10个不同核心逻辑；
- 同一核心逻辑最多保留少量预先登记版本；
- 若确需参数敏感度版本，必须另列为“敏感度分支”，不计入核心逻辑数量；
- 候选生成结束后一次性冻结；
- 不得查看一个、调整一个、再生成一个。

---

## 3.6 指定收益要求的处理

用户可以要求“从冻结候选中寻找历史收益在某范围的案例”，但不能要求AI通过反复修改达到该范围。

允许流程：

```text
独立生成候选池
→ 全部冻结
→ 统一验证
→ 按真实结果分类
→ 从已有结果中选择内容案例
```

禁止流程：

```text
设定盈利20%
→ 调周期
→ 调号码
→ 加过滤器
→ 改停止条件
→ 直到历史达到20%
```

若用户提出指定收益，AI必须指出：

- 这会增加后验拟合；
- 推荐先固定候选数量和规则；
- 最终可能没有候选落入指定范围；
- 没有合适候选时不得伪造。

---

## 3.7 冻结包最小内容

每个冻结方案至少包括：

- 方案ID；
- 版本；
- 生成时间；
- 批次ID；
- 候选序号；
- 核心逻辑编号；
- 训练数据范围；
- 验证区范围；
- 独立样本外窗口ID；
- 完整软件TXT；
- 枪弹目录树；
- 顶部GUI轮投要求；
- 所有参数；
- 投注和停止条件；
- 预设指标；
- 随机基准条件；
- 方案指纹；
- TXT哈希；
- 冻结人/AI；
- 是否已查看任何验证结果。

修改任何核心字段都必须升版本。  
V2是新候选，不得覆盖V1。

---

## 3.8 查重顺序

1. 优先读取 `历史方案索引.jsonl`；
2. 规范化当前指纹；
3. 计算字段相似度；
4. 比较核心假设和验证问题；
5. 查找相似方案ID；
6. 再读取 `06_历史方案记忆.md`理解上下文；
7. 输出R0-R3；
8. R2默认停止，除非明确做复现实验；
9. R3禁止换名生成。

重复等级：

- R0：无明显重复；
- R1：部分原子重复，但假设或结构明显不同；
- R2：高度相似；
- R3：核心逻辑相同。

---

## 3.9 默认参数元数据

下列参数必须带统一标记：

| 参数 | 暂定值 |
|---|---|
| 技术驱动/问题驱动 | 70%/30% |
| 快速候选 | 10-15 |
| 标准候选 | 20-30 |
| 大型候选 | 30-50 |
| 主要参数上限 | 5 |
| 判断层级上限 | 3 |
| 核心技术上限 | 3 |
| 新颖度变化维度 | 至少2，优先3 |

每项均写：

```text
状态=暂定默认值
证据=尚未完成足够真实批次验证
允许调整=是
复审条件=完成3个批次或30个实验后
```

---

## 3.10 防数据泄漏检查

冻结前确认：

- 没有读取样本外号码分布；
- 没有用完整数据决定冷热阈值；
- 没有用结果挑位置；
- 没有因收益改停止条件；
- 没有从视频标题倒推实验结果；
- 没有把旧样本外当新样本外；
- 没有让历史失败版本消失；
- 候选数量和版本数量可追溯。

任何一项失败，样本外证据不得标E5。

---

## 3.11 分析角度路由引擎

原“双引擎”继续保留，但在其前增加分析路由层：

```text
数据审计
→ 玩法与软件门控
→ 分析角度预注册
→ 训练区状态分析
→ 技术组合驱动或问题驱动转译
→ 方案套候选
```

分析路由不是第四种预测技术，而是选择研究入口的方法。

---

## 3.12 预注册角度池

每批必须记录：

- 分析角度索引总数；
- 按玩法过滤后的数量；
- 按软件过滤后的数量；
- 实际进入训练区计算的角度数量；
- 入围角度；
- 被淘汰角度及原因；
- 是否看过其他角度结果；
- 是否存在异常冠军。

不能计算189个角度后只公开一个冠军。

---

## 3.13 套装候选池

从本版本开始，候选计数以“套装”为主、子方案为辅：

```text
候选套装总数=
子方案总数=
不同分析角度族数=
不同核心逻辑数=
同逻辑参数版本数=
轮投候选数=
并投候选数=
枪编排候选数=
高级倍投候选数=
```

默认10候选真实闭环应解释为10个候选套装或10个明确独立的套装结构，不是10个随意TXT。

---

## 3.14 方案套构造纪律

每个套装应尽量包含：

1. 主假设子方案；
2. 至少一个对立或替代假设；
3. 一个随机、平倍或简单基准；
4. 明确的执行方式；
5. 统一资金上限；
6. 套装级停止条件。

不要求每套机械包含三个子方案；但只有一个子方案时，必须说明为何不适合套装化。

---

## 3.15 轮投和并投必须分别冻结

以下字段属于方案指纹：

- 顶部GUI方案轮投状态；
- 子方案勾选集合；
- GUI排序；
- 枪文件是否启用；
- 并投时重叠注单如何处理；
- 各子方案倍投状态是否独立；
- 各方案初始局数。

从轮投改为并投，必须升版本并重新验证。

---

## 3.16 投注路径不能参与选角度

正确顺序：

```text
先冻结号码/触发逻辑
→ 用平倍判断选号逻辑
→ 再选择普通或高级倍投
→ 做技术×资金路径对照
```

禁止：

- 通过激进倍投把随机号码方案包装成盈利；
- 看完连错后定制恰好覆盖该连错的倍率；
- 只报告高级倍投，不报告平倍基线；
- 根据样本外最大回撤修改熔断点后仍称原样本外。

---

## 3.17 无合理角度时的输出

AI允许并应当输出：

> 本批次在预注册角度池中，没有发现同时满足软件可实现、样本充分、与历史方案不重复和可建立随机基准的候选。

不得为了交付文件强制拼出一个方案。



---

## 3.15 V3.8.0候选竞争与静默输出

1. 除最小软件探针外，禁止第一个可行候选直接冻结；
2. 标准批次候选池至少覆盖3个逻辑候选族；
3. 硬门槛先于评分，无法执行、R3重复、样本外泄漏和指定收益倒推不能靠高总分挽救；
4. 每个候选必须对照`负面方案模式索引.jsonl`；
5. 候选池数量、逻辑族数量、主要淘汰原因和最终选择理由保存在工作包内部；
6. 面向用户默认不展示完整导演问答和逐项评分，只给最终方案和简短说明；
7. 用户要求审计时，可以展示结构化选择依据，但仍不输出无用的长篇思维过程；
8. 外部参考方案按10模块同时进入保留、重构或否定路径，不得照搬后再补理由。

候选透明的含义是“可在工作包中审计”，不是“每次都把全部中间过程铺到聊天窗口”。

---

## 3.18 V3.9.0候选功能画像多样性

标准候选池除3个逻辑族外，还必须至少覆盖3种功能画像：基准画像、状态画像、执行或资金画像。

同一组“号码逻辑+监控状态+执行结构+资金路径+停止方式”连续3批重复时施加重复惩罚。仍然入选必须说明它为何比低覆盖功能更匹配本次问题。

功能画像不能用收益排名决定；先冻结画像，再统一验证。
