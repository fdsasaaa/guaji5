# 15_单一完整包、投注数字与资金路径可见性协议

版本：V3.9.7-DELIVERY-FUNDING-VISIBILITY-1  
状态：最高优先级用户交付与资金路径覆盖规则  
适用：所有 `STANDARD_SCHEME_TASK`

---

## 15.1 用户侧只允许一个下载入口

标准方案完成后，助手对用户只发送**一个完整ZIP下载链接**。不得再并列发送方案ZIP、PPTX、验证JSON、GitHub Actions制品包等多个链接。

完整ZIP根目录必须严格只有3项：

1. 一个自然命名的方案文件夹；
2. 一个对应的讲解PPTX；
3. 一个YouTube SEO TXT。

禁止：

- 外层再放“方案套.zip”形成二级ZIP；
- 把验证JSON、清单、记录模板、README、内部探针放进用户完整包；
- 用户没有要求时额外发送分项下载链接。

机器合同：`controller/external_delivery_contract.json`。

---

## 15.2 方案文件夹只保留可导入文件

用户完整包中的方案文件夹只允许包含挂机软件实际需要导入的文件及其必要子目录。

默认禁止：

- README、使用说明、核对表；
- CSV记录模板；
- 验证JSON、manifest；
- 随机对照；
- 默认关闭的行为探针；
- 任何嵌套ZIP。

随机对照、测试探针和审计记录可以保存在GitHub内部或CI制品中，但不得污染用户最终方案文件夹。

---

## 15.3 投注数字必须三重可见

任何包含固定或冻结投注数字的方案，数字必须同时出现于：

1. **文件名**：例如 `百位269-定码轮换.txt`；
2. **TXT真实投注字段**：例如 `定码轮换内容=2 6 9`；
3. **PPT主讲页面**：明确写出百位、十位、个位实际投注数字。

不得只在内部JSON、备注、二级ZIP或算法说明中出现数字。用户打开完整包后，无需搜索即可确认本轮投注内容。

动态选号方案若不存在固定数字，必须在文件名和PPT中明确显示动态规则及实际软件字段，禁止用抽象技术名代替真实投注内容。

---

## 15.4 SEO文件固定为一套成品

SEO TXT只包含：

- 1个标题；
- 1行英文逗号分隔的8—10个标签；
- 1份描述。

不得在用户最终包中提供3—5个标题备选、多个描述版本或内部发布说明。用户要的是可直接复制发布的单一成品。

---

## 15.5 平倍不再是自动默认答案

`1,1,1,1...`可以作为隔离选号逻辑的基准，但不得因为证据等级高、实现简单或风险较低而自动成为每批最终资金路径。

自主生成任务中：

1. 必须先形成FLAT、LIMITED_LINEAR、PRESSURE_RELEASE、ADVANCED_STATE四路候选；
2. 若用户明确要求资金路径具有变化，最终用户交付不得继续使用全1序列；
3. E2普通倍投允许作为**受控交付实验**，但不得伪装成E3成熟能力；
4. 受控实验必须单独生成 `delivery_execution_contract.json`；
5. 最高倍数默认不超过3倍；
6. 必须有固定期数硬停止、完整毛暴露预算和首次导入核对；
7. 禁止无限马丁、无上限递增和“精确回本”叙事。

成熟证据基准与用户交付实验可以分层：

- `function_orchestration.json`保存软件成熟度基准和四路审议；
- `delivery_execution_contract.json`保存本次实际写入TXT的资金路径；
- 两者不得相互冒充，实际TXT、PPT和用户资金披露必须以后者为准。

---

## 15.6 默认温和压力释放模板

用户未指定具体序列、但明确要求不要机械平倍时，默认候选为：

```text
1,1,2,3,2,1,1,1
```

设计含义：

- 前两步保持低压；
- 中段升至2倍、3倍；
- 随后主动降至2倍、1倍；
- 末段回到低压，不持续追损。

固定边界：

- 最高3倍；
- 8步总倍数12；
- 若每期基础暴露3元，一轮毛暴露36元；
- 24期三轮毛暴露预算108元；
- 满24期停止；
- 资金路径不改变号码中奖概率。

该模板只是默认受控候选，不得机械复制到所有未来方案。未来仍应根据命中率、注数、赔率、观察期数和软件证据重新设计。

---

## 15.7 强制机器验收

每次交付必须运行：

```bash
python tools/validate_external_delivery.py \
  --delivery-zip "dist/<项目>_交付/<项目>_完整交付.zip" \
  --execution-contract "controller/runs/<RUN_ID>/delivery_execution_contract.json"
```

失败状态包括：

- `MULTIPLE_EXTERNAL_DOWNLOADS`
- `DELIVERY_ROOT_NOT_EXACTLY_THREE`
- `NESTED_SCHEME_ZIP_PRESENT`
- `SCHEME_FOLDER_INTERNAL_FILES_PRESENT`
- `BETTING_NUMBERS_NOT_VISIBLE`
- `TXT_FUNDING_SEQUENCE_MISMATCH`
- `AUTONOMOUS_FLAT_DEFAULT_REPEATED`
- `SEO_NOT_SINGLE_READY_TO_PUBLISH_SET`

任何一项失败，不得称为完成交付。
