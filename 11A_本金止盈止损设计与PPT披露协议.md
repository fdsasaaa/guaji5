# 11A_本金止盈止损设计与PPT披露协议

版本：V3.9.2-CAPITAL-STOP-1  
状态：强制执行  
适用范围：所有正式挂机方案、方案套、资金路径实验版及对应人工讲解型PPT。  
定位：在方案冻结前完成建议本金、止盈和止损决策，并把最终金额准确写入同一PPT。

---

## 11A.1 核心原则

每个正式挂机方案都必须完成资金规模审议，不能只设计号码规则、倍投序列或停止字段而不说明需要多少本金。

最终必须冻结：

1. 建议本金金额；
2. 止盈止损模式；
3. 若启用止盈，止盈金额；
4. 若启用止损，止损金额；
5. 金额的计算依据和执行边界。

建议本金是方案在冻结参数、投注单位和资金路径下的测试或运行资金建议，不是盈利保证。

---

## 11A.2 四种允许模式

止盈止损必须从以下四种模式中明确选择一种：

1. `NONE`：不设置止盈，也不设置止损；
2. `STOP_LOSS_ONLY`：只设置止损；
3. `TAKE_PROFIT_ONLY`：只设置止盈；
4. `BOTH`：同时设置止盈和止损。

不得保持“以后再说”“视情况设置”或字段空缺的未决状态。

不设置某项时，必须记录不设置理由，例如：

- 本批只验证号码逻辑，采用固定期数结束；
- 软件止盈或止损行为证据不足；
- 当前路径使用人工观察，不启用自动金额停止；
- 止盈会过早截断本次前向样本；
- 已使用更严格的局数封顶或熔断规则替代。

“不设置”是允许的设计结论，但必须是审议后的结论。

---

## 11A.3 设计依据

具体生成方法优先读取：

- `11_智能功能调度与资金路径编排协议.md`；
- `资金路径模板库.jsonl`；
- 软件字典中已验证的盈利停止、亏损停止、复位、熔断和跳转规则；
- 当前方案的实际投注结构和验证结果。

本金、止盈和止损至少参考适用项：

- 单注金额；
- 每期实际注数；
- 并投、轮投或枪弹结构；
- 平倍、普通倍投或高级状态路径；
- 最大单期投入；
- 完整路径总倍数；
- 理论全挂暴露；
- 历史最大连挂和压力连挂；
- 最大回撤；
- 实际赔率和返奖口径；
- 测试期数；
- 用户风险预算；
- 软件止盈止损行为证据；
- 是否存在复位、降压、熔断或人工停止。

禁止脱离这些参数随意填写整数金额。

---

## 11A.4 建议本金冻结

每个正式方案必须输出一个数字化的建议本金。

内部记录至少包含：

```text
方案ID=
资金路径版本=
单注金额=
每期注数范围=
最大单期投入=
完整路径总投入=
压力连挂期数=
压力暴露金额=
风险缓冲=
建议本金=
建议本金性质=测试本金/正式观察本金/压力测试本金
计算假设=
```

建议本金应覆盖冻结路径的合理压力暴露和必要缓冲，不能只等于首期投注金额。

当赔率、实际注数或软件行为证据不足时，不得伪装成精确最优本金；应给出基于明确假设的“测试本金”，并在隐藏附录说明假设和证据等级。

若一个方案套包含平倍版和高风险实验版，且资金暴露不同，必须分别给出建议本金；只有暴露完全一致时才可共用一个金额。

---

## 11A.5 止盈设计

止盈可以不设置，也可以单独设置或与止损同时设置。

启用止盈时，必须说明：

- 止盈金额；
- 相对建议本金的比例；
- 是自动停止、跳转、复位还是人工停止；
- 达到止盈后是否当天停止、整批停止或回到初始路径；
- 为什么该金额不会明显破坏本次验证目标。

止盈不能只依据“看起来利润不错”设定，应结合测试期数、正常波动、方案目标和软件执行能力。

若不启用止盈，必须说明本方案用什么替代结束条件，例如固定期数、固定样本、人工复核或批次结束。

---

## 11A.6 止损设计

止损可以不设置，也可以单独设置或与止盈同时设置。

启用止损时，必须说明：

- 止损金额；
- 相对建议本金的比例；
- 对应哪一类风险：连续挂、单日亏损、路径全挂、最大回撤或人工熔断；
- 是停止实投、停止追损、停止整个方案还是复位到平倍；
- 软件是否已验证能够按该金额正确停止。

止损金额必须与最大单期投入、完整资金路径和压力连挂相容。止损小于一次正常投入或与冻结路径无法共存时，判定为设计冲突。

若不启用自动止损，必须保留其他明确风险边界，例如固定期数、最大局数、倍率封顶、人工熔断或仅模拟运行。

---

## 11A.7 PPT主讲披露

所有正式挂机方案PPT必须在主讲部分显示建议本金金额。

建议放在风险页、使用建议页或资金路径页，不得为了这一要求机械新增无价值页面。

主讲页至少显示：

```text
建议本金：¥X
止盈：不设置 / ¥Y
止损：不设置 / ¥Z
```

规则：

- 建议本金始终必须显示具体金额；
- 启用止盈时必须显示止盈金额，不能只写百分比；
- 启用止损时必须显示止损金额，不能只写百分比；
- 只启用一项时，另一项明确写“不设置”；
- 两项都不启用时，明确写“止盈：不设置”“止损：不设置”；
- 可以同时显示百分比，但金额是强制字段；
- 不得使用模糊文字替代金额，例如“适量本金”“小额止损”“盈利后停止”。

主讲表达应简洁，避免把完整计算表搬到屏幕上。

---

## 11A.8 PPT隐藏附录证据

隐藏附录必须保存本金和止盈止损的完整设计依据，至少包括适用项：

- 资金路径版本；
- 单注金额和每期注数；
- 最大单期投入；
- 总倍数或完整路径投入；
- 压力连挂假设；
- 历史或模拟最大回撤；
- 建议本金计算过程；
- 止盈模式、金额、比例和触发后动作；
- 止损模式、金额、比例和触发后动作；
- 软件字段及行为证据等级；
- 自动停止还是人工执行；
- 未验证边界和替代停止条件。

主讲页显示的本金、止盈和止损金额必须与隐藏附录完全一致。

---

## 11A.9 结论与建议一致性

资金建议必须服从方案结论。

若结论为“尚未证明优势”或负面结果：

- 建议本金应定位为低风险测试或观察本金；
- 不得建议扩大资金；
- 不得启用依赖追损制造盈利观感的高风险止盈目标；
- 止损或替代风险边界应更保守。

若资金路径本身是实验对象，PPT必须明确区分号码逻辑是否有效与资金路径是否只是改变波动。

---

## 11A.10 强制失败状态

出现以下情况不得冻结或交付：

- `CAPITAL_RECOMMENDATION_MISSING`：没有建议本金金额；
- `STOP_MODE_UNDECIDED`：止盈止损模式未明确选择；
- `TAKE_PROFIT_AMOUNT_MISSING`：启用止盈但没有止盈金额；
- `STOP_LOSS_AMOUNT_MISSING`：启用止损但没有止损金额；
- `CAPITAL_STOP_BASIS_MISSING`：金额没有计算依据；
- `CAPITAL_STOP_PATH_CONFLICT`：本金或止损与冻结资金路径冲突；
- `PPT_CAPITAL_MISSING`：PPT主讲部分没有建议本金；
- `PPT_TAKE_PROFIT_MISSING`：启用止盈但PPT未显示止盈金额；
- `PPT_STOP_LOSS_MISSING`：启用止损但PPT未显示止损金额；
- `PPT_CAPITAL_STOP_MISMATCH`：主讲与隐藏附录金额不一致；
- `PPT_STOP_MODE_MISLEADING`：未设置的项目未明确写“不设置”，或停止对象表述错误。

---

## 11A.11 最终冻结记录

每个正式方案冻结时必须保存：

```text
建议本金金额=
止盈止损模式=NONE/STOP_LOSS_ONLY/TAKE_PROFIT_ONLY/BOTH
止盈金额=不设置或具体金额
止损金额=不设置或具体金额
自动执行能力=
停止对象=
达到后动作=
计算依据=
证据等级=
PPT主讲披露位置=
PPT隐藏附录位置=
```

固定原则：

> 每套方案都要知道需要多少本金；止盈止损可以不设置，但不能不审议；一旦设置，金额必须在PPT中清楚展示并可追溯。
