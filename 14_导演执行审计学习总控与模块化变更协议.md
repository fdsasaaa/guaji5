# 14_导演执行审计学习总控与模块化变更协议

版本：V4-CONTROLLER-CANDIDATE-1  
状态：候选规则，合并到 `main` 后生效  
目标：把用户前端压缩为一句话，把复杂度放入GitHub后端，同时保证可回滚、可扩展、可审计。

---

## 14.1 一句话入口

标准入口：

> 启动彩票总控：读取 fdsasaaa/guaji5 的 main，按本次要求直接生成并交付合格方案套。

无额外要求时：

> 启动彩票总控：读取 fdsasaaa/guaji5 的 main，自主生成并交付一套合格方案。

入口短，不代表过程省略。接管者必须执行：

```text
接收任务
→ 仓库与输入预检
→ 导演候选设计
→ 冻结设计合同
→ 执行生成
→ 验证
→ 语义审计
→ 有限返工
→ 交付
→ 学习记录
```

不得因为用户只说一句话，就跳过中间证据。

---

## 14.2 GitHub、AI与程序的职责

1. GitHub `main`：唯一稳定正式源，保存规则、工具、状态、索引、历史、测试与回滚证据。
2. 任务分支：本次修改和生成过程的隔离区。
3. AI总导演：读取全能力、提出候选、选择架构并冻结合同。
4. 执行器：按冻结合同生成TXT、方案套和PPT，不得暗中改规则。
5. 程序总控：保存状态、检查转移、记录证据、执行静态校验和形成回滚计划。
6. GitHub Actions：独立复验仓库结构和专项校验。

GitHub不是自动思考主体；AI也不得把聊天记忆当作正式源。任何重要规则只有写入仓库并通过校验后才生效。

---

## 14.3 强制状态机

正式任务必须使用 `controller/pipeline.json` 中的阶段与转移。

主要阶段：

- `INTAKE`：形成结构化任务单；
- `PREFLIGHT`：读取稳定源、版本、输入、依赖和回滚基线；
- `DIRECTOR`：至少生成3种功能画像并比较；
- `CONTRACT_FROZEN`：冻结方案和PPT执行合同；
- `EXECUTION`：生成方案源、TXT和PPT蓝图；
- `VALIDATION`：格式、编码、数据、风险与运行校验；
- `AUDIT`：逻辑、重复、后验拟合、PPT一致性和交付语义审计；
- `REWORK`：按错误类别返回相应阶段；
- `DELIVERY`：生成并核对ZIP和唯一PPTX；
- `LEARNING`：写入事实与候选经验；
- `COMPLETED`：所有证据闭环。

禁止从 `DIRECTOR` 直接跳到 `DELIVERY`，禁止未冻结合同直接生成正式交付。

---

## 14.4 本次设计合同

导演阶段必须生成 `design_contract.json`，至少冻结：

1. 研究问题与实验假设；
2. 数据范围、样本内外和随机基准；
3. 候选功能画像与淘汰理由；
4. 号码与触发逻辑；
5. 正投、反投、换号和监控；
6. 时间限制和模拟/真实切换；
7. 枪弹结构、并投或轮投；
8. 四路资金路径比较及最终选择；
9. 建议本金、止盈、止损、暂停、恢复、重置和封顶；
10. TXT格式、编码与引用；
11. PPT阶段、结构、主讲口径和证据来源；
12. 验收标准、失败状态和返工路径。

合同冻结后，任何关键逻辑改变都必须退回 `DIRECTOR` 或 `CONTRACT_FROZEN`，更新原因并形成新合同版本。禁止执行器静默漂移。

---

## 14.5 自动返工纪律

最多允许3轮返工：

- 输入、版本或依赖错误：返回 `PREFLIGHT`；
- 逻辑重复、后验拟合或能力选择错误：返回 `DIRECTOR`；
- 合同前后不一致：返回 `CONTRACT_FROZEN`；
- TXT、编码、构建或引用错误：返回 `EXECUTION`；
- 数据、回测或风险错误：返回 `VALIDATION`；
- PPT、语义或主讲一致性错误：返回 `AUDIT`；
- ZIP、文件数量或交付完整性错误：返回 `DELIVERY`。

达到3轮仍失败必须停止，保留失败证据并说明阻塞。不得为了“必须交付”伪造通过。

---

## 14.6 回滚与升级记录

每次系统修改必须在写入前形成 `rollback_manifest.json`，记录：

- 任务ID和升级ID；
- 基线分支；
- 基线提交SHA；
- 计划修改的文件；
- 每个文件的修改前SHA-256；
- 修改原因和影响范围；
- 所属扩展域；
- 必须执行的校验；
- 回滚目标和回滚顺序；
- 是否涉及迁移；
- 是否允许删除文件。

修改完成后追加：

- 修改后SHA-256；
- 提交SHA；
- PR编号；
- 校验结果；
- 已知限制；
- 最终合并提交或放弃原因。

回滚原则：

1. `main`永远只代表已验证稳定版本；
2. 默认回滚到最近一次已验证的 `main` 提交；
3. 禁止强推覆盖历史；
4. 失败分支、失败提交和失败原因不得删除；
5. 回滚后必须再次运行全部相关校验；
6. 数据迁移必须提供逆向迁移或兼容读取方案。

---

## 14.7 五类扩展域

所有变更必须先归入 `controller/extensions.json` 中的一个或多个域：

### PPT域

只负责PPT协议、模板、页面、备注、渲染和专项验收。不得通过修改PPT反向改变已冻结方案逻辑。

### 方案域

负责号码、触发、监控、正反投、资金路径、TXT和枪弹结构。不得破坏GitHub治理和PPT品牌固定要求。

### 程序域

负责Python工具、构建器、校验器、CI和接口。破坏性接口变化必须提供迁移器或兼容层。

### 系统域

负责总控、状态、版本、协议、学习、治理和回滚。不得静默改变已验证的TXT和PPT语义。

### 清理域

负责多余文件识别、隔离、删除和恢复。清理默认只生成计划，必须先引用扫描、后隔离、再观察、最后删除。

跨域修改必须生成影响报告，说明哪些域被触及、各自校验是否通过。核心协议与普通功能改动原则上分开PR。

---

## 14.8 文件清理安全规则

任何“清理多余文件”任务默认执行以下顺序：

```text
仓库清单
→ 引用扫描
→ 分类：正式源/构建产物/历史证据/重复文件/未知文件
→ 生成清理计划
→ 移入隔离区或标记废止
→ 运行全量校验
→ 观察至少一个完整任务周期
→ 再单独提交删除PR
```

禁止：

- 直接批量删除未知文件；
- 删除失败证据和历史回滚材料；
- 删除仍被清单、协议、工作流、代码或索引引用的文件；
- 把“文件名看起来旧”当作删除依据；
- 在功能升级PR中顺手大规模清理。

---

## 14.9 学习的三级权限

- L1事实记录：批次结果、使用能力、未使用理由、失败原因、审计结果，可自动写入；
- L2规则候选：多次重复现象、潜在优化和异常模式，只能进入候选池；
- L3正式晋级：必须有多次证据、样本外验证、治理校验和独立PR。

单次高收益、单次失败或一次用户偏好变化，都不得直接改写核心规则。

---

## 14.10 合并门槛

系统升级PR至少必须通过：

1. `python tools/validate_repository.py`；
2. `python tools/validate_controller_architecture.py`；
3. `python tools/lottery_controller.py validate`；
4. 受影响扩展域的专项校验；
5. 变更范围与回滚清单审查；
6. 无直接写 `main`、无强推、无自动合并。

默认创建Draft PR。用户或独立审查确认后再转Ready和合并。

---

## 14.11 最终原则

> 前端可以只有一句话，但后端不能只有一段提示词。

后端必须包含：状态机、冻结合同、模块边界、验证器、审计器、返工路线、升级记录、回滚清单和受控学习。复杂度应当被系统吸收，而不是转嫁给用户。
