# 16_投注内容格式唯一源与防臆造门控协议

版本：V1.0.0  
状态：ACTIVE

## 目的

本协议解决“AI知道玩法含义，但自行发明投注数字格式”的系统性风险。

`01_软件格式与已验证执行规则.md`继续作为底层规则入口；`controller/betting_format_registry.json`作为投注数字语法的机器可执行唯一源。任何生成器不得绕过该注册表自行拼接分隔符、段数、空值字段或策略专用包装。

## 强制顺序

正式主方案TXT生成必须按以下顺序执行：

1. 确定承载策略、玩法类型、玩法名称；
2. 在`controller/betting_format_registry.json`中找到完全匹配的format_id；
3. 读取段数、分隔符、外层pattern、必需模板值和证据；
4. 只允许把本次具体号码实例化到已登记语法中；
5. 对生成内容运行`tools/validate_betting_format_registry.py`；
6. 未命中注册表、证据不足、字段与母版不一致时，直接BLOCK，不得生成正式TXT。

## 已冻结：CAT05 高级定码轮换 + 前二 + 直选复式

历史验证规则：

- `高级定码轮换内容`每局格式：`局号|号码集合|中后参数|挂后参数`；
- 直选复式各位置号段使用短横线`-`分隔；
- 二星/前二必须正好2个号段；
- 每个号段为非空、不重复的数字序列；
- 已验证前二样例：`1|012-123|1|1`；
- 历史013前二直选复式人工导入成功；
- 013母版中`任选位置=`为空，`换号期数=1`。

因此精确二码60的号码集合为：

```text
6-0
```

完整局定义为：

```text
1|6-0|1|1
```

这里的`6-0`不是新增格式，而是把第一位候选集合缩为`6`、第二位候选集合缩为`0`后，对已验证“两段短横线分隔”语法的实例化。

## 禁止规则

以下行为一律视为FORMAT_HALLUCINATION并阻断：

- 因为“二码看起来像60”就直接写`60`；
- 自行改为`6 0`、`6,0`、`6|0`；
- 不查母版就自行填写`任选位置=0,1`；
- 从其他策略复制分隔符；
- 仅凭GUI名称推断TXT字段；
- 注册表未覆盖时为了凑交付包而创造格式。

## CI要求

仓库总校验必须执行：

```text
python tools/validate_betting_format_registry.py --self-test
```

并至少包含：

- 已验证正例通过；
- `6-0`按已冻结前二直选复式语法通过；
- `60`、`6 0`、`6,0`、`6|0`、`6--0`、三段格式必须失败；
- `任选位置=0,1`对该format_id必须失败；
- 生成器必须读取注册表，不得写死`a-b`作为独立事实源。

## 证据等级边界

语法通过只证明格式与历史规则一致，不等于平台奖金、实盘收益或高级倍投多方案状态同步已验证。资金与运行状态仍由各自门控负责。
