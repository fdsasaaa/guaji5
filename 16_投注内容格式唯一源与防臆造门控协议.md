# 16_投注内容格式唯一源与防臆造门控协议

版本：V1.2.0  
状态：ACTIVE

## 目的

本协议解决两类系统性风险：

1. AI自行发明分隔符、段数或字段；
2. AI把“数学上可能等价”的投注写法当成“软件语义等价”，擅自把完整号码改成位置集合，或反向改写。

`01_软件格式与已验证执行规则.md`继续作为底层规则入口；`controller/betting_format_registry.json`作为投注对象语义与投注数字语法的机器可执行唯一源。任何生成器不得绕过该注册表。

## 强制顺序：先语义，后玩法，再格式

生成主方案TXT必须按以下顺序执行：

1. 先确定用户真正要投注的对象语义；
2. 再由语义确定玩法类型与玩法名称；
3. 再确定承载策略；
4. 在`controller/betting_format_registry.json`中找到完全匹配的format_id；
5. 检查`generation_usage`；
6. 只有`formal_allowed`才可生成正式TXT；证据不足则BLOCK或仅生成明确验证包；
7. 读取号码格式、外层pattern、必需模板值和证据；
8. 对生成内容运行`tools/validate_betting_format_registry.py`；
9. 未命中注册表、语义不匹配、证据不足或字段与母版不一致时，直接BLOCK。

## 前二最重要的语义分界

### A. 完整二位号码：前二 + 直选单式

如果投注对象是完整的两位号码，例如：

```text
60
08
49
```

或者一次投注多个完整号码：

```text
28 49 60 81 02
```

则必须使用：

```text
semantic_intent=EXACT_COMPLETE_TWO_DIGIT_NUMBER
玩法类型=前二
玩法名称=直选单式
```

号码规则：每一注是完整两位数字，多注之间使用单个ASCII空格分隔。

高级定码轮换外层保持：

```text
局号|完整号码或完整号码集合|参数1|参数2
```

用户于2026-08-08确认本批设置无问题，以下写法升级为正式格式证据：

```text
1|28|1|1
1|49|1|1
1|60|1|1
1|81|1|1
1|02|1|1
```

因此 `高级定码轮换 + 前二 + 直选单式` 的格式状态由原 test_only 升级为 `formal_allowed`。

**禁止把完整号码60改写为6-0。**

### B. 两个位置各自的候选集合：前二 + 直选复式

只有当投注对象本身就是两个位置的候选集合时，才使用：

```text
semantic_intent=POSITION_SET_TWO_DIGIT_DIRECT_MULTI
玩法类型=前二
玩法名称=直选复式
```

例如：

```text
6-0
012-123
```

这里的`-`表示两个位置号段之间的分隔，不表示一个完整二位号码的书写方式。

## 用户确认后的证据边界

2026-08-08用户确认解决的是：

- `前二 + 直选单式`的完整号码表达；
- `高级定码轮换`承载该玩法时的主TXT字段与显示设置；
- 28/49/60/81/02不得再改写为2-8/4-9/6-0/8-1/0-2。

该确认**不自动升级**以下独立行为：

- 五个主方案引用同一个高级倍投配置时，状态是否共享；
- 连续未中时五方案是否同步推进ID；
- 任一方案命中后其余方案是否同步回ID1；
- 平台实际返奖、返点、限额；
- 长期收益或正期望。

格式证据、运行行为证据、资金经济证据必须分层保存，禁止连带升级。

## 永久禁止规则

以下行为一律阻断：

- 完整号码`60`自动转成`6-0`；
- 完整号码`08`自动转成`0-8`；
- 看到两位数字就自行决定用单式或复式；
- 只凭“数学等价”跨玩法名称替换；
- 从其他策略复制投注格式但不检查承载策略组合证据；
- 注册表未覆盖时为了凑交付包而创造格式。

## CI要求

仓库总校验必须执行：

```text
python tools/validate_betting_format_registry.py --self-test
```

至少覆盖：

- `前二直选复式`：`6-0`、`012-123`通过，`60`失败；
- `前二直选单式`：`60`、`08`、`28 49 60 81 02`通过，`6-0`失败；
- 将完整号码`60`交给复式renderer必须抛出`SemanticFormatMismatch`；
- `高级定码轮换+前二+直选单式`正式生成必须通过；
- 正式生成TXT必须使用`玩法名称=直选单式`和完整号码，不得出现`6-0`；
- `任选位置=`保持为空；
- 格式正式化不得解除高级倍投多方案运行状态与平台经济门控。

## 长期记忆规则

以后凡用户要求“固定完整前二号码”“投某个完整两位数”“二星单个号码”或同义表达，默认先解析为：

```text
EXACT_COMPLETE_TWO_DIGIT_NUMBER -> 前二 -> 直选单式
```

只有用户明确要求“第一位一组候选 + 第二位一组候选”“复式”“位置集合”时，才允许选择前二直选复式。

生成前必须经过注册表，不允许依赖模型记忆直接拼字符串。
