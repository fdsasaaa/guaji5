# 16_投注内容格式唯一源与防臆造门控协议

版本：V1.1.0  
状态：ACTIVE

## 目的

本协议解决两类系统性风险：

1. AI自行发明分隔符、段数或字段；
2. AI把“数学上可能等价”的投注写法当成“软件语义等价”，擅自把完整号码改成位置集合，或反向改写。

`01_软件格式与已验证执行规则.md`继续作为底层规则入口；`controller/betting_format_registry.json`作为投注对象语义与投注数字语法的机器可执行唯一源。任何生成器不得绕过该注册表。

## 强制顺序：先语义，后玩法，再格式

生成主方案TXT必须按以下顺序执行：

1. 先确定用户真正要投注的对象语义；
2. 再由语义确定玩法类型与玩法名称；
3. 再确定承载策略；
4. 在`controller/betting_format_registry.json`中找到完全匹配的format_id；
5. 检查`generation_usage`；
6. 只有`formal_allowed`才可生成正式TXT；`test_only`只能生成验证包；
7. 读取号码格式、外层pattern、必需模板值和证据；
8. 对生成内容运行`tools/validate_betting_format_registry.py`；
9. 未命中注册表、语义不匹配、证据不足或字段与母版不一致时，直接BLOCK。

## 前二最重要的语义分界

### A. 完整二位号码：直选单式

如果投注对象是完整的两位号码，例如：

```text
60
08
49
```

或者一次投注多个完整号码：

```text
28 49 60 81 02
```

则语义为：

```text
semantic_intent=EXACT_COMPLETE_TWO_DIGIT_NUMBER
玩法类型=前二
玩法名称=直选单式
```

字典中的已验证投注内容示例为：

```text
08 50 89
```

即：每一注是完整两位数字，多注之间使用单个ASCII空格分隔。

**禁止把完整号码60改写为6-0。**

### B. 两个位置各自的候选集合：直选复式

如果投注对象是：

- 第一位候选：6
- 第二位候选：0

或更一般地：

- 第一位候选：012
- 第二位候选：123

则语义为：

```text
semantic_intent=POSITION_SET_TWO_DIGIT_DIRECT_MULTI
玩法类型=前二
玩法名称=直选复式
```

内容格式为：

```text
6-0
012-123
```

这里的`-`表示两个位置号段之间的分隔，不表示一个完整二位号码的书写方式。

## 本次错误的正式定性

此前生成器收到完整二位号码`60`后，将其按前二直选复式实例化为`6-0`。这种写法作为“直选复式位置集合”语法本身可以成立，但它改变了用户要求的投注对象表示方式，因此属于：

```text
SEMANTIC_FORMAT_MISMATCH
```

不是简单的分隔符错误。

系统不得再用“只含一个组合时开奖结果集合相同”作为理由进行这种替换。

## 高级定码轮换 + 前二 + 直选单式当前边界

现有字典已经明确：

- `前二 + 直选单式`的完整号码格式为`08 50 89`；
- 高级定码轮换每局外层仍为`局号|号码集合|参数1|参数2`；
- 但`高级定码轮换 + 前二 + 直选单式`当前仍为`low_confidence_derivation / test_only`，尚未取得用户人工导入验证，不能升级成正式组合。

因此当前允许生成的最小验证写法是：

```text
玩法类型=前二
玩法名称=直选单式
高级定码轮换内容=1|60|1|1
```

但必须：

```text
test_only=true
allow_formal=false
needs_user_import_validation=true
```

并建议TXT第1行使用`False`，防止导入后自动参与真实投注。

一旦用户人工导入确认该组合显示的投注数字为完整`60`、注数正确，并确认高级倍投引用可恢复，再按证据升级流程处理。

## 禁止规则

以下行为一律阻断：

- 完整号码`60`自动转成`6-0`；
- 完整号码`08`自动转成`0-8`；
- 看到两位数字就自行决定用单式或复式；
- 只凭“数学等价”跨玩法名称替换；
- 从其他策略复制投注格式但不检查承载策略组合证据；
- test_only组合包装成formal_allowed；
- 注册表未覆盖时为了凑交付包而创造格式。

## CI要求

仓库总校验必须执行：

```text
python tools/validate_betting_format_registry.py --self-test
```

至少覆盖：

- `前二直选复式`：`6-0`、`012-123`通过，`60`失败；
- `前二直选单式`：`60`、`08`、`28 49 60 81 02`通过，`6-0`失败；
- 将完整号码`60`交给复式renderer必须抛出`SemanticFormatMismatch`；
- `高级定码轮换+前二+直选单式`正式生成必须被阻断；
- 验证包必须使用`玩法名称=直选单式`和完整号码，不得出现`6-0`。

## 证据等级边界

投注号码格式正确，只证明投注内容表达与字典语义一致。它不自动证明：

- 高级定码轮换+前二+直选单式已经人工导入通过；
- 高级倍投与该主方案组合已经E3验证；
- 多方案高级倍投状态同步；
- 平台奖金、返点、限额或长期收益。

这些仍由各自门控负责。
