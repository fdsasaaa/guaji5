# 00C 高级倍投 GUI 导出强制覆盖规则

版本：V3.9.12-ADVANCED-GUI-EXPORT-1  
状态：强制执行  
证据：2026-08-17 用户在当前挂机软件GUI中手工调整高级倍投后，直接从软件导出的 `高级倍投_A_耐久软复位.txt`。

## 1. 优先级

本文件属于 `NEWER_USER_CONFIRMED_OR_GITHUB_VERIFIED`。凡与旧版 `controller/betting_format_registry.json`、旧规则书、V3.2/V3.4遗留规则冲突时，本文件对“高级倍投导出格式”优先。

旧规则中以下结论自本证据起废止：

- 高级倍投只有9字段；
- `中后监控=True` / `挂后监控=True` 一律禁止；
- `中后跳转=True-方案名` / `挂后跳转=True-方案名` 一律禁止；
- 高级倍投不能跳转到主方案中的其他方案；
- 当前软件高级倍投导出一定是 UTF-8 BOM。

## 2. 当前实测导出编码

本次软件原生导出文件实测：

- 编码：GBK；
- BOM：无；
- 换行：CRLF；
- 12行之间有11个CRLF；
- 文件末尾无额外换行。

除非以后有更新的软件原生导出证据，否则生成与复刻当前高级倍投文件优先按该格式。

## 3. 当前实测16字段顺序

每局一行，字段以英文分号 `;` 分隔，当前实测每行固定16字段：

```text
软件名称
ID
倍数
中后ID
挂后ID
中后监控
中后跳转
挂后监控
挂后跳转
是否盈利跳转
是否亏损跳转
盈利金额
亏损金额
盈利跳转局数
亏损跳转局数
SchemeCreator
```

标准骨架：

```text
软件名称=CXGGJ;ID=1;倍数=1;中后ID=1;挂后ID=2;中后监控=True;中后跳转=False-目标方案;挂后监控=False;挂后跳转=False-目标方案;是否盈利跳转=False;是否亏损跳转=False;盈利金额=50000;亏损金额=50000;盈利跳转局数=1;亏损跳转局数=1;SchemeCreator=<软件导出值>
```

## 4. GUI 与字段映射

用户在GUI中调整后再导出的结果确认：

- `中后监控=True`：GUI“中后 → 重新监控”被勾选；`False`为未勾选。
- `挂后监控=True`：GUI“挂后 → 重新监控”被勾选；`False`为未勾选。
- `中后跳转=True-主方案名`：GUI“中后 → 跳到”被勾选，并序列化目标主方案名称。
- `中后跳转=False-主方案名`：GUI未勾选跳到；软件仍可能序列化一个方案名称，因此不得仅因后缀存在方案名就判定已跳转。
- `挂后跳转=True-主方案名` / `False-主方案名`：与中后同理。

`中后ID` / `挂后ID` 是高级倍投内部局号跳转；`中后跳转` / `挂后跳转` 是是否切换到主方案中的另一个方案。两者是不同层级，不得混淆。

## 5. 当前样本直接证明的组合能力

同一个高级倍投文件中可分别出现：

1. 中后重新监控；
2. 挂后重新监控；
3. 中后跳转到另一个主方案；
4. 挂后跳转到另一个主方案；
5. 普通内部中后ID/挂后ID软复位。

因此未来组合回利可以设计“命中后内部降压 + 重新监控”“挂后继续内部局”“命中后切换到回补方案”“挂后切换到救援方案”等状态图，但必须逐项冻结，不允许整批机械复制相同动作。

## 6. 尚未扩大解释的字段

当前导出还确认存在：

- `是否盈利跳转`
- `是否亏损跳转`
- `盈利金额`
- `亏损金额`
- `盈利跳转局数`
- `亏损跳转局数`

本次只确认字段名称、顺序和导出形态；没有实盘/前向行为证据前，不擅自解释金额统计口径、触发时点、跨方案作用域。

## 7. SchemeCreator 边界修正

当前软件原生导出的高级倍投文件中，`SchemeCreator` 为非空软件生成值。因此：

- “主方案TXT默认 `SchemeCreator=` 空值以避免加密/锁定显示”的规则继续有效；
- 该规则不得机械套用到 `GJBTScheme` 高级倍投导出文件；
- 高级倍投文件的 `SchemeCreator` 非空值只证明当前软件会这样导出，不等于已证明其加密算法或必须手工生成同一值；
- 对已有软件原生导出文件做往返修改时应优先保留其原值，不得把主方案的空值规则强行覆盖过去。
