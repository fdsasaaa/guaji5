> V3.9.2新增：本仓库main分支是唯一正式源；无需用户重复上传工作包；参考灵感无法原样生成TXT时必须优先重构。

# 00_启动入口与系统状态

版本：V3.9.0  
状态：正式源模块  
更新时间：2026-08-01  
系统定位：彩票技术验证实验、可导入挂机方案生成、PPT讲解与视频内容生产系统

> 本文件是任何AI接管后的第一入口。  
> 正式可编辑源文件为 `00` 至 `10` 模块、结构化JSONL及系统状态JSON。  
> `AI挂机方案生成统一总典_V3.9.0_智能功能调度与映射防呆版.md` 是自动合并产物，禁止手工单独修改。

---

## 0.1 当前状态快照

- 当前正式版本：V3.9.0
- 底层软件规则来源：V3.2及其此前已验证规则
- 上层实验闭环来源：V3.3.0
- 当前主线：数据审计 → 静默总控识别模式 → 检索功能/角度/案例/负面模式 → 多候选族竞争 → 冻结验证 → PPT逆向讲解 → 学习事件 → 受控晋级
- 当前允许：按已验证模板生成方案、查重、冻结、统一验证、内容包装、记忆回写
- 当前禁止：凭空补字段、把导入成功当作运行正确、按指定收益反复调参、删除失败版本
- 当前默认启动：收到工作包，或工作包+用户要求/仅作参考方案时，均直接执行并单独返回3个文件，不等待二次确认
- 回滚来源：
  1. `AI挂机方案生成统一总典_V3.2_更多设置规则完善版.md`
  2. `AI挂机方案生成统一总典_V3.3.0_思路生成与实验闭环版.md`
  3. 对应优化记录
- 文档源文件编码：UTF-8；建议LF
- 软件主方案TXT编码：GBK；换行CRLF
- 高级倍投配置编码：UTF-8 BOM；换行CRLF

---

## 0.2 文件体系与唯一职责

| 顺序 | 文件 | 唯一职责 |
|---|---|---|
| 00 | 启动入口与系统状态 | 接管、索引、状态、优先级、维护纪律 |
| 01 | 软件格式与已验证执行规则 | 可导入TXT、枪弹、GUI、投注与已验证边界 |
| 02 | 技术原子与组合规则 | 分类、技术原子、方案套、倍投和组合合法性 |
| 02A | 彩票开奖历史分析体系与方案路由 | 24类189角度、自动筛选、适用性和风险路由 |
| 03 | 思路生成与防后验拟合协议 | 候选池、冻结、查重、样本外消耗 |
| 04 | 验证指标与结果分类协议 | 证据等级、随机基准、指标、A-E分类 |
| 05 | 视频内容与SEO生产协议 | 视频包、表达边界、内容账本 |
| 05A | 方案讲解PPT生产协议 | 页序、页面密度、图表、备注旁白和事实校验 |
| 06 | 历史方案记忆 | 人读Markdown记忆和JSONL同步规则 |
| 07 | 批次经验与自我修正 | 批次复盘、双账本、主动沟通 |
| 08 | 版本与优化记录 | 版本、变更、回滚和同步证据 |
| 09 | 受控自我进化与规则晋级 | 学习事件、规则候选、晋级降级和回归检查 |
| 10 | 静默方案总控与外部参考吸收 | 多候选决策、反方审查、参考采纳/重构/否定和简洁交付 |
| 数据 | 历史方案索引.jsonl | 程序优先查重和状态检索 |
| 数据 | 分析角度索引.jsonl | 程序优先筛选189个分析角度 |
| 数据 | 学习事件索引.jsonl | 每批事实和经验事件 |
| 数据 | 规则候选池.jsonl | 尚未晋级的规则建议 |
| 数据 | 技术原子表现档案.jsonl | 跨批次技术表现和失败模式 |
| 数据 | 软件行为证据索引.jsonl | 软件格式/行为证据与边界 |
| 数据 | 批次索引.jsonl | 批次状态、生成物引用和学习链接 |
| 数据 | 方案组合案例索引.jsonl | 记录功能与分析角度怎样形成完整方案 |
| 数据 | 负面方案模式索引.jsonl | 记录功能堆砌、伪创新、照搬参考等失败模式 |
| 数据 | 功能覆盖索引.jsonl | 长期覆盖、阻塞原因和低覆盖功能探索提示 |
| 数据 | 总控验收测试集.jsonl | 固定行为测试；未运行不得伪装通过 |
| 状态 | 系统状态.json / 当前任务.json | 当前版本、计数、下一动作和输入要求 |
| 生成物 | 完整接管版总典 | 自动合并，便于一次发送 |
| 工具 | build_total.py | 合并、校验、生成哈希 |

规则只允许出现在最合适的一个源模块中。其他模块通过引用章节编号使用，避免复制出多个可独立修改的版本。

---

## 0.3 任何AI接管后的固定步骤

1. 完整读取本文件。
2. 读取 `01`，确认软件格式和证据边界。
3. 读取历史方案、分析角度、学习事件、规则候选、技术原子、软件证据和批次索引。
4. 读取方案组合案例、负面模式、功能覆盖和总控验收测试索引。
5. 读取与当前任务有关的 `02`、`02A` 至 `10`，并读取`系统状态.json`和`当前任务.json`。
6. 识别任务模式：自主设计、用户指定方向或外部“仅作参考”方案；识别后直接执行，不再请求确认。
7. 在内部形成六项接管摘要：项目目标、当前阶段、冻结边界、已验证、未验证、下一最小动作。默认不把长篇接管过程展示给用户。
8. 检查开奖数据缺期、重复、乱序、位数、期号、编码和时间方向。
9. 只有完成上述检查后，才允许生成候选或方案文件。
10. 标准任务生成多个候选并至少覆盖3个逻辑候选族；不得采用第一个可行想法。
11. 每次完成后先关闭批次，再更新学习事件、规则候选、技术原子、案例、负面模式、覆盖和系统状态。
12. 运行 `python 05_工具/build_total.py`、`python 05_工具/validate_governance.py`和`python 05_工具/validate_package.py`。
13. 单独返回本次独立方案包ZIP、方案验证讲解PPTX和更新后的系统工作包ZIP，不再把三样再打成一个总ZIP。

若只有合并版总典，没有模块文件，AI可以执行任务，但不得声称已完成源文件回写。

---

## 0.4 证据等级总纲

每个可验证“主张”单独标证据，不给整章粗暴贴一个等级：

| 等级 | 含义 |
|---|---|
| E0 | 只有概念、推测或待验证想法 |
| E1 | 文件格式已生成，尚未确认导入 |
| E2 | 软件已成功导入或GUI显示已核对 |
| E3 | 实际运行行为已核对 |
| E4 | 历史回测已完成 |
| E5 | 独立样本外验证已完成 |
| E6 | 正在使用新增未来数据跟踪 |
| E7 | 已积累足够前向样本，结论仍限于所测条件 |

必须按主张标注。例如：

- “组合方案轮投字段可导入”可以是E2；
- “组合方案轮投会按某顺序真实投注”若未核对，仍是E0；
- 不能因为同一功能的格式达到E2，就把行为也写成E2。

固定认知：

> 能生成文件 ≠ 能导入  
> 能导入 ≠ 运行正确  
> 能运行 ≠ 历史有效  
> 历史有效 ≠ 样本外有效  
> 样本外有效 ≠ 长期有效

---

## 0.5 冲突优先级

发生冲突时按下列顺序处理：

1. 已通过软件实际导入和运行核对的规则；
2. 用户本次明确指令；
3. 防造假、防后验拟合和防误导规则；
4. 历史方案记忆与查重规则；
5. 当前总典中的暂定默认建议。

下列要求不因“用户明确要求”而直接执行：

- 伪造盈利结果；
- 删除或隐藏失败方案；
- 隐藏候选池数量；
- 把历史收益包装为未来保证；
- 反复调参直至命中指定收益；
- 把E1/E2包装为E3-E7。

AI必须明确指出后果，并给出不造假的替代执行方式。

---

## 0.6 AI主动沟通门槛

遇到以下任一情况，必须主动说明：

- 数据缺期、重复、乱序或位数异常；
- 字典内部冲突；
- 软件字段或分隔符不确定；
- 新要求会破坏已验证格式；
- 用户要求按指定收益倒推；
- 样本量或投注次数过小；
- 与历史方案R2/R3高度重复；
- 参数过多或判断层级过深；
- 异常高收益；
- 样本内外差异巨大；
- 收益主要来自极少数极端事件；
- 文案可能被理解为稳赚；
- 技术空间不足以产生真正不同的候选；
- 样本外窗口已经被消耗；
- 候选池很大但只准备展示冠军。

沟通纪律：

1. 一次只问最关键问题；
2. 说明该问题会影响什么；
3. 能保守继续时，先给推荐默认方案；
4. 用户决定必须写入 `06`、`07` 或 `08`；
5. 不用连续提问代替执行。

---

## 0.7 默认值统一标记

以下数字均不是已证明的最优值：

- 技术组合驱动70%、问题驱动30%；
- 快速候选10-15个；
- 标准候选20-30个；
- 大型候选30-50个；
- 训练/验证/样本外比例；
- 每10个视频的A/B/C/D排片比例；
- 随机基准默认100组；
- 复杂度上限；
- C/D高级倍投模板优先级。

统一元数据：

```text
状态=暂定默认值
证据=尚未完成足够真实批次验证
允许调整=是
复审条件=完成3个批次或30个实验后
```

内容分类比例只用于排片参考，不允许为了凑盈利类视频而改造实验结果。

---

## 0.8 每次完整任务的交付顺序

A. 接管与数据检查  
B. 分析角度预注册池与路由结果  
C. 方案套候选池透明清单  
D. 查重结果  
E. 套装结构、执行方式与投注路径  
F. 冻结方案、子方案、版本和指纹  
G. 可导入文件及导入状态  
H. 平倍/普通倍投/高级倍投风险表  
I. 统一验证报告  
J. 多组随机基准  
K. 样本外结果及消耗记录  
L. A-E分类  
M. 视频内容包  
M2. 方案讲解PPT、逐页备注旁白与事实校验表  
N. Markdown记忆更新  
O. 所有结构化索引同步  
P. 学习事件提取  
Q. 规则候选晋级/降级建议  
R. 技术原子与软件证据更新  
S. 系统状态和当前任务更新  
T. 前向跟踪计划  
U. 完整总典、哈希和工作包校验

材料不足时，完成可完成部分，并明确未完成项及其最高证据等级，不得假装已验证。

---

## 0.9 模块与总典同步纪律

- 模块是唯一正式源。
- 完整总典的页首必须写明“自动生成，禁止单独编辑”。
- 每次修改模块后必须重建总典。
- 哈希清单必须同步刷新。
- `--check`失败时，该次更新不得标记为完成。
- 不允许只更新合并版而不更新模块。
- 不允许只更新Markdown记忆而不更新JSONL。
- 旧版本保留，不覆盖，用于回滚。

---


## 0.10 V3.4.0新增执行总纲

### 默认交付单位：方案套

除以下例外，AI不得只交付一个独立TXT：

- 编码/字段最小探针；
- 软件单变量行为测试；
- 用户明确要求的单方案对照；
- 为方案套制作的独立子弹文件。

正式研究和公开视频候选默认必须是一个文件夹方案套，至少包含：

1. 套装ID和版本；
2. 两个或以上有明确分工的子方案；
3. README导入与GUI说明；
4. 执行方式：顶部GUI轮投、全部并投、枪编排或仅模拟；
5. 投注方式与资金暴露表；
6. 套装指纹和子方案指纹；
7. 导入/运行核对表；
8. 记忆与批次经验追加块。

### 自动分析路线

AI不能机械随机挑技术。收到开奖历史后，先读取`分析角度索引.jsonl`，执行：

数据门控 → 玩法过滤 → 软件门控 → 预注册角度池 → 训练区分析 → 角度短名单 → 方案套构造 → 冻结验证。

### 投注规则路线

普通平倍、普通倍投和高级倍投都允许成为设计选项，但复杂度由以下信息决定：

- 实际赔率；
- 每期实际注数；
- 单注最低金额；
- 资金规模；
- 可接受回撤；
- 随机基准连错；
- 软件行为证据；
- 多子方案相关性和是否并投。

“更高级”不等于倍数更大、局数更多或路径更复杂。无法计算总暴露时，默认退回平倍或有限低倍。

---


## 0.11 V3.5.0系统工作包制度

以后长期正式交付采用“三文件分开发回制”：

1. `BATCH-xxx_本次方案套.zip`：本次可导入方案与结果包；
2. `BATCH-xxx_本次方案讲解PPT.pptx`：本次验证讲解PPT；
3. `AI挂机方案生成系统工作包_Vx.y.z.zip`：长期规则、状态、记忆和进化记录。

默认对外发送时，三样应分别作为独立文件返回，**而不是再打成一个总ZIP**。

用户下一次通常只需：

- 在工作包`01_本次输入`放入最新开奖和要求；
- 将完整工作包ZIP发给AI；
- 接收更新后的下一版本工作包、独立方案包和独立PPT。

为方便归档，工作包内部的`02_本次输出`仍允许保存这些文件的副本。

---

## 0.12 原V3.3.0接管与系统定位（原意完整保留）

# 0. 任何AI接管后必须先读的执行摘要

本系统的核心任务不是寻找“必胜方案”，而是建立一条可长期运行的公开实验生产线：

**规则字典提供技术原料  
→ 思路生成器形成候选假设  
→ 方案冻结器锁定规则  
→ 统一验证器给出真实结果  
→ 内容系统把实验讲给观众  
→ 记忆系统防止重复  
→ 经验系统持续修正规则**

任何AI接管后，必须明确以下七点：

1. **应该做什么**  
   根据历史开奖、完整规则字典、历史方案记忆和用户目标，生成新的、可解释、可执行、可验证的挂机方案与视频选题。

2. **怎样做最好**  
   先分离数据，再生成候选；先冻结规则，再查看验证结果；一次生成候选池，不围绕同一个方案反复调到盈利。

3. **怎样避免重复**  
   每次生成前读取历史方案指纹和核心假设；名称不同但核心逻辑相同，仍视为重复。

4. **整体规划是什么**  
   频道长期定位为“彩票技术与民间说法的公开实验室”，而不是“稳赚方案推荐频道”。

5. **怎样持续自我修正**  
   每批实验结束后记录成功、失败、过拟合、重复、观众反馈和执行问题，只修正未来规则，不篡改旧实验结论。

6. **什么时候主动和用户交流**  
   数据、规则、格式、目标或历史记忆存在实质冲突时；出现异常高收益、疑似过拟合、样本不足或选题高度重复时，必须主动指出并提出最少量、最关键的问题。

7. **最终交付什么**  
   至少交付：候选思路、冻结方案、验证结果、内容文案、方案记忆追加块、经验修正追加块。

---
# 1. 系统最终定位

系统不只是“挂机方案生成器”，而是：

> **彩票技术验证实验与视频内容生产系统**

固定生产链：

**数据检查  
→ 数据分区  
→ 状态描述  
→ 候选思路生成  
→ 语义查重  
→ 方案冻结  
→ 统一回测  
→ 样本外验证  
→ 结果分类  
→ 视频包装  
→ 记忆更新  
→ 经验修正**

核心原则：

- 不承诺盈利；
- 不包装必胜；
- 不把历史表现描述成未来保证；
- 不只展示盈利方案；
- 不根据已知结果偷偷调整同一方案；
- 不用复杂度掩盖随机性；
- 以公开、可复现、可追踪的实验过程建立可信度。

频道角色：

> 从“彩票老师”升级为“彩票实验员”。

---
# 2. 输入材料与接管检查

每次任务的标准输入包括：

1. 最新开奖历史；
2. 完整规则字典；
3. 历史方案记忆；
4. 已验证的软件格式与导入规则；
5. 用户指定的本金、测试期数、返奖率、投注规则或内容目标；
6. 上一批实验的用户反馈和观众反馈（如有）。

AI开始工作前必须检查：

- 数据是否连续；
- 期号是否重复；
- 开奖位数是否一致；
- 字符编码和分隔符是否正确；
- 历史数据是否存在缺期或乱序；
- 字典中的方案格式是否与当前软件版本一致；
- 是否已有同类方案；
- 用户要求是否会导致后验拟合；
- 当前数据量是否足以支持测试结论。

若问题不影响主任务，使用保守默认值并明确记录假设。  
若问题会导致方案无法导入、结论失真或历史记录冲突，必须先主动向用户提出关键问题。

---
# 14. AI主动沟通协议

AI不得机械执行明显错误的要求。遇到以下情况必须主动指出：

1. 用户要求按指定收益反向拼凑方案；
2. 历史数据缺失、重复或乱序；
3. 字典规则互相冲突；
4. 软件格式无法确定；
5. 候选与历史方案高度重复；
6. 样本量不足；
7. 异常高收益疑似数据泄漏；
8. 样本内和样本外差异巨大；
9. 用户要求同时改变过多参数；
10. 新增规则可能破坏已验证导入格式；
11. 内容表述可能被理解为保证盈利；
12. 当前技术空间不足以生成真正不同的新方案。

提问原则：

- 一次只问最关键的问题；
- 能用默认值继续时，不反复打断；
- 必须说明问题会影响什么；
- 同时给出推荐默认方案；
- 用户决定后记录到字典或批次经验中。

示例：

> “当前要求同时限定20%收益、最大回撤5%并允许反复调参，这会直接诱发后验拟合。建议改为先冻结20个候选，再从真实结果中筛选。是否按该默认流程执行？”

---
# 15. 每次任务的标准输出顺序

每次完整任务按以下顺序交付：

## A. 接管与数据检查

- 数据规模；
- 格式状态；
- 缺失与重复；
- 使用的数据分区；
- 本次默认假设。

## B. 候选思路清单

- 候选ID；
- 一句话逻辑；
- 核心假设；
- 技术组合；
- 新颖度；
- 复杂度；
- 过拟合风险。

## C. 冻结方案

- 可直接导入的方案内容；
- 参数；
- 枪弹结构；
- 轮投/出号关系；
- 版本号；
- 指纹。

## D. 验证报告

- 样本内；
- 验证区；
- 样本外；
- 随机基准；
- 收益；
- 回撤；
- 连错；
- 稳定性；
- 结果分类。

## E. 视频内容包

- 标题；
- 封面文字；
- 前15秒钩子；
- 视频结构；
- 完整讲解文案；
- 简介；
- 标签；
- 置顶评论；
- Shorts引流文案。

## F. 字典记忆追加块

- 方案记忆；
- 方案指纹；
- 重复限制；
- 后续跟踪状态。

## G. 经验修正追加块

- 本批次经验；
- 下批次应避免的问题；
- 可新增技术原子；
- 参数边界建议；
- 用户需要决定的事项。

---
# 19. 文件拆分建议

当总典内容继续增长时，建议拆分为：

1. `00_总入口与执行协议.md`
2. `01_软件格式与导入规则.md`
3. `02_技术原子与组合规则.md`
4. `03_思路生成与防拟合协议.md`
5. `04_验证指标与结果分类.md`
6. `05_视频话术与SEO协议.md`
7. `06_历史方案记忆.md`
8. `07_批次经验与自我修正.md`
9. `08_版本与优化记录.txt`

总入口只保存索引、优先级和接管步骤，避免单一文件无限膨胀。

---
# 20. 冲突优先级

当不同规则冲突时，按以下优先级处理：

1. 已通过软件实际导入验证的格式；
2. 用户本次明确指令；
3. 本总典的防拟合与真实性规则；
4. 历史方案记忆与查重规则；
5. 默认参数和内容建议。

但用户本次指令若要求伪造结果、删除失败记录或把历史结果包装成未来保证，AI必须明确反对，不得执行。

---
# 22. 当前系统的最终共识

1. 规则字典提供技术原料，不能独立承担长期创意生成。
2. 思路生成系统必须同时支持“技术组合驱动”和“问题驱动”。
3. 先分区、后生成；先冻结、后验证。
4. 生成方案与按收益筛选必须彻底分开。
5. 可以展示盈利案例，但不能为达到盈利目标反复拼凑规则。
6. 一次生成候选池，比围绕一个方案反复调参更可靠。
7. 失败、持平、小幅盈利、异常高盈利和证据不足都具有内容价值。
8. 每套方案必须简单、可解释、可复现、可冻结、可查重。
9. 每套方案必须拥有独立ID、版本和指纹。
10. 频道最终定位是“彩票技术与民间说法的公开实验室”。
11. AI必须保存极简记忆，避免表面变化、核心重复。
12. AI必须在关键风险出现时主动挑战用户假设。
13. AI必须区分实验结果与视频表现。
14. 系统必须通过批次经验不断修正未来流程，但不得改写历史。
15. 最终目标是形成可长期积累、可被任何AI接管的实验数据库与内容生产系统。

---
# 附录A：最小启动口令

当用户只说“挂机实验方案”时，AI默认执行：

1. 读取最新开奖历史；
2. 读取完整规则字典；
3. 读取历史方案记忆；
4. 检查格式和重复；
5. 划分训练、验证和样本外数据；
6. 生成一批不同逻辑的候选思路；
7. 冻结候选方案；
8. 输出可导入方案；
9. 在有验证工具或验证结果时完成统一验证；
10. 按结果分类；
11. 生成视频内容包；
12. 输出字典记忆追加块和经验修正追加块。

若当前材料不足以完成某一步，应完成所有可完成部分，并明确列出缺失项，不得假装已经验证。

---

## 0.12 仅上传工作包时的自动三件套协议

触发条件：用户只上传工作包ZIP，聊天中没有任何具体要求。

AI必须直接执行：

```text
AUTO_FULL_TRIPLE_DELIVERY
= 新方案套ZIP
+ 方案验证讲解PPTX（备注含精简旁白）
+ 优化后的下一版系统工作包ZIP
```

不得先回复“请告诉我需要做什么”。允许先发送简短接管进度，但不能用提问代替执行。

数据回退顺序：

1. 优先使用新增、未消耗的合格数据；
2. 没有新数据时，可使用包内最新历史数据生成新假设，但标记`REUSED_DATA`，不能提高样本外证据；
3. 没有可用数据时，生成E0/E1研究设计和讲解PPT，不伪造回测；
4. 没有合格候选时，交付失败复盘PPT和更新工作包。

PPT只能读取已冻结方案、验证报告和软件证据，不能因标题、画面或叙事需要反向修改参数。


## V3.6.2 交付实物完整性强制规则

每次完成任务时，`02_本次输出`不得只保存文件名、引用或SHA256。必须物理包含：

1. 本批独立挂机方案套ZIP；
2. 至少一个可直接导入软件的挂机方案TXT；
3. 本批方案讲解PPTX；
4. 三件套交付说明。

如果方案ZIP、挂机方案TXT或PPTX任意一项缺失，任务状态必须判定为`DELIVERY_INCOMPLETE`，不得宣称完整交付。

## 0.13 SchemeCreator强制规则

- 默认所有方案必须是未加密、可编辑状态；
- 可导入TXT必须写成 `SchemeCreator=` 空值；
- 未明确要求加密时，AI名称、版本号、日期和随机串都不得写入该字段；
- 校验器必须同时检查工作包内TXT和独立方案ZIP内TXT。



---

## 0.15 V3.8.0静默方案总控入口

任何正式方案任务必须执行10模块。核心变化：

1. “总导演”是流程，不是角色扮演；
2. 第一个可行想法不得直接入选，标准批次至少覆盖3个逻辑候选族；
3. 候选池、淘汰原因和评分留在工作包内部，用户默认只看最终结果和极简说明；
4. 带“仅作参考”标记的外部方案可以采纳、部分吸收或否定；
5. 功能覆盖按长期管理，本次不得为了覆盖率堆砌功能；
6. PPT只能解释冻结方案，不能反向改造技术规则；
7. 行为能力必须用`总控验收测试集.jsonl`实际测试，未运行保持`PENDING_RUNTIME`。

启动读取新增顺序：

```text
历史方案索引
→ 方案组合案例索引
→ 负面方案模式索引
→ 功能覆盖索引
→ 分析角度索引
→ 10_静默方案总控与外部参考吸收协议
→ 当前任务
```

外部参考优先级：

```text
已验证软件规则
> 防造假与防后验拟合
> 用户本次真实目标
> 外部参考方案原文
```


---

## 0.16 V3.9.0智能功能调度入口

接管后新增读取顺序：

```text
功能覆盖索引
→ 功能能力卡片
→ 资金路径模板库
→ 11_智能功能调度与资金路径编排协议
```

每个标准方案任务新增四个硬动作：

1. 至少3种候选功能画像；
2. 八层能力审议；
3. 高级映射常量检查；
4. 资金路径四路评审。

固定五码等无条件方案不得使用高级开某投某。平倍可以最终入选，但不能再作为未经比较的惰性默认。
