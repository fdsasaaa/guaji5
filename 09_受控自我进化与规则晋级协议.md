# 09_受控自我进化与规则晋级协议

版本：V3.8.0  
状态：正式源模块  
更新时间：2026-07-31  
适用范围：每次方案生成、回测、软件核对、内容生产和系统治理任务结束后

> 本系统允许积累经验，但禁止把一次结果自动写成永久真理。  
> 自我进化的目标不是让总典每次变长，而是让事实可追踪、经验可复验、错误可降级、规则可回滚。

---

## 9.1 核心原则

固定闭环：

```text
原始事实
→ 学习事件
→ 规则候选
→ 跨批次复验
→ 晋级/维持/降级/废弃
→ 回归检查
→ 正式版本
```

禁止闭环：

```text
单次盈利
→ 自动认定技术有效
→ 自动修改默认参数
→ 删除反例
```

下列内容必须分开：

- 软件格式事实；
- 软件运行行为；
- 流程经验；
- 技术原子表现；
- 投注路径风险；
- 视频内容表现；
- 用户明确决定。

---

## 9.2 四层知识状态

### 第一层：原始事实

只保存发生了什么，不解释原因。

例如：

```text
方案套=B331-SET-001
模式=全部并投
判断次数=995
命中率=49.45%
情景结果=亏损
```

原始事实不得因后续研究被改写，只能追加纠错记录。

### 第二层：学习事件

从一个批次中提炼值得继续观察的经验。

学习事件不是正式规则，必须标明：

- 来源批次；
- 事实依据；
- 证据等级；
- 适用范围；
- 支持证据；
- 反对证据；
- 当前状态。

### 第三层：规则候选

当同类学习事件重复出现，形成待晋级规则。

规则候选必须明确：

- 支持批次数；
- 反对批次数；
- 独立样本外数量；
- 晋级门槛；
- 降级门槛；
- 错误晋级风险；
- 是否需要用户确认。

### 第四层：正式规则

只有满足证据门槛、完成回归检查并保留回滚源后，才能写入00—09正式模块。

---

## 9.3 学习事件分类

| 分类 | 含义 | 示例 |
|---|---|---|
| S | 软件格式与运行行为 | GUI轮投开关、编码、枪弹同目录 |
| P | 流程与方案生成经验 | 默认方案套、冻结、候选透明度 |
| T | 技术原子与分析角度 | 冷热、遗漏、长龙、位置交换 |
| B | 投注和资金路径 | 平倍、普通倍投、高级倍投、熔断 |
| C | 内容生产 | 标题、留存、讲解顺序 |
| G | 系统治理 | 工作包、版本、索引、回滚 |

C类内容表现不得晋级为T类技术有效性。  
B类历史盈利不得自动提高T类选号逻辑证据。

---

## 9.4 状态机

每条学习事件或规则候选只能处于以下状态之一：

- `OBSERVE`：继续积累证据；
- `PROMOTE_RECOMMENDED`：达到门槛，等待正式审批；
- `PROMOTED`：已进入正式规则；
- `KEEP`：正式规则继续有效；
- `DEMOTE`：降低优先级或证据；
- `DEPRECATE`：停止默认使用，但保留历史；
- `REJECT`：证据不支持；
- `CONFLICT`：证据或规则冲突，等待用户决定。

禁止直接删除失败、废弃或冲突记录。

---

## 9.5 不同知识的晋级门槛

### 9.5.1 软件格式与行为

格式主张：

- 文件已实际成功导入；
- 编码、换行、字段和软件版本已记录；
- 最高可达到E2。

运行行为主张：

- 用单变量探针核对真实投注或状态变化；
- 测试环境和软件版本已记录；
- 最高可达到E3。

核心底层规则若会影响所有方案，建议至少复现2次或有明确历史证据链。

### 9.5.2 流程规则

暂定晋级门槛：

- 至少3个真实批次；或
- 累计30个冻结方案套/实验；
- 没有重大反例；
- 不破坏底层格式；
- 回归测试通过。

用户明确指定的治理方式可以先成为正式工作规则，但必须标记“用户决定”，不能伪装成统计结论。

### 9.5.3 技术原子与分析角度

技术原子不得被晋级为“有效技术”，除非至少满足：

- 多段时间顺序样本外；
- 匹配随机基准；
- 参数敏感度；
- 候选池数量披露；
- 多重尝试控制；
- 前向跟踪；
- 反例记录。

即使达到门槛，也只允许晋级为：

> 优先研究角度 / 在所测条件下具有继续研究价值

不得写成长期预测保证。

### 9.5.4 投注与高级倍投

必须具备：

- 实际赔率；
- 实际注数范围；
- 单注金额；
- 本金与最大允许回撤；
- 最大倍数；
- 一轮累计暴露；
- 连错压力测试；
- 深层中奖后净损益；
- 技术×资金路径2×2对照；
- 熔断与强制复位；
- 多方案同步高倍风险。

仅凭历史净收益更高，不得晋级。

### 9.5.5 内容生产

至少积累一组可比较内容后再调整默认值。建议：

- 10个以上同类型视频；
- 曝光量足以比较；
- 点击率、留存、观看时长分开；
- 不改写实验事实。

---

## 9.6 自动允许与人工门槛

### 可以自动完成

- 追加学习事件；
- 更新技术原子使用次数；
- 更新成功、失败和异常计数；
- 建立相似方案链接；
- 发现规则冲突；
- 生成规则候选；
- 生成晋级或降级建议；
- 运行回归检查；
- 重建完整总典；
- 更新系统状态和哈希。

### 不得未经确认自动完成

- 修改V3.2底层软件格式；
- 删除旧规则和失败记录；
- 把一次盈利技术设为优先；
- 提高技术有效性证据；
- 提高默认资金风险；
- 将候选规则写成正式规则；
- 改写历史回测；
- 重复使用已消耗样本外；
- 发布新的正式版本。

---

## 9.7 每次批次结束后的固定学习流程

1. 锁定批次ID和数据哈希；
2. 检查候选、版本和失败记录是否完整；
3. 登记样本外消耗；
4. 写入`批次索引.jsonl`；
5. 提取0—10条高价值学习事件；
6. 更新`学习事件索引.jsonl`；
7. 更新`技术原子表现档案.jsonl`；
8. 更新`软件行为证据索引.jsonl`；
9. 检查是否触发已有规则候选；
10. 更新`规则候选池.jsonl`；
11. 生成晋级/降级建议，不自动批准；
12. 更新`系统状态.json`和`当前任务.json`；
13. 重建总典；
14. 运行系统校验；
15. 返回更新后的系统工作包和本次独立方案包。

如果本批次没有产生新经验，可以记录：

```text
学习事件数量=0
原因=没有出现超出既有规则的新事实
```

不能为了显得“进化”强行制造经验。

---

## 9.8 反证和降级机制

系统必须主动寻找反证：

- 新批次是否推翻旧候选；
- 前向数据是否弱于历史；
- 软件新版本是否改变行为；
- 参数轻微变化是否崩溃；
- 随机方案是否达到相似表现；
- 规则是否只在一个位置或时间段有效。

触发以下任一情况时，规则进入`DEMOTE`或`CONFLICT`审查：

- 连续2个独立批次出现反例；
- 样本外与样本内方向相反；
- 结果主要由极少数事件贡献；
- 软件环境变化；
- 回归测试失败；
- 新证据表明旧解释错误。

---

## 9.9 技术原子表现档案

每个技术原子累计：

- 使用方案ID；
- 使用批次数；
- 玩法和位置；
- 参数范围；
- 样本内表现；
- 样本外表现；
- 前向表现；
- 随机基准位置；
- 参数敏感度；
- 常见失败模式；
- 与其他原子的相关性；
- 当前优先级；
- 当前证据等级。

档案记录的是历史表现，不是未来承诺。

---

## 9.10 软件行为证据索引

每条软件行为独立登记：

- 主张；
- 软件环境；
- 验证方式；
- E0—E3；
- 来源；
- 已知边界；
- 是否可作为生成硬规则；
- 是否需要复验。

格式证据与行为证据必须分开，不能因文件导入成功就推断真实投注顺序。

---

## 9.11 系统工作包治理

以后正式信息源是：

```text
AI挂机方案生成系统工作包_Vx.y.z.zip
```

每次完整任务默认由三个核心交付组成：

1. 本次独立方案套/结果包；
2. 本次方案验证讲解PPT；
3. 更新后的系统工作包。

系统工作包保存规则、索引、状态和摘要；老批次大型文件单独归档，通过文件名和SHA256引用，避免工作包无限膨胀。

正式源优先级：

1. 工作包内模块和结构化索引；
2. 工作包内自动合并总典；
3. 独立发送的总典或优化记录便捷副本；
4. 聊天记录。

---

## 9.12 防膨胀规则

工作包默认只保留：

- 所有正式源模块；
- 所有结构化索引；
- 当前系统状态；
- 当前输入模板；
- 最近批次的摘要；
- 外部结果包的文件名和哈希；
- 上一正式版本完整ZIP用于回滚。

不默认长期内嵌：

- 视频原文件；
- 所有逐期明细；
- 重复开奖数据；
- 每批全部失败TXT；
- 大型截图；
- 已有独立ZIP的重复副本。

达到以下任一条件时进行整理：

- ZIP超过100MB；
- 批次归档超过30个；
- 同一数据重复保存3份以上；
- 索引与正文出现明显重复。

---

## 9.13 正式规则晋级审批单

```text
候选规则ID=
规则内容=
适用范围=
支持批次=
反对批次=
独立样本外数量=
前向数量=
随机基准=
参数敏感度=
软件证据=
错误晋级风险=
回归测试=
建议动作=PROMOTE/KEEP/DEMOTE/REJECT
用户决定=
生效版本=
正式写入位置=
回滚点=
```

---

## 9.14 回归检查清单

任何正式升级前必须检查：

- [ ] V3.2底层编码未改变；
- [ ] 枪弹同目录规则未改变；
- [ ] GUI轮投没有被写成TXT可控字段；
- [ ] E1/E2没有被误写成E3；
- [ ] 已消耗样本外没有恢复为独立；
- [ ] 失败版本没有删除；
- [ ] 候选池数量仍然披露；
- [ ] 默认交付仍是方案套；
- [ ] 并投总暴露仍需计算；
- [ ] 高级倍投仍有上限和熔断；
- [ ] 内容表现没有替代实验结果；
- [ ] 模块、JSONL、总典和哈希同步；
- [ ] 旧版本可回滚。

---

## 9.15 当前暂定晋级参数

```text
流程规则复审=3个真实批次或30个冻结实验
技术角度复审=至少3段独立样本外并完成随机基准
内容默认值复审=至少10个可比较视频
工作包保留完整近期批次=最近1至3批
规则候选自动晋级=禁止
```

状态=暂定默认值  
证据=系统治理起点，尚未经过足够批次验证  
允许调整=是  
复审条件=完成3个V3.5.0闭环批次后

## 9.16 PPT与自我进化隔离

PPT属于内容层产物，不属于技术证据。

允许从PPT制作过程中学习：

- 哪种页序更容易理解；
- 哪种图表更清楚；
- 哪种旁白更精简；
- 哪些术语需要改成大白话。

禁止因PPT或视频效果：

- 修改冻结参数；
- 删除失败候选；
- 提高A-E或E0-E7等级；
- 把接近随机改写为有效；
- 为了故事完整伪造收益、本金、赔率或软件行为。


---

## 9.17 V3.8.0总控知识治理

新增四类正式索引：方案组合案例、负面方案模式、功能覆盖、总控验收测试。

治理规则：

1. 组合案例可以因结构清晰而收录，但技术效果等级仍按原方案证据；
2. 负面模式出现一次即可进入观察，重复出现后提高门控优先级；
3. 功能覆盖等级只表示被使用和被验证的程度，不表示有效性；
4. 总控评分权重不得根据单次冠军自动晋级；
5. 外部参考被用户认可不等于技术规则晋级；
6. 行为测试未实际运行时保持`PENDING_RUNTIME`；
7. 未来模型执行失败必须记录为学习事件，不得因协议文字存在而判定通过；
8. 正式流程规则由用户明确确认后可以写入模块，但效果型结论仍需跨批次证据。
