# 05A_方案讲解PPT生产协议

版本：V3.8.0  
定位：把已经冻结的方案套逆向转化为可录制、可复核、以验证为核心的精简PPT；PPT不参与技术筛选，也不改变实验结论。

---

## 5A.1 核心定位

PPT不是“卖方案”的宣传页，而是一次公开实验的讲解骨架。固定叙事是：

```text
存在一种X方案或技术说法
→ 它依据Y1、Y2等设计逻辑
→ 我们先把规则固定并制作成挂机方案/人工测试思路
→ 再用历史数据、随机基准和样本外检验它的表现
→ 最后只给出与证据等级相匹配的有限结论
```

PPT必须让观众分清：

- 方案原理；
- 软件实现；
- 测试设计；
- 历史结果；
- 随机基准；
- 局限与下一步。

---

## 5A.2 默认生成条件

完成正式方案套并冻结后，默认自动生成PPT。以下情况仍可生成，但必须降级标注：

- 只有方案设计、没有历史验证：标记“设计稿/E0-E1”；
- 只完成历史验证：标记“历史实验/E4”；
- 使用重复或已消耗数据：明确写“非独立样本外”；
- 软件尚未导入：明确写“待E2/E3核对”；
- 没有合格方案：允许制作“本批为何没有入选方案”的复盘PPT。

PPT不得为了内容完整而伪造缺失数据。

---

## 5A.3 默认页数与一页一题

默认8—12页；复杂方案最多15页。每页只讲一个明确话题，标题本身应能回答“这一页要说明什么”。

推荐结构：

1. **封面问题**：X方案到底有没有用？
2. **技术说法**：这种方案通常怎样理解？
3. **设计原理**：Y1、Y2分别是什么？
4. **方案结构**：由哪些子方案、位置或窗口组成？
5. **执行方式**：人工思路、挂机文件、轮投/并投如何运行？
6. **冻结规则**：哪些参数在看结果前已经固定？
7. **测试方法**：数据范围、训练/验证/样本外、随机基准。
8. **核心结果**：命中、回撤、连错、成本或套装整体表现。
9. **随机对照**：相同条件下，随机方案能做到什么程度？
10. **有限结论**：有效、接近随机、失败或证据不足。
11. **下一步**：是否导入核对、前向跟踪或停止研究。
12. **风险提示/片尾**：历史结果不代表未来。

可按方案删减，但不能删除“冻结规则、结果边界、有限结论”。

---

## 5A.4 页面文字密度

默认每页：

- 1个短标题；
- 1句核心结论；
- 最多3个短要点；
- 优先使用流程图、结构图、时间轴、对照柱图或数字卡片；
- 不把长段旁白直接堆在页面上；
- 不把完整回测明细表塞进正文页。

单页正文建议不超过70个汉字；结果页可适度增加，但必须保留视觉层级。

---

## 5A.5 旁白备注规则

每页必须在PowerPoint备注区放入对应旁白。默认：

- 1—3句；
- 约40—100个汉字；
- 只补充页面没有写全的逻辑；
- 口语化、可直接朗读；
- 不重复逐字念页面；
- 不加入页面证据没有支持的新结论。

旁白应尽量短。复杂技术先讲直觉，再讲参数；不要在一页同时解释多个分支。

---

## 5A.6 开场模板

推荐开场表达：

> 有一种X方案，它的思路是Y1、Y2。听起来似乎有道理，但实际投注效果怎样不能靠感觉。我们把规则固定，制作成挂机方案或人工测试思路，再用历史数据和随机对照验证一下。

开场必须包含：

- 测试对象；
- 核心原理；
- 为什么值得测；
- 不预设盈利结论。

---

## 5A.7 结果展示规则

结果页优先显示：

- 样本数量；
- 命中率或收益；
- 最大回撤；
- 最大连错；
- 随机基准分位；
- 训练、验证、后段是否一致；
- A-E分类和E0-E7证据等级。

若赔率、本金或单注金额未知，不显示虚构利润，只显示命中、注数和风险结构。

异常高收益必须同时显示：候选池数量、是否多次扫描、参数敏感度和随机基准。

---

## 5A.8 视觉默认值

在用户没有指定样式时：

- 16:9宽屏；
- 简洁研究/实验室风格；
- 大标题、少文字、强数字；
- 同一套PPT最多使用两种字体；
- 颜色只承担分类含义，不用炫技装饰；
- 盈利不默认使用夸张绿色，亏损不使用恐吓式红色；
- 每页保留足够留白；
- 图表只展示支持当前话题的数据。

用户可在`01_本次输入/PPT样式与视频讲解偏好.md`覆盖默认值。

---

## 5A.9 方案结构图要求

PPT应尽量把方案转换成观众可理解的结构，而不是直接展示软件字段：

```text
上一期开奖/历史窗口
→ 提取信号
→ 映射到号码或位置
→ 子方案分工
→ 轮投/并投/枪编排
→ 产生下一期投注
```

若软件真实行为尚未E3，只能写“预期执行流程”或“文件设计流程”，不得写成已确认行为。

---

## 5A.10 与视频内容包的关系

- `05_视频内容与SEO生产协议`负责标题、封面、描述、标签和公开视频边界；
- 本模块负责PPT页序、页面信息、图表和逐页旁白；
- 两者共享同一组冻结事实；
- PPT页数不等于视频时长；
- 内容表现只能优化表达，不能倒推修改技术规则。

---

## 5A.11 默认PPT交付物

每个正式方案套默认输出：

1. `方案ID_方案验证讲解_V1.pptx`；
2. `方案ID_PPT逐页脚本与旁白.md`；
3. `方案ID_PPT事实校验表.json`。

事实校验表至少包含：

```text
方案ID=
方案版本=
数据范围=
候选池数量=
冻结时间=
训练/验证/样本外=
随机基准=
证据等级=
软件导入等级=
PPT页数=
每页事实来源=
未验证主张=
```

---

## 5A.12 PPT生成前检查

- [ ] 方案和参数已冻结；
- [ ] 方案ID、版本、执行方式一致；
- [ ] 页面数字与验证报告一致；
- [ ] 训练、验证、样本外没有混写；
- [ ] 重复数据已披露；
- [ ] 随机基准没有被省略；
- [ ] 失败候选数量没有隐藏；
- [ ] 软件E1/E2/E3边界准确；
- [ ] 每页只有一个核心话题；
- [ ] 每页备注旁白简短可朗读；
- [ ] 没有“稳赚、必中、保证收益”等表达；
- [ ] 结论与A-E、E0-E7一致。

---

## 5A.13 自动模式的PPT选择逻辑

AUTO_DISCOVERY下：

1. 默认给最终正式方案套制作一份PPT；
2. 若最终没有合格方案，制作“失败/接近随机复盘PPT”；
3. 若多套方案都具有不同教育价值，主PPT仍只讲一套，其余列入后续选题；
4. 不因某套结果更适合标题而替换实验入选规则；
5. PPT制作完成后才允许优化标题和页面顺序，不允许修改实验参数。


## V3.6.2 交付实物完整性强制规则

每次完成任务时，`02_本次输出`不得只保存文件名、引用或SHA256。必须物理包含：

1. 本批独立挂机方案套ZIP；
2. 至少一个可直接导入软件的挂机方案TXT；
3. 本批方案讲解PPTX；
4. 三件套交付说明。

如果方案ZIP、挂机方案TXT或PPTX任意一项缺失，任务状态必须判定为`DELIVERY_INCOMPLETE`，不得宣称完整交付。


---

## V3.7.2 PPT美工强制规则

用户指出：仅有纯文本页面的PPT不合格。以后PPT交付必须同时满足以下条件：

1. **不得交付纯文本页**：除极少数封面/结论页外，每页必须有版式设计、图示、流程、对照、数字卡片或图表之一。
2. **必须有统一视觉主题**：固定背景色、主色、辅助色、标题样式、页脚和页面编号。
3. **一页一个话题**：标题必须直接表达该页要讲的核心观点，正文不超过4条短句。
4. **备注区写旁白**：旁白必须放入PowerPoint备注，不堆在页面正文。
5. **结果页必须图形化**：涉及命中率、样本区间、随机基准、敏感性对比时，应优先使用图表、数字卡片或对照结构。
6. **风险页必须视觉突出**：REUSED_DATA、B级观察、未完成前向验证等关键限制必须在页面上显眼呈现。
7. **导出前必须渲染自检**：用页面截图或缩略图检查是否仍像“Word搬家”；若像纯文本稿，必须重做。
8. **工作包验收**：若PPT没有明显美工层级，任务状态不得标记为完整完成。

推荐最低结构：

- 封面：大标题 + 关键数字 + 风险标签；
- 规则页：双子方案结构图；
- 原理页：映射示意；
- 执行页：轮投时间线；
- 结果页：柱状图或数字卡片；
- 风险页：随机基准/数据复用警示；
- 结论页：等级、前向期数、纪律。


## V3.7.2：公开视频PPT口语化表达强制规则

PPT是给观众看的，不是给软件或AI看的。以后生成讲解PPT时，必须执行以下规则：

1. **禁止在观众可见页面使用内部编号**：例如 B001、B002、BATCH、B370_SET、REUSED_DATA、E4/E5、GUI 等。
2. 软件方案TXT和内部冻结记录可以保留编号；但PPT页面和备注旁白必须改成自然表达。
3. 推荐替代表达：
   - B001 → 方案1 / 第一个方案 / 第一个思路；
   - B002 → 方案2 / 第二个方案 / 第二条路径；
   - REUSED_DATA → 复用同一批历史数据；
   - E4/E5 → 已做历史验证 / 还缺少新增前向验证；
   - GUI → 软件界面 / 软件里的轮投开关；
   - 方案X、Y1、Y2 → 本次方案、思路1、思路2。
4. 页面标题必须像人讲课，不得像程序字段名。
5. 备注旁白也要口语化，不能出现内部文件名、编号堆叠或JSON式表达。
6. PPT出片前必须做一次术语扫描；若观众页仍出现内部编号，判定为`PPT_COPY_NOT_HUMANIZED`。

原则：文件名可以工程化，PPT表达必须公开视频化。


---

## V3.7.4：PPT实操教程风格强制规则

用户上传公开视频样本后，确认本系统生成的PPT不能再做成“美化报告”，而要更接近用户真实讲解方式：**大白话规则说明 + 实际号码演示 + 简短结果复盘**。

### 1. PPT定位

PPT是录视频时的讲解提纲和规则总览，不是完整研究报告。正式验证细节、候选池、证据等级、随机基准等应完整保存在方案包和工作包中，但不必全部挤进观众页面。

### 2. 页面密度允许不均匀

- 核心技术页：必须讲清“看什么、怎么选、为什么这样选、用一期号码怎么跑”；
- 过渡页/风险页：可以只有一句话、一张图或一个关键数字；
- 辅助统计页：只保留影响观众理解的核心数字，其余放备注或工作包。

严禁为了填满页面而制造条目。严谨不等于把全部统计都展示出来。

### 3. 必须有真实运行实例

每份方案讲解PPT至少要有一页“实际跑一遍”的页面，展示：

```text
上一期开奖 → 本期看哪个位置/规则 → 生成哪些号码 → 下一期开奖 → 是否命中 → 下一期换什么
```

若使用的是演示例子而不是真实历史号码，必须显眼标注“演示例子”。优先使用真实历史数据。

### 4. 备注区改为提示词

用户不是严格照稿朗读。备注区默认写提示词、转场句和提醒点，不默认写长篇完整旁白。例如：

```text
先说为什么想到轮投。
强调不是两个一起投。
举上一期千位为0的例子。
这里慢一点，把5个号码一个个指出来。
```

只有用户明确要求逐字稿时，才写完整朗读稿。

### 5. 语言风格

页面标题应像真人视频标题，例如：

- “先别管公式，先知道它怎么跑”
- “举几期真实号码，跑一遍就明白了”
- “最需要警惕的一点”

避免系统化表达：

- “技术假设验证模块”；
- “冻结边界与证据等级”；
- “数据复用风险提示”；
- “候选池搜索空间披露”。

这些内容可以放备注或工作包，不放观众主画面。

### 6. 风险提醒页

风险提醒不要求每次做成长列表。允许整页只放一句强提醒，例如：

> 只换一下谁先开始，结果就明显变了。

> 看起来不错，但它还不是未来有效的证明。

这种表达比堆满免责声明更适合公开视频。

### 7. 技术解释底线

核心技术不能只给名字。至少要让观众听懂：

1. 看上一期哪个位置；
2. 用什么规则选号码；
3. 一期真实号码如何产生下一期选择；
4. 什么时候判定命中或失败。

无法讲清来源的技术，不得用科学化名称包装。

### 8. 下一版PPT验收

若PPT看起来仍像“标题 + 4条系统要点”的模板报告，应判定为 `PPT_STILL_REPORT_STYLE`，必须重做。


---

## V3.8.0：静默总控与外部参考的PPT边界

1. PPT必须在10模块完成最终选择和冻结后生成；
2. 外部参考被采纳时，可以说明“参考某种流传方案进行验证”，但不能把来源自述写成事实；
3. 外部参考被部分吸收时，只讲最终冻结逻辑，必要时用一句话说明仅保留了某个创意；
4. 外部参考被否定时，不强行为其制作正面教程；可以改成“为什么这个说法经不起验证”的实验视频；
5. 页面仍执行不均匀信息密度：核心技术讲清，辅助统计简化，风险页可只有一句话；
6. PPT内容表现不得参与总控评分中的技术证据和历史效果评价。
