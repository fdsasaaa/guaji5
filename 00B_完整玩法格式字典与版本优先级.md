# 00B_完整玩法格式字典与版本优先级

版本：V3.9.7-FORMAT-REGISTRY-MERGE-1  
状态：当前格式知识覆盖层  
适用范围：方案TXT、玩法/类别映射、专属字段、投注内容语义、更多设置与倍投格式

## 1. 目的

本文件解决一个历史断点：当前 `01_软件格式与已验证执行规则.md` 中仍保留“缺模板时回到V3.2”的历史文字，但V3.2原文件已经不再作为可访问正式依赖。

从本版本开始：

> **任何AI不得再把“去找V3.2旧文件”作为生成前置条件。**

所有当前已掌握的玩法格式知识必须收敛到 GitHub 当前仓库，机器唯一格式总表为：

`controller/betting_format_registry.json`

如果某个已知玩法在该注册表中缺少条目，状态定义为 `REGISTRY_DEFECT`，这是仓库需要修复的问题，不是让用户重新寻找旧文件的问题。

## 2. 合并优先级：新版本永远优先

本次补全不是把V3.4覆盖到V3.9，而是做“新规则为主、旧资料补空”的单向合并。

优先级固定如下：

1. 当前 GitHub `main` 已验证规则、禁用规则、证据等级和软件行为证据；
2. GitHub 中具有明确用户确认或验证证据、但尚未收敛进main格式总表的更新事实；
3. V3.4完整接管版中当前仓库仍为空缺的格式/分类事实；
4. 旧系统真实TXT样本，仅用于填补仍然缺失的专属字段、字段顺序和示例。

硬规则：

- 低优先级来源只能填空；
- 不得把V3.9的 `blocked/test_only/E0-E3` 状态降级或覆盖成V3.4旧状态；
- 不得用旧字段替换当前已验证的新字段；
- 发现冲突时保留高优先级值，并把低优先级事实记录到 `legacy_evidence`，不得静默覆盖；
- “更完整”不等于“更旧的字段更多”，最新证据边界始终优先。

## 3. 当前完整字典的职责

`controller/betting_format_registry.json` 必须同时保存：

- 已知一级分类及其专属字段；
- 已知二级/三级玩法语义；
- 当前 formal_allowed 组合；
- test_only / blocked / probe_only 边界；
- 投注内容语义与分隔符；
- 主TXT通用字段骨架；
- 普通倍投与高级倍投格式；
- 投注监控、换号、正反集、真实/模拟、时间、盈亏停止/跳转；
- 编码与换行；
- 证据来源、证据等级、已知运行边界；
- 当前无法正式运行但已经知道“应该怎样设计”的探针模板。

目标不是把所有功能都标成正式可用，而是做到：

> **已知玩法必须“知道是什么、怎样写、证据到哪里、能否正式生成”；不再允许出现既知道玩法名称、又不知道如何处理的无状态空白。**

## 4. 生成门控

生成器处理任意玩法时必须先查注册表：

- `formal_allowed`：可按登记模板正式生成；
- `verified_with_history`：二次核对当前同类证据后生成；
- `test_only` / `probe_only`：只允许隔离探针，不得混入正式方案；
- `blocked` / `abandoned`：禁止生成；
- 未登记：`REGISTRY_DEFECT`，停止该玩法生成并创建字典修复任务。

不得通过“换一个看起来类似的一级分类”“用数学等价格式改写”“删掉不懂的专属字段”绕过注册表。

## 5. V3.4补充的正确使用方式

V3.4继续作为历史完整接管证据之一，但只承担补空角色。它提供的分类体系、更多设置、监控、高级倍投、方案套和分析路由等内容，如果当前V3.9已有更新版本，则沿用V3.9；只有当前V3.9格式总表为空缺时才吸收V3.4/旧样本。

本次已明确补回的关键历史格式证据包括：

- 冷热温出号完整专属字段族：`冷热温统计期数`、`冷热温出号类型`、`冷热温容错个数`；
- 冷热温真实旧TXT中的完整通用字段顺序与更多设置字段；
- 高级定码轮换投注内容的局结构；
- 组合方案出号与组合方案轮投分隔符；
- 已知20个formal_allowed玩法组合；
- 投注监控、换号、正反集、真实/模拟、时间、盈亏停止/跳转的当前边界。

其中任何与当前V3.9冲突的旧值都不得成为最终值。

## 6. 与01模块的关系

`01_软件格式与已验证执行规则.md` 继续负责“人读规则与证据边界”；本文件和机器注册表负责消除历史回滚依赖。

如果01仍出现“从V3.2回滚源提取”的历史文字，当前执行解释统一改为：

> 先查询 `controller/betting_format_registry.json`；注册表已登记则直接按当前最高优先级条目执行；注册表缺失则判定 `REGISTRY_DEFECT` 并修复仓库，不得要求用户寻找V3.2。

01中更严格的新禁令、证据边界和字段语义仍然优先，不因本覆盖层而放宽。

## 7. CI硬门槛

仓库必须运行：

`python tools/validate_betting_format_registry.py`

至少检查：

1. 当前01列出的formal_allowed组合全部存在于注册表；
2. 所有已知一级分类都有显式状态；
3. 不允许 `unknown` 作为已知分类最终状态；
4. 版本优先级存在且V3.4不能覆盖current；
5. 高级倍投、监控和更多设置关键字段存在；
6. 冷热温三个专属字段存在；
7. 未登记玩法默认BLOCK而不是猜测；
8. 不再存在“必须依赖外部V3.2文件才能继续”的机器依赖。

## 8. 结论

从本覆盖层进入正式源后，GitHub本身应成为玩法格式知识的自包含入口。后续发现新玩法、新字段或用户导入确认时，只向注册表追加/升级证据，不回退覆盖已有更高版本事实。
