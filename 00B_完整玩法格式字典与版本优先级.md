# 00B_完整玩法格式字典与版本优先级

版本：V3.9.8-FORMAT-REGISTRY-MONITOR-FIX  
状态：当前格式知识覆盖层  
适用范围：方案TXT、玩法/类别映射、专属字段、投注内容语义、更多设置与倍投格式

## 1. 目的

本文件解决一个历史断点：当前 `01_软件格式与已验证执行规则.md` 中仍保留“缺模板时回到V3.2”的历史文字，但V3.2原文件已经不再作为可访问正式依赖。

从本版本开始：

> **任何AI不得再把“去找V3.2旧文件”作为生成前置条件。**

所有当前已掌握的玩法格式知识必须收敛到 GitHub 当前仓库。机器知识分成两层：

- `controller/betting_format_registry.json`：当前正式/测试/阻塞状态与最高优先级格式事实；
- `controller/legacy_play_grammar_catalog.json`：旧系统已经掌握但当前main没有逐项展开的完整玩法语法知识，只补知识，不提升当前证据等级。

如果某个已知玩法在两层知识中都没有记录，状态定义为 `REGISTRY_DEFECT`，这是仓库需要修复的问题，不是让用户重新寻找旧文件的问题。

## 2. 合并优先级：新版本永远优先

本次补全不是把V3.4覆盖到V3.9，而是做“新规则为主、旧资料补空”的单向合并。

优先级固定如下：

1. 当前 GitHub `main` 已验证规则、禁用规则、证据等级和软件行为证据；
2. GitHub 中具有明确用户确认或验证证据、但尚未收敛进main格式总表的更新事实；
3. V3.4完整接管版中当前仓库仍为空缺的格式/分类事实；
4. 旧版26字母生成器文档中的完整投注语法和一级分类知识；
5. 旧系统真实TXT样本，仅用于填补仍然缺失的专属字段、字段顺序和示例。

硬规则：

- 低优先级来源只能填空；
- 不得把V3.9的 `blocked/test_only/E0-E3` 状态降级或覆盖成V3.4/旧生成器状态；
- 不得用旧字段替换当前已验证的新字段；
- 发现冲突时保留高优先级值，旧事实只作为历史证据保存，不得静默覆盖；
- “更完整”不等于“更旧的字段更多”，最新证据边界始终优先。

## 3. 当前完整字典的职责

`controller/betting_format_registry.json` 负责当前可执行状态，至少保存：

- 当前已知一级分类及其专属字段；
- 当前正式二级/三级玩法语义；
- formal_allowed、verified_with_history、test_only、blocked、probe_only边界；
- 当前投注内容语义与分隔符；
- 主TXT通用字段骨架；
- 普通倍投与高级倍投格式；
- 投注监控、换号、正反集、真实/模拟、时间、盈亏停止/跳转；
- 编码与换行；
- 证据来源、证据等级、已知运行边界。

`controller/legacy_play_grammar_catalog.json` 负责防止“系统曾经会、后来字典没展开就失忆”，至少保存：

- 历史11个实用一级分类/生成选项及当前处置；
- 遗漏出号6个历史锁定字段；
- 固定取码、高级定码轮换等投注内容外层结构；
- 二位、三位、四位、五位、定位胆、龙虎、趣味、不定位的投注内容格式；
- 直选单式/复式、组三/组六、组选保胆/包胆等已知语法；
- 任二/任三旧注数控制知识；
- 旧规则与当前V3.9冲突时的明确胜出方。

目标不是把所有旧功能重新标成正式可用，而是做到：

> **已知玩法必须“知道是什么、怎样设计、怎样表达、当前证据到哪里、能否正式生成”；不再允许出现既知道玩法名称、又不知道如何处理的无状态空白。**

## 4. 生成门控

生成器处理任意玩法时先查当前注册表，再查历史语法目录：

- `formal_allowed`：可按当前登记模板正式生成；
- `verified_with_history`：二次核对当前同类证据后生成；
- `test_only` / `probe_only`：只允许隔离探针，不得混入正式方案；
- `blocked` / `abandoned`：禁止生成；
- 只在历史语法目录出现：说明“设计知识已知、当前正式资格未建立”，必须走探针/注册表升级；
- 两层都未登记：`REGISTRY_DEFECT`，停止该玩法生成并创建字典修复任务。

不得通过“换一个看起来类似的一级分类”“用数学等价格式改写”“删掉不懂的专属字段”绕过注册表。

## 5. V3.4与旧生成器补充的正确使用方式

V3.4继续作为历史完整接管证据之一，但只承担补空角色。旧26字母生成器的 `scheme_generation_rules_complete.md`、`PROJECT_SOLUTION_LOG.md`、`quick_reference_guide.md` 与真实TXT样本同样只承担补知识角色。

本次补回的关键历史知识包括：

- 冷热温出号完整专属字段族：`冷热温统计期数`、`冷热温出号类型`、`冷热温容错个数`；
- 冷热温真实旧TXT中的完整通用字段顺序与更多设置字段；
- 遗漏出号6个历史锁定字段；
- 固定取码 `位置范围|类型范围|投注内容` 的历史格式；
- 高级定码轮换 `round|bet_content|param1|param2` 的外层结构；
- 二/三/四/五位数的直选、组选等完整投注内容语法；
- 历史11个实用一级分类/生成选项；
- 已知20个main formal_allowed玩法组合；
- 2026-08-08已确认的前二直选单式“完整两位号码”语义；
- 投注监控、换号、正反集、真实/模拟、时间、盈亏停止/跳转的当前边界。

明确不恢复的旧错误/旧状态：

- 旧版曾删除 `组合方案轮投`，但当前V3.9已经重新建立E2格式，因此保留V3.9；
- 旧版 `龙虎高级开某投某` 曾有成功记录，但当前01禁止虚构该策略名/字段，因此当前禁令胜出；
- 旧版 `冷热温出号数据` 字段当前已被列为异常虚构字段，不得复活；
- 旧模板中的 `LHHC` 只保留为旧样本事实，不得覆盖当前各分类的软件名称规则；
- 旧TXT中若出现 `投注监控=False-50000`、`True-50000` 或其他包含非0/1字符的监控序列，统一视为旧样本错误，不得作为兼容写法恢复。

### 5.1 投注监控01序列硬规则（2026-08-13用户纠错）

投注监控与金额字段是两套完全不同的语义，不得混用。

当前硬规则固定为：

- 关闭：只能精确写 `投注监控=False-`；
- 开启：只能写 `投注监控=True-<非空01序列>`；
- `0=挂`；
- `1=中`；
- 监控序列字符集只允许 `0` 与 `1`；
- `2-9` 均没有投注监控状态含义，尤其不能把 `50000` 之类金额值误塞进投注监控；
- `False-50000`、`True-50000`、`True-0121` 一律判定为非法；
- 历史文件中出现上述非法值时，只保存“这是旧样本错误”的证据，不得复制到当前模板、默认值或正式方案。

金额 `50000` 可以出现在 `盈利停止/亏损停止/盈利跳转/亏损跳转/真实投注/模拟投注` 等对应金额或阈值字段中，但不能据此推导投注监控允许相同载荷。

## 6. 与01模块的关系

`01_软件格式与已验证执行规则.md` 继续负责“当前人读规则与证据边界”；00B与两层机器字典负责消除历史回滚依赖和知识失忆。

如果01仍出现“从V3.2回滚源提取”的历史文字，当前执行解释统一改为：

> 先查询 `controller/betting_format_registry.json`；当前正式条目优先。若当前表未登记，再查 `controller/legacy_play_grammar_catalog.json` 获取已知设计语法并按当前证据降级处理；两层都不存在才标记 `REGISTRY_DEFECT`。不得要求用户寻找V3.2。

01中更严格的新禁令、证据边界和字段语义始终优先，不因本覆盖层而放宽。

## 7. CI硬门槛

仓库必须运行：

`python tools/validate_betting_format_registry.py`

至少检查：

1. 当前01列出的formal_allowed组合全部存在于当前注册表；
2. 当前已知一级分类都有显式状态；
3. 历史11个实用一级分类/生成选项都有当前处置；
4. 不允许 `unknown` 作为已知分类最终状态；
5. 版本优先级存在且V3.4/旧生成器不能覆盖current；
6. 高级倍投、监控和更多设置关键字段存在；
7. 冷热温三个专属字段和遗漏出号六字段存在；
8. 二/三/四/五位数等主要投注语法族存在；
9. 未登记玩法默认BLOCK/REGISTRY_DEFECT而不是猜测；
10. 不再存在“必须依赖外部V3.2文件才能继续”的机器依赖；
11. 投注监控关闭值必须是 `False-`，开启语法必须是 `True-{01_sequence}`，并拒绝任何含2-9的监控状态序列或金额型载荷。

## 8. 结论

从本覆盖层进入正式源后，GitHub本身成为玩法格式知识的自包含入口。后续发现新玩法、新字段或用户导入确认时，只追加或升级当前注册表/历史语法目录的证据；任何旧版本整表导入都不得覆盖已有更高优先级事实。
