# RUN-20260806-RETURN-REBATE-001 验证报告

## 已完成
- 读取 main 正式源关键规则、当前状态、软件证据、资金编排协议和本金止盈止损协议。
- 使用 `01_本次输入/哈希分分彩_20260731_0181至0380.txt` 的200期数据完成个位热7码复算。
- 完成经济结构假设核算、四条资金路径比较、强回利逐档首次命中证明。
- 本地生成方案套ZIP、PPTX、YouTube发布参考TXT，并生成外层完整交付ZIP。
- PPT已渲染为图片并完成版面检查。

## 用户导入失败回归
用户实际导入旧修正版后触发：

```text
System.NullReferenceException
IntelligentPlanning.ConfigurationStatus.Scheme.get_Value()
```

### 根因
1. 导入目录混入 `投注数字_直接查看.txt`。该文件是说明文字，不是软件方案，但扩展名为 `.txt`，软件会按方案对象解析，导致 `Scheme.Value` 为空。
2. 固定7码错误使用了不存在的通用字段 `投注内容=`；固定号码必须由已验证一级分类承载。
3. 旧文件第2行/专属字段组合没有继承定码轮换模板；正确结构应为：第2行 `定码轮换`、字段 `定码轮换内容=4 8 6 9 3 5 7`、`定码轮换单组=True`。
4. 定位胆固定位置方案不应写 `任选位置=4`，应保持 `任选位置=` 空值。
5. 高级倍投主方案应使用8个占位1，并由 `GJBTScheme/高级倍投主配置.txt` 提供8局固定9字段；旧修正版只写了6个占位1。

### 修复后结构
- 高级倍投导入目录根部只保留1个主方案TXT。
- 唯一子目录为 `GJBTScheme/`，其中只保留 `高级倍投主配置.txt`。
- 说明、验证记录和普通倍投对照全部移出高级倍投导入目录。
- 首版修复状态机为：前4局 `1→2→6→18` 未中推进；第4局结束后无论中挂均进入4期1倍强制降压；第8局结束回第1局。

### 字节级验证
- 主方案：GBK、CRLF、末尾2个空行。
- 高级倍投配置：UTF-8 BOM、CRLF、8局、每局固定9字段。
- 第2行：`定码轮换`。
- 必填字段：`定码轮换内容=4 8 6 9 3 5 7`。
- 禁止字段：不存在 `投注内容=`。
- `任选位置=` 为空。
- 导入根目录不存在说明型TXT。
- 新高级倍投ZIP通过独立导入包校验器；旧修正版被校验器判定为无效。

## 30期耐久状态机追加设计

用户进一步要求几十期耐久运行。经核算，不能继续要求每个档位首次命中都完整回本；否则7码经济结构下倍数会近似扩张为 `1,2,6,18,54...`。

追加方案保持8局软件结构，但使用循环状态实现长时间运行：

```text
1倍：中留1，挂进2
2倍：中回1，挂进3
6倍：无论中挂都进4
第4—8局全部1倍：每中一次前进一局，每挂一次停留当前局；累计5次命中后回1
```

验证结果：

- 最高倍数由18降至6。
- 最高单期毛投入由252元降至84元。
- 30期全部未中总倍数36、毛投入504元、含返点净损468.72元。
- 建议测试期数上限30期，人工止损480元，测试本金不低于1500元。
- 固定号码200期同口径结构重放：原方案历史最大回撤351.54元，新方案240.52元；历史演示净值由511.98元降至469.98元。
- 新方案牺牲部分收益强度，换取更低单期暴露和约31.6%的历史回撤下降。

新增记录：

- `02_本次输出/个位热七码回利测试/30期耐久高级倍投设计.md`
- `02_本次输出/个位热七码回利测试/30期耐久高级倍投验证记录.json`

## 降级项
- 仓库约束文件中实际赔率、单注金额、本金、最大回撤和平台限额为空；本批经济结构为演示假设，不得视为平台实测。
- 高级状态倍投证据上限仍为E2；本次只修复文件结构和提供隔离状态机，不把其包装成已验证盈利方案。
- 软件真实导入、GUI字段恢复及中后/挂后ID实际跳转仍需要用户在当前软件版本复验。

## GitHub状态
- 修复与耐久设计继续写入原分支 `task/return-rebate-path-20260806` 和 Draft PR #28。
- 不直接修改 main。
