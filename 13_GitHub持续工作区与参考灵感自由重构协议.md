# 13_GitHub持续工作区与参考灵感自由重构协议

版本：V3.9.2  
状态：强制执行

## 1. GitHub作为唯一正式源

1. 私有仓库 `fdsasaaa/guaji5` 的 `main` 分支代表当前稳定正式版本。
2. 不再要求用户每次重复发送完整工作包；执行前先读取仓库最新版。
3. ZIP只是发布物，不是长期源。规则、索引、工具和方案源文件必须逐文件保存。
4. 每次修改使用任务分支，完成自动校验后再合并到`main`。
5. 仓库不可访问、核心文件损坏或版本状态冲突时，才要求用户补充材料。

## 2. “仅供参考”的准确含义

用户提供的参考方案只是灵感来源，不是必须忠实复刻的规格。总导演可以：

- 保留原思路直接实现；
- 拆分条件和执行层实现；
- 修改触发、号码、位置、周期、监控和资金路径；
- 大幅重构，甚至与原文面目全非；
- 否定原方法，只保留有价值的研究角度或趣味结构。

最终目标不是复述参考内容，而是生成质量更高、能够导入、能够执行、能够验证的方案。

## 3. 无法原样生成TXT时的强制处理顺序

1. 尝试直接表达。
2. 尝试使用软件已验证的监控、开某投某、轮投、枪弹结构、时间范围、盈亏跳转等能力拆分实现。
3. 尝试近似重构，但必须披露与原始参考的差异。
4. 尝试自由重构为新的可执行方案。
5. 只有软件能力无法覆盖任何合理替代方案、关键模板缺失且无法安全推导，或源文件损坏时，才允许阻断。

禁止因为某一条原始条件无法编码，就只交说明书或PPT而不交TXT。

## 4. 总导演全能力审议

每个候选方案必须审议以下能力，但不要求全部启用：

- 号码算法与条件映射；
- 正投、反投和不同触发值的差异；
- 监控、模拟/真实切换、循环切换；
- 时间范围、开始/停止条件；
- 盈利/亏损跳转、止盈止损和熔断；
- 方案轮投、并投、枪弹结构与多方案组合；
- 平倍、普通阶梯、非单调缓压、高级状态倍投；
- 随机基准、对立组、样本外和前向记录。

“不启用”必须来自逻辑判断，不能来自总导演没有想到。

## 5. 映射与资金路径防偷懒

1. 高级开某投某若0—9触发后输出完全相同，除非该字段语义明确要求常量，否则判定为错误类别或伪高级功能。
2. 未指定倍投时，也必须完成四路资金路径比较：平倍、有限阶梯、非单调缓压和状态型高级倍投。
3. 平倍可以胜出，但必须有比较记录，不能机械默认。
4. 禁止为追求历史收益使用无上限追损、伪装马丁或无法解释的高倍数。

## 6. 直接执行规则

收到方案要求、工作包或参考灵感后直接完成：设计、TXT、验证、PPT、记录更新和版本升级，无需等待二次确认。轻微缺口采用保守默认值并在交付中披露。

## 7. 对外交付与GitHub回滚规则

1. 标准方案任务完成后，默认只向用户发送两项成品：
   - 独立方案套ZIP；
   - 实操教程风格讲解PPTX。
2. 可导入TXT应包含在方案套ZIP内；除非排错需要，不要求再单独重复发送。
3. 系统规则、结构化索引、批次记录、学习事件、状态文件和版本记录必须直接回写GitHub，不再依赖更新工作包ZIP传递。
4. 更新后的系统工作包ZIP默认不生成、不发送。只有用户明确要求、需要离线归档、跨平台迁移，或GitHub暂时不可用时才生成。
5. 每次系统修改必须保留：任务分支、提交记录、差异、校验结果和合并记录。不得直接覆盖`main`且不留证据。
6. `main`只接收通过校验的版本；发生升级错误时，优先回滚到上一个已验证的`main`提交，而不是依赖用户保存的工作包。
7. 回滚不得删除失败版本的审计证据。失败提交、原因、修复提交和最终恢复点必须可追踪。
8. 仓库内部可以保存必要的方案源文件和构建记录，但不得把“内部归档”误解为必须向用户发送完整工作包。
