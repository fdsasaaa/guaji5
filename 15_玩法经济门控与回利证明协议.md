# 15_玩法经济门控与回利证明协议

版本：V1.0  
状态：本PR候选规则  
适用范围：任何声称“首次命中可覆盖此前累计亏损、当期成本并留微利”的挂机方案。

## 1. 禁止先定倍数再补经济参数

正式路径冻结前必须记录：玩法、实际注数、单注金额、当期总成本、中奖总返奖、中奖净收益、返点、抽水、理论命中率、历史命中率、最低投注单位、平台最高倍数、单注/单期/账户限额、可用本金与最大允许暴露。

任何字段为 `null`、未知或仅来自口头估计时，方案状态只能是 `ECONOMICS_GATED` 或 `BLOCKED`，不得标记为可实盘。

## 2. 逐档回利公式

设：

- `c`：1倍时所有并投方案的当期总成本；
- `P`：最坏命中情形下的中奖总返奖；
- `r`：按投注额计算的返点率；
- `S`：本档前累计倍数；
- `m`：本档倍数；
- `g`：预设最低微利。

本档首次命中后的净结果：

```text
net = P*m + c*(S+m)*r - c*S - c*m
```

必须逐档满足：

```text
net >= g
```

返点口径不确定时，正式保证必须使用 `r=0`；不得把未确认返点计入回本证明。

## 3. 反向生成最小整数倍数

当 `r=0` 时：

```text
m_i = max(最低允许倍数, ceil((c*S_{i-1}+g)/(P-c)))
```

若 `P<=c`，不存在递推回利路径，立即阻塞。每一档必须使用当前约束下的最小整数倍数，除非另有明确的风险降压理由。

## 4. 本金与平台双重封顶

生成过程必须同时检查：

- `c*sum(m_i) <= 本金`；
- `m_i <= 平台最高倍数`；
- `c*m_i <= 平台单期总额上限`；
- 单注金额不低于平台最低注单金额；
- 单注与账户累计限额均未触发。

最大有效期数是上述条件首次失效前的最后一档，不得由用户愿望直接指定。

## 5. 多方案并投

多方案同时投注时必须：

1. 证明号码是否互斥、可多中或存在重复下注；
2. 采用最坏命中数量计算 `P`；
3. 计算叠加注数与单期总成本；
4. 明确顶部GUI轮投状态；
5. 验证多方案是否共享或各自保存高级倍投状态。

未完成第5项E3运行验证时，高级多方案路径只能标记为 `PROBE_ONLY` 或 `BLOCKED_RUNTIME`。

## 6. 强制证据

每次运行证据目录至少保存：

- `economics_gate.json`；
- `recovery_proof.json`；
- 逐档明细；
- 全链不中损失；
- 至少命中一次概率；
- 最大单期暴露；
- 最大有效期数；
- 阻塞原因与解除条件。

## 7. 禁止事项

- 用历史命中率代替理论概率；
- 用“高概率至少中一次”包装正期望；
- 在未知奖金时声称回本；
- 超过本金后继续列出无效档位；
- 用简单翻倍表冒充反向经济设计；
- 缺少正式软件模板时猜测TXT字段。
