# 00B_统一交付文件夹规则

版本：V1.0  
状态：最高优先级交付包装覆盖层  
更新时间：2026-08-02

本文件只调整标准方案任务的**对外发送包装方式**。除交付包装外，方案创建、TXT内容、方案套ZIP内容、PPT内容、验证流程、资金规则、GitHub回写、版本记录和工作包策略均保持不变。

## 1. 优先级

本文件在“对外发送数量、文件夹结构和下载包装”方面，高于 `SYSTEM_MANIFEST.json`、`SYSTEM_STATE.json`、`系统状态.json`、`当前任务.json`、`00_启动入口与系统状态.md`、`00A_当前强制覆盖与废止规则.md` 及历史README中的旧交付表述。

旧文件中出现“方案ZIP与PPTX分开发送”“对外默认文件数为2”等内容时，统一按本文件执行；旧文字保留为历史演进证据，不得继续作为当前发送规则。

## 2. 当前唯一默认交付方式

标准方案任务仍然生成：

1. 一个独立方案套ZIP；
2. 一个对应的人工讲解型PPTX。

生成完成后，将这两个文件原样放入同一个文件夹。为了通过聊天、浏览器或制品系统发送文件夹，必须把该文件夹压缩成**一个统一交付文件夹ZIP**后发送给用户。

推荐结构：

```text
<方案ID>_交付文件夹.zip
└─ <方案ID>_交付文件夹/
   ├─ <原方案名称>_方案套.zip
   └─ <原方案名称>_人工讲解型PPT.pptx
```

统一交付文件夹内含且仅含：

- 原方案套ZIP；
- 原人工讲解型PPTX。

用户默认只收到外层这一个统一交付文件夹ZIP，不再分别收到两个下载文件。

## 3. 不得改变的内容

- 内层方案套ZIP继续保持原文件名、原结构、原TXT和原说明；
- PPTX继续保持原文件名、页面、备注、隐藏附录和固定品牌页；
- 不得把方案套ZIP解散后与PPT文件混装；
- 不得把方案内容与PPT内容合并成一个新文档；
- 不得把系统工作包、验证日志、构建清单、源代码或其他内部文件放入交付文件夹；
- 工作包ZIP仍默认不生成、不发送；
- 用户明确要求分开发送时，才允许例外。

这只是传输包装调整，其他内容不受影响。

## 4. 自动构建与校验

标准构建顺序：

```text
生成方案套ZIP与PPTX
→ python tools/package_delivery_folder.py
→ python tools/validate_delivery_folder_policy.py
→ 只上传并发送 *_交付文件夹.zip
```

校验必须确认：

- 外层ZIP只有一个顶层文件夹；
- 顶层文件夹内只有两个文件；
- 恰好一个文件为方案套ZIP；
- 恰好一个文件为PPTX；
- 不含系统工作包或其他附加文件。
