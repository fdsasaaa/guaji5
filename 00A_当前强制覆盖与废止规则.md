# 00A_当前强制覆盖与废止规则

版本：V3.9.2-OVERRIDE-2  
状态：最高优先级入口覆盖层  
更新时间：2026-08-01

本文件用于覆盖 `00_启动入口与系统状态.md` 中保留的历史条款。历史原文继续保留作为演进证据，但不得再作为当前执行规则。

接管顺序：

```text
AGENTS.md
→ SYSTEM_MANIFEST.json
→ SYSTEM_STATE.json
→ 00_启动入口与系统状态.md
→ 本文件00A
→ 05A_方案讲解PPT生产协议.md
→ PPT页面类型卡片.jsonl
→ PPT讲解验收测试集.jsonl
```

发生冲突时，本文件、SYSTEM_MANIFEST、SYSTEM_STATE、当前任务和05A模块优先于00文件中的旧条款。

---

## 1. 废止旧三文件交付

以下历史规则全部废止：

- 每次返回方案ZIP、PPTX和更新工作包ZIP三份文件；
- 自动三件套协议；
- 默认生成并发送下一版系统工作包；
- 逐页旁白或事实校验表作为独立交付文件。

当前默认只向用户交付：

1. 独立方案套ZIP；
2. 一个讲解PPTX。

系统规则、状态、索引和回滚记录直接写入GitHub。工作包ZIP只在用户明确要求、离线迁移或GitHub不可用时生成。

---

## 2. 废止独立PPT配套文件

以下历史交付物不再生成：

- 独立口播稿；
- 逐页旁白Markdown；
- PPT事实校验表JSON；
- 独立技术报告；
- 独立操作说明。

同一PPT内部包含主讲页面、演讲者备注、必要技术附录和固定品牌结束页。

---

## 3. 废止“先完整案例后规则”

旧的“先举例、再归纳规则”默认顺序废止。

当前强制顺序：

```text
封面
→ 技术定义
→ 真实技术缘由
→ 可独立执行的核心规则
→ 必要规则细节和边界
→ 完整实际案例
→ 验证结果
→ 风险分析
→ 使用建议
→ 明确结论
→ 技术附录（按需）
→ 固定品牌结束页
```

规则页可以使用极小数字示意帮助解释，但完整案例不得早于技术规则。

---

## 4. 当前PPT导演硬标准

- 每套项目只生成一个PPTX；
- 第一页必须是真正封面；
- 封面标题原则上不超过10字、最多12字；
- 技术缘由必须真实，不得编造；
- 规则必须完整到能够独立执行；
- 案例必须展示输入、过程、结果和判定；
- 屏幕文字与口头旁白严格分离；
- 每页先识别页面类型；
- 页数动态决定；
- 建议必须具体；
- 结论必须明确；
- 技术附录位于结论之后、品牌页之前；
- 品牌页必须为真正最后一页；
- 最后一页必须显示 `www.laocaimi.org` 和 `https://t.me/laocaimi1314`；
- 必须通过人工讲解审查、页面类型审查和全页渲染审查。

详细规则唯一来源：`05A_方案讲解PPT生产协议.md`。
