# 00A_当前强制覆盖与废止规则

版本：V3.9.3-OVERRIDE-6  
状态：最高优先级入口覆盖层  
更新时间：2026-08-01

本文件用于覆盖 `00_启动入口与系统状态.md` 中保留的历史条款。历史原文继续保留作为演进证据，但不得再作为当前执行规则。

接管顺序：

```text
AGENTS.md
→ SYSTEM_MANIFEST.json
→ SYSTEM_STATE.json
→ 00_启动入口与系统状态.md
→ 本文件00A
→ 05A_方案讲解PPT生产协议.md
→ 05B_固定首页第二页末页协议.md
→ 11_智能功能调度与资金路径编排协议.md
→ 11A_本金止盈止损设计与PPT披露协议.md
→ PPT页面类型卡片.jsonl
→ PPT讲解验收测试集.jsonl
→ PPT压缩与精度规则.json
→ assets/ppt/fixed_pages/PPT固定页资源清单.json
```

发生冲突时，本文件、SYSTEM_MANIFEST、SYSTEM_STATE、当前任务、05A、05B和11A模块优先于00文件中的旧条款。固定页位置、素材和复制方式发生冲突时，05B优先于05A。

---

## 1. GitHub是唯一正式规则源

项目规则、底层逻辑、协议、状态、索引、验收标准和后续修改必须直接写入GitHub。

聊天长期记忆只能帮助识别项目和定位仓库，不得替代GitHub正式源；尚未写入GitHub并通过校验的规则，不得宣称已经完成系统升级。

所有修改必须使用任务分支，通过仓库校验和GitHub Actions后才能合并到 `main`。

---

## 2. 废止旧三文件交付

以下历史规则全部废止：

- 每次返回方案ZIP、PPTX和更新工作包ZIP三份文件；
- 自动三件套协议；
- 默认生成并发送下一版系统工作包；
- 逐页旁白或事实校验表作为独立交付文件。

当前默认只向用户交付：

1. 独立方案套ZIP；
2. 一个讲解PPTX。

系统规则、状态、索引和回滚记录直接写入GitHub。工作包ZIP只在用户明确要求、离线迁移或GitHub不可用时生成。

---

## 3. 废止独立PPT配套文件

以下历史交付物不再生成：

- 独立口播稿；
- 逐页旁白Markdown；
- PPT事实校验表JSON；
- 独立技术报告；
- 独立操作说明。

同一PPT内部包含主讲页面、全部页面演讲者备注、隐藏技术附录和固定品牌结束页。

---

## 4. 废止机械拆页和固定正文结构

旧的“每个模块都单独成页”“为了结构完整补齐页面”和“所有页面统一卡片布局”全部废止。

当前规则：

- 每页必须提供不可替代信息；
- 删除后不影响理解的页面必须合并或删除；
- 简单技术正文主讲通常控制在7至9页；
- 定义、缘由、规则和边界可以按内容合并；
- 风险、建议和资金摘要可以合并；
- 没有数据不得制造结果页；
- 不把内部制作指令、过渡语或讲解顺序说明写进观众页面。

页数不机械固定，唯一标准是用最少页面把技术真正讲清楚。固定第一页、固定第二页和固定最后一页不属于“机械正文模板”，不得因正文动态化而删除。

---

## 5. 废止程序化和重复表达

主讲页不得优先使用 `n`、`n+3`、程序变量或内部字段表达简单规则。

必须优先转换为准确自然的人话，并通过数字环、箭头、颜色和具体案例消除左右、循环和边界歧义。

同一数字、图表或结论无新增意义时不得重复出现。允许留白，不得为了填满空间增加装饰图表、重复标签或无意义文字。

---

## 6. 挂机方案本金与止盈止损硬标准

每个正式挂机方案必须在冻结时明确：

- 建议本金金额；
- 止盈止损模式：不设置、只设置止损、只设置止盈或同时设置；
- 启用止盈时的止盈金额；
- 启用止损时的止损金额；
- 达到金额后的动作和计算依据。

止盈止损可以不设置，但不能不审议，也不能保持未决。

挂机方案PPT主讲部分必须显示建议本金金额；启用止盈或止损时必须显示对应金额，未启用项目必须明确写“不设置”。

不得为资金披露机械新增无价值页面，应优先并入风险、建议或资金路径页面。

隐藏附录必须保存单注金额、实际注数、资金路径、压力暴露、建议本金计算过程、止盈止损模式、金额、比例、达到后动作和软件证据；主讲与附录金额必须完全一致。

---

## 7. 当前PPT导演硬标准

- 每套项目只生成一个PPTX；
- 规则修改必须写入GitHub正式源；
- 第一页必须是真正封面；
- 标题必须准确表达项目核心动作，准确优先于绝对简短；
- 封面标题原则上不超过10字、最多12字；
- 默认顺序为“技术定义→真实技术缘由或实验规则说明→核心规则与必要边界→完整案例”；
- 技术缘由必须回答具体对象为什么被选择，不得编造；
- 规则必须完整到能够独立执行；
- 完整案例必须位于核心规则之后，并展示输入、过程、结果和判定；
- 每张候选页面必须通过页面价值门槛；
- 主讲只保留最关键数据；
- 封面、主讲、附录和品牌页全部必须有备注；
- 演讲者备注必须简短、自然、与本页对应；
- 风险建议必须说明停止、暂停或继续的具体对象；
- 建议只保留2至4条，并与结论一致；
- 隐藏附录位于结论之后、品牌页之前，正常播放自动跳过；
- 隐藏附录必须保存样本、命中、基准、统计、版本、验证状态、执行边界和资金设计等必要证据；
- 主讲数字、案例、资金金额、备注和隐藏附录必须一致；
- 品牌页必须为最后一张正常播放页面；
- 最后一页必须显示 `www.laocaimi.org` 和 `https://t.me/laocaimi1314`；
- `www.laocaimi.org` 必须链接到 `http://www.laocaimi.org`，Telegram地址必须链接到自身；
- 必须通过讲解审查、压缩去程序化审查、精度审查和全页渲染审查。

---

## 8. 固定首页、第二页和末页硬标准

所有正式PPT必须执行以下顺序：

```text
第1页：固定金属板背景封面
第2页：固定平台与联系页
第3页起：动态项目正文
结论之后：隐藏技术附录（按需）
最后一张正常播放页面：固定蓝金实验室结束页
```

具体要求：

- 第一页背景必须使用 `assets/ppt/fixed_pages/首页背景图谱.png`；
- 首页图片必须嵌入幻灯片背景层，禁止以可双击、可拖动的普通全页图片代替；
- 第二页必须直接使用 `assets/ppt/fixed_pages/固定第二页_平台与联系.pptx`；
- 第二页联系方式必须为 `Telegram：@laocaimi1314`，禁止出现QQ和旧号码；
- 第二页不得出现“稳赚”“必胜”“100%稳定提现”等不可验证承诺；
- 正文必须从第三页开始，固定第二页不得移动或删除；
- 最后一张正常播放页面必须直接使用 `assets/ppt/fixed_pages/固定最后一页_标准版.pptx`；
- 固定最后一页不得重新排版、改色、裁切或在其后增加普通播放页面；
- 固定资源缺失或哈希异常时必须停止生成，不得近似重画；
- 必须通过 `python tools/validate_ppt_fixed_pages.py`。

详细规则唯一来源：

- `05A_方案讲解PPT生产协议.md`
- `05B_固定首页第二页末页协议.md`
- `11A_本金止盈止损设计与PPT披露协议.md`
- `PPT页面类型卡片.jsonl`
- `PPT讲解验收测试集.jsonl`
- `PPT压缩与精度规则.json`
- `assets/ppt/fixed_pages/PPT固定页资源清单.json`
