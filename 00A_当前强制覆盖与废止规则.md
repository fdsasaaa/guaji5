# 00A_当前强制覆盖与废止规则

版本：V3.9.4-OVERRIDE-7  
状态：最高优先级入口覆盖层  
更新时间：2026-08-03

本文件覆盖 `00_启动入口与系统状态.md` 中保留的历史条款。历史原文只作为演进证据，不得再作为当前执行规则。

接管顺序：

```text
AGENTS.md
→ SYSTEM_MANIFEST.json
→ SYSTEM_STATE.json
→ 00_启动入口与系统状态.md
→ 本文件00A
→ 05_视频内容与SEO生产协议.md
→ 05C_YouTube发布参考与统一交付协议.md
→ 05A_方案讲解PPT生产协议.md
→ 05B_固定首页与末页协议.md
→ 11_智能功能调度与资金路径编排协议.md
→ 11A_本金止盈止损设计与PPT披露协议.md
→ PPT页面类型卡片.jsonl
→ PPT讲解验收测试集.jsonl
→ PPT压缩与精度规则.json
→ assets/ppt/fixed_pages/PPT固定页资源清单.json
```

发生冲突时，本文件、SYSTEM_MANIFEST、SYSTEM_STATE、当前任务、05A、05B和11A优先于旧条款。固定页位置、素材和复制方式发生冲突时，05B优先于05A。

---

## 1. GitHub是唯一正式规则源

项目规则、协议、状态、索引、验收标准和后续修改必须直接写入GitHub。所有正式修改使用任务分支，通过仓库校验和GitHub Actions后才能合并到 `main`。

---

## 2. 统一为一个完整交付ZIP

标准方案任务默认只向用户发送一个外层ZIP。外层ZIP根目录必须且只能包含：

1. 自然名称的方案套ZIP；
2. 一个自然名称的讲解PPTX；
3. 一个本期专属 `YouTube发布参考.txt`。

YouTube发布参考TXT必须包含3至5个标题参考、8至10个英文逗号单行标签和一份独立视频描述。内部日志、构建清单、方案ID、批次ID和统计底稿不得进入对外ZIP。

不再默认生成工作包ZIP、独立口播稿、逐页旁白、事实校验表、技术报告或操作说明。

---

## 3. 废止PPT隐藏附录

PPT只保留视频录制和人工讲解真正需要的页面与演讲者备注。以下内容不得再塞进PPT：

- 隐藏技术附录；
- 附录分隔页；
- 仅供内部复核的完整统计表；
- 规则版本、字段、证据等级和计算过程的堆叠页；
- 正常录制不会讲到的备用资料。

需要追溯的规则、统计、资金计算和验证证据写入GitHub批次记录、审计记录或结构化索引，不占用PPT页面。

---

## 4. 废止机械拆页和固定结构

每页必须提供不可替代信息；删除后不影响理解的页面必须合并或删除。页数不机械固定，唯一标准是用最少页面把技术讲清楚。

固定第一页背景与固定最后一页属于品牌组件；正文从第二页开始动态生成。固定第二页已经取消，不得重新加入。

---

## 5. 废止程序化命名和工程痕迹

PPT可见文字、页标题、方案名称和对外交付文件名不得出现 `B001`、`B392_SET_001`、`方案ID`、`批次ID` 等工程式编号。

软件导入确实需要的内部编号只能保留在TXT、结构化索引或审计记录中，不得出现在观众页面和演讲者备注中。PPT必须使用自然名称，例如“冷热轮投验证”“远邻号码实验”。

---

## 6. 金额单位统一为“元”

PPT可见文字、演讲者备注和对外方案说明中的金额统一写成“120元”“止损300元”。禁止使用“120U”“300U”等表达，也不使用 `¥120` 代替自然中文金额。

原始日志或外部资料中的U仅可原样保留在内部证据记录中；进入PPT前必须换算或改写为“元”。

---

## 7. 挂机前不预判验证结果

新方案生成时默认处于 `PRE_RUN_SETUP` 阶段。此阶段PPT只讲：

- 为什么设计这个实验；
- 规则怎样执行；
- 怎样导入和挂机；
- 观察多少期；
- 建议本金、止盈止损和风险边界；
- 最后怎样判定结果。

不得利用历史开奖数据直接在PPT中下结论，不得提前写“证明不盈利”“这是坏方案”“无优势”“验证失败”等结果性判断。

只有用户提供真实挂机结果、实际运行记录或明确指定的前向验证结果后，才能进入 `POST_RUN_REVIEW` 阶段，在PPT中表达实际结果和复盘结论。历史开奖数据可以帮助内部设计方案，但不能冒充本次挂机的最终答案。

---

## 8. 当前PPT导演硬标准

- 每套项目只生成一个PPTX；
- 第一页使用固定金属板背景并写入真正背景层；
- 正文从第二页开始，禁止固定平台联系页；
- 标题准确、自然，不显示工程编号；
- 金额统一使用“元”；
- 默认顺序为“技术定义→真实技术缘由或实验规则说明→核心规则与必要边界→完整案例”；
- 新方案PPT只做验证准备，不提前给盈利或好坏结论；
- 结果页、对比页和结论页只有在收到真实挂机结果后才能使用；
- 每张页面必须通过页面价值门槛；
- 全部页面必须有简短自然的演讲者备注；
- PPT中不得存在隐藏附录或无录制价值页面；
- 最后一张正常播放页面固定使用蓝金实验室结束页；
- 最后一页网址和Telegram链接必须可点击；
- 必须通过讲解审查、命名审查、金额单位审查、阶段审查、精度审查和全页渲染审查。

---

## 9. 固定首页与末页硬标准

```text
第1页：固定金属板背景封面
第2页起：动态项目正文
最后一页：固定蓝金实验室结束页
```

- 首页背景使用 `assets/ppt/fixed_pages/首页背景图谱.png`，必须嵌入背景层；
- 不得生成、复制或保留固定第二页；
- 正文页数按内容决定；
- 最后一页直接使用 `assets/ppt/fixed_pages/固定最后一页_标准版.pptx`；
- 固定资源缺失或哈希异常时停止生成；
- 必须通过 `python tools/validate_ppt_fixed_pages.py`。

详细规则唯一来源：

- `05A_方案讲解PPT生产协议.md`
- `05B_固定首页与末页协议.md`
- `11A_本金止盈止损设计与PPT披露协议.md`
- `PPT页面类型卡片.jsonl`
- `PPT讲解验收测试集.jsonl`
- `PPT压缩与精度规则.json`

---

## 10. YouTube发布参考与SEO硬标准

- 每个标准方案任务必须生成本期专属YouTube发布参考TXT；
- 标题参考3至5个，围绕一个核心关键词和一个真实问题或实验条件；
- 标签8至10个，必须在同一行用英文逗号分隔；
- 描述前两行先说明本期对象和观看价值，并自然出现1至2个核心关键词；
- `PRE_RUN_SETUP`阶段不得在标题或描述中提前写盈利、亏损、命中率、回撤、优于随机或验证失败；
- `POST_RUN_REVIEW`阶段只能使用用户提供的真实挂机记录或明确前向结果；
- 禁止关键词堆砌、误导性元数据、保证收益和未发生的结果；
- SEO文件默认不自动加入博彩平台链接、开户链接或外部联系方式；
- 标题、描述和标签不得出现工程编号，金额统一使用“元”；
- 最终外层ZIP必须严格只有方案套ZIP、PPTX和YouTube发布参考TXT三项。

详细规则唯一来源：`05C_YouTube发布参考与统一交付协议.md`。
