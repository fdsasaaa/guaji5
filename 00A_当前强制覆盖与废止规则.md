# 00A_当前强制覆盖与废止规则

版本：V3.9.2-OVERRIDE-3  
状态：最高优先级入口覆盖层  
更新时间：2026-08-01

本文件用于覆盖 `00_启动入口与系统状态.md` 中保留的历史条款。历史原文继续保留作为演进证据，但不得再作为当前执行规则。

接管顺序：

```text
AGENTS.md
→ SYSTEM_MANIFEST.json
→ SYSTEM_STATE.json
→ 00_启动入口与系统状态.md
→ 本文件00A
→ 05A_方案讲解PPT生产协议.md
→ PPT页面类型卡片.jsonl
→ PPT讲解验收测试集.jsonl
→ PPT压缩与精度规则.json
```

发生冲突时，本文件、SYSTEM_MANIFEST、SYSTEM_STATE、当前任务和05A模块优先于00文件中的旧条款。

---

## 1. GitHub是唯一正式规则源

项目规则、底层逻辑、协议、状态、索引、验收标准和后续修改必须直接写入GitHub。

聊天长期记忆只能帮助识别项目和定位仓库，不得替代GitHub正式源；尚未写入GitHub并通过校验的规则，不得宣称已经完成系统升级。

所有修改必须使用任务分支，通过仓库校验和GitHub Actions后才能合并到 `main`。

---

## 2. 废止旧三文件交付

以下历史规则全部废止：

- 每次返回方案ZIP、PPTX和更新工作包ZIP三份文件；
- 自动三件套协议；
- 默认生成并发送下一版系统工作包；
- 逐页旁白或事实校验表作为独立交付文件。

当前默认只向用户交付：

1. 独立方案套ZIP；
2. 一个讲解PPTX。

系统规则、状态、索引和回滚记录直接写入GitHub。工作包ZIP只在用户明确要求、离线迁移或GitHub不可用时生成。

---

## 3. 废止独立PPT配套文件

以下历史交付物不再生成：

- 独立口播稿；
- 逐页旁白Markdown；
- PPT事实校验表JSON；
- 独立技术报告；
- 独立操作说明。

同一PPT内部包含主讲页面、演讲者备注、隐藏技术附录和固定品牌结束页。

---

## 4. 废止机械拆页和固定结构

旧的“每个模块都单独成页”“为了结构完整补齐页面”和“所有页面统一卡片布局”全部废止。

当前规则：

- 每页必须提供不可替代信息；
- 删除后不影响理解的页面必须合并或删除；
- 简单技术主讲通常控制在7至9页；
- 定义、缘由、规则和边界可以按内容合并；
- 风险与建议可以合并；
- 没有数据不得制造结果页；
- 不把内部制作指令、过渡语或讲解顺序说明写进观众页面。

页数不机械固定，唯一标准是用最少页面把技术真正讲清楚。

---

## 5. 废止程序化和重复表达

主讲页不得优先使用 `n`、`n+3`、程序变量或内部字段表达简单规则。

必须优先转换为准确自然的人话，并通过数字环、箭头、颜色和具体案例消除左右、循环和边界歧义。

同一数字、图表或结论无新增意义时不得重复出现。允许留白，不得为了填满空间增加装饰图表、重复标签或无意义文字。

---

## 6. 当前PPT导演硬标准

- 每套项目只生成一个PPTX；
- 规则修改必须写入GitHub正式源；
- 第一页必须是真正封面；
- 标题必须准确表达项目核心动作，准确优先于绝对简短；
- 封面标题原则上不超过10字、最多12字；
- 技术缘由必须回答具体对象为什么被选择，不得编造；
- 规则必须完整到能够独立执行；
- 完整案例必须位于核心规则之后，并展示输入、过程、结果和判定；
- 每张候选页面必须通过页面价值门槛；
- 主讲只保留最关键数据；
- 演讲者备注必须简短、自然、与本页对应；
- 风险建议必须说明停止、暂停或继续的具体对象；
- 建议只保留2至4条，并与结论一致；
- 隐藏附录位于结论之后、品牌页之前，正常播放自动跳过；
- 隐藏附录必须保存样本、命中、基准、统计、版本、验证状态和执行边界等必要证据；
- 主讲数字、案例、备注和隐藏附录必须一致；
- 品牌页必须为最后一张正常播放页面；
- 最后一页必须显示 `www.laocaimi.org` 和 `https://t.me/laocaimi1314`；
- 必须通过讲解审查、压缩去程序化审查、精度审查和全页渲染审查。

详细规则唯一来源：

- `05A_方案讲解PPT生产协议.md`
- `PPT页面类型卡片.jsonl`
- `PPT讲解验收测试集.jsonl`
- `PPT压缩与精度规则.json`
