# 00A_当前强制覆盖与废止规则

版本：V3.9.7-OVERRIDE-9  
状态：最高优先级入口覆盖层  
更新时间：2026-08-04

本文件覆盖 `00_启动入口与系统状态.md`、`SYSTEM_MANIFEST.json` 和其他历史模块中保留的旧交付条款。历史原文只作为演进证据，不得再作为当前执行规则。

接管顺序：

```text
AGENTS.md
→ SYSTEM_MANIFEST.json
→ SYSTEM_STATE.json
→ 00_启动入口与系统状态.md
→ 本文件00A
→ 02A_彩票开奖历史分析体系与方案路由.md
→ 05_视频内容与SEO生产协议.md
→ 05C_YouTube发布参考与统一交付协议.md
→ 05A_方案讲解PPT生产协议.md
→ 05D_PPT技术来源与号码证据协议.md
→ 05B_固定首页与末页协议.md
→ 11_智能功能调度与资金路径编排协议.md
→ 11A_本金止盈止损设计与PPT披露协议.md
→ 15_单一完整包与投注资金可见性协议.md
→ controller/external_delivery_contract.json
→ PPT页面类型卡片.jsonl
→ PPT讲解验收测试集.jsonl
→ PPT技术来源验收测试集.jsonl
→ PPT压缩与精度规则.json
→ PPT技术来源与号码证据规则.json
→ assets/ppt/fixed_pages/PPT固定页资源清单.json
```

发生冲突时，本文件和第15号协议优先于 `SYSTEM_MANIFEST.json`、`SYSTEM_STATE.json` 及05C中的历史交付结构；号码来源、PPT固定页和资金披露冲突时，分别由05D、05B、11A和第15号协议中的更严格条款优先。

---

## 1. GitHub是唯一正式规则源

项目规则、协议、状态、索引、验收标准和后续修改必须直接写入GitHub。所有正式修改使用任务分支，通过仓库校验和GitHub Actions后才能合并到 `main`。

---

## 2. 统一为一个严格三项完整交付ZIP

标准方案任务默认只向用户发送**一个下载链接**，指向一个外层完整ZIP。外层ZIP根目录必须且只能包含：

1. 一个自然名称的方案文件夹；
2. 一个自然名称的讲解PPTX；
3. 一个本期专属SEO TXT。

方案文件夹只允许存在挂机软件实际需要导入的文件及必要子目录，不得包含README、核对表、记录模板、随机对照、探针、验证JSON或嵌套方案ZIP。

SEO TXT只允许包含：1个标题、1行8至10个英文逗号标签、1份描述。废止3至5个标题备选和多版本描述。

用户侧禁止并列发送方案ZIP、PPT、验证JSON、GitHub Actions制品包等多个链接。内部日志、构建清单、方案ID、批次ID和统计底稿只保留在GitHub内部或CI审计制品中。

详细规则唯一来源：

- `15_单一完整包与投注资金可见性协议.md`
- `controller/external_delivery_contract.json`
- `tools/validate_external_delivery.py`

---

## 3. 废止PPT隐藏附录

PPT只保留视频录制和人工讲解真正需要的页面与演讲者备注。以下内容不得再塞进PPT：

- 隐藏技术附录；
- 附录分隔页；
- 仅供内部复核的完整统计表；
- 规则版本、字段、证据等级和计算过程的堆叠页；
- 正常录制不会讲到的备用资料。

需要追溯的完整统计、资金计算和验证证据写入GitHub批次记录、审计记录或结构化索引。但是决定具体号码的关键统计、排名和选择理由属于观众必须理解的技术核心，必须在正文中展示，不能以“内部资料”为由删除。

---

## 4. 废止机械拆页和固定结构

每页必须提供不可替代信息；删除后不影响理解的页面必须合并或删除。页数不机械固定，唯一标准是用最少页面把技术讲清楚。

固定第一页背景与固定最后一页属于品牌组件；正文从第二页开始动态生成。固定第二页已经取消，不得重新加入。

不得为了压缩页数删除“号码怎样从历史数据得来”的核心页面。

---

## 5. 废止程序化命名和工程痕迹

PPT可见文字、页标题、方案名称和对外交付文件名不得出现 `B001`、`B392_SET_001`、`方案ID`、`批次ID` 等工程式编号。

软件导入确实需要的内部编号只能保留在TXT、结构化索引或审计记录中，不得出现在观众页面和演讲者备注中。PPT必须使用自然名称，例如“冷热轮投验证”“远邻号码实验”。

同样禁止把内部纠错话术写给客户看，例如“更严谨的说法”“四组测试码预设”“给用户看的”“为了方便演示”。出现此类措辞说明技术来源没有建立，应停止交付并重做。

---

## 6. 金额单位统一为“元”

PPT可见文字、演讲者备注和对外方案说明中的金额统一写成“120元”“止损300元”。禁止使用“120U”“300U”等表达，也不使用 `¥120` 代替自然中文金额。

原始日志或外部资料中的U仅可原样保留在内部证据记录中；进入PPT前必须换算或改写为“元”。

---

## 7. 挂机前不预判验证结果

新方案生成时默认处于 `PRE_RUN_SETUP` 阶段。此阶段PPT只讲：

- 为什么设计这个实验；
- 历史数据中观察到了什么；
- 具体号码或状态怎样计算出来；
- 规则怎样执行；
- 怎样导入和挂机；
- 观察多少期；
- 建议本金、止盈止损和风险边界；
- 最后怎样判定结果。

不得利用历史开奖数据直接在PPT中下结论，不得提前写“证明不盈利”“这是坏方案”“无优势”“验证失败”等结果性判断。

历史开奖可以作为号码来源证据，例如展示定位冷热、遗漏、重号、长龙、跨度、和值或位置关系的统计，但这些统计只解释“为什么选这些号码”，不能冒充本次挂机的最终答案。

只有用户提供真实挂机结果、实际运行记录或明确指定的前向验证结果后，才能进入 `POST_RUN_REVIEW` 阶段，在PPT中表达实际结果和复盘结论。

---

## 8. 号码来源与技术缘由最高优先级闸门

所有正式方案必须先从 `02A_彩票开奖历史分析体系与方案路由.md` 和 `分析角度索引.jsonl` 选择真实研究角度，再生成号码和执行方案。

任何具体号码、胆码、杀码、号码组或轮换组进入PPT前，必须完成：

1. 数据文件、期号范围和样本数量冻结；
2. 观察位置或属性冻结；
3. 分析角度ID和自然名称冻结；
4. 原始频次、遗漏、差值、排名、阈值或映射过程冻结；
5. 取码数量、并列处理和排除规则冻结；
6. 最终号码逐个依据冻结；
7. PPT、TXT和GitHub号码证据包一致性检查。

固定投注数字还必须三重可见：

- 文件名包含实际数字；
- TXT真实投注字段包含实际数字；
- PPT主讲页明确显示实际数字。

禁止：

- 随意找几组数字后再补“热码、温码、冷码”故事；
- 只给四组号码和四个漂亮名称，不说明实际统计；
- 把随机出号包装成主技术；
- 把外部一次计算包装成软件运行中的动态分析；
- 用“测试码预设”替代真实技术来源；
- 让观众只能相信结论而无法按规则复算；
- 把投注数字藏在二级ZIP、内部JSON或演讲者备注中。

如果软件只能使用挂机前冻结号码，可以采用真实历史窗口计算后冻结，但PPT必须讲清计算过程，并说明运行期间不自动更新。

详细规则唯一来源：

- `05D_PPT技术来源与号码证据协议.md`
- `PPT技术来源与号码证据规则.json`
- `PPT技术来源验收测试集.jsonl`
- `15_单一完整包与投注资金可见性协议.md`

---

## 9. 当前PPT导演硬标准

- 每套项目只生成一个PPTX；
- 第一页使用固定金属板背景并写入真正背景层；
- 正文从第二页开始，禁止固定平台联系页；
- 标题准确、自然，不显示工程编号；
- 金额统一使用“元”；
- 默认顺序为“研究问题→历史现象→号码计算来源→投注规则→完整案例→资金路径→挂机判定”；
- 每组具体号码必须能够从所展示的数据、窗口、统计和选取规则中复算；
- 实际投注数字必须在主讲页直接展示；
- 实际写入TXT的资金序列必须在PPT中直接展示；
- 标签名称不能代替号码依据；
- 新方案PPT只做验证准备，不提前给盈利或好坏结论；
- 结果页、对比页和结论页只有在收到真实挂机结果后才能使用；
- 每张页面必须通过页面价值门槛；
- 全部页面必须有简短自然的演讲者备注；
- PPT中不得存在隐藏附录或无录制价值页面；
- 最后一张正常播放页面固定使用蓝金实验室结束页；
- 最后一页网址和Telegram链接必须可点击；
- 必须通过技术来源审查、号码证据审查、讲解审查、命名审查、金额单位审查、阶段审查、精度审查和全页渲染审查。

---

## 10. 固定首页与末页硬标准

```text
第1页：固定金属板背景封面
第2页起：动态项目正文
最后一页：固定蓝金实验室结束页
```

- 首页背景使用 `assets/ppt/fixed_pages/首页背景图谱.png`，必须嵌入背景层；
- 不得生成、复制或保留固定第二页；
- 正文页数按内容决定；
- 最后一页直接使用 `assets/ppt/fixed_pages/固定最后一页_标准版.pptx`；
- 固定资源缺失或哈希异常时停止生成；
- 必须通过 `python tools/validate_ppt_fixed_pages.py`。

详细规则唯一来源：

- `05A_方案讲解PPT生产协议.md`
- `05D_PPT技术来源与号码证据协议.md`
- `05B_固定首页与末页协议.md`
- `11A_本金止盈止损设计与PPT披露协议.md`
- `15_单一完整包与投注资金可见性协议.md`
- `PPT页面类型卡片.jsonl`
- `PPT讲解验收测试集.jsonl`
- `PPT技术来源验收测试集.jsonl`
- `PPT压缩与精度规则.json`
- `PPT技术来源与号码证据规则.json`

---

## 11. YouTube SEO与资金路径硬标准

- 每个标准方案任务必须生成本期专属SEO TXT；
- 标题必须恰好1个，围绕一个核心关键词和一个真实问题或实验条件；
- 标签8至10个，必须在同一行用英文逗号分隔；
- 描述必须恰好1份，前两行先说明本期对象和观看价值，并自然出现1至2个核心关键词；
- 描述必须与真实技术来源、投注数字和实际资金路径一致；
- `PRE_RUN_SETUP`阶段不得在标题或描述中提前写盈利、亏损、命中率、回撤、优于随机或验证失败；
- `POST_RUN_REVIEW`阶段只能使用用户提供的真实挂机记录或明确前向结果；
- 禁止关键词堆砌、误导性元数据、保证收益和未发生的结果；
- SEO文件默认不自动加入博彩平台链接、开户链接或外部联系方式；
- 标题、描述和标签不得出现工程编号，金额统一使用“元”；
- 用户明确反对机械平倍时，最终TXT不得继续写全1序列；实际资金路径必须冻结在 `delivery_execution_contract.json`；
- E2普通倍投只能作为受控交付实验，必须最高倍数封顶、固定期数停止、计算毛暴露预算并要求首次导入核对；
- 资金路径只改变暴露节奏，不提高号码中奖概率；
- 最终外层ZIP必须严格只有方案文件夹、PPTX和SEO TXT三项。

详细规则唯一来源：

- `05C_YouTube发布参考与统一交付协议.md`（保留通用SEO原则）
- `15_单一完整包与投注资金可见性协议.md`（覆盖标题数量与交付结构）
- `controller/external_delivery_contract.json`
