# AGENTS.md

任何AI或自动化代理接管本仓库时必须：

1. 先读取 `SYSTEM_MANIFEST.json`、`SYSTEM_STATE.json`、`00_启动入口与系统状态.md`、`00A_当前强制覆盖与废止规则.md` 和第10—13号协议。00A用于覆盖00中保留的历史旧条款。
2. 默认直接执行用户任务，不等待二次确认。
3. 用户的“仅供参考”不是硬规格，无法直接生成TXT时必须优先重构为可执行方案。
4. 不得用错误软件类别伪造TXT；高级映射不得把同一常量集合复制到0—9。
5. 未指定倍投时仍需完成四路资金路径审议。
6. 修改必须在分支完成，运行 `python tools/validate_repository.py`，通过后才可合并。
7. `main`是唯一稳定正式源；ZIP、PPTX属于构建产物，不替代源文件。
8. 项目规则、底层逻辑和协议修改必须直接写入GitHub正式源。聊天长期记忆不得替代仓库规则，不得把尚未写入GitHub的内容描述为系统已升级。
9. 标准方案任务对外默认只发送方案套ZIP和讲解PPTX；系统规则、索引、状态、优化记录与回滚证据直接回写GitHub。除非用户明确要求、需要离线备份或仓库不可用，不再生成或发送更新后的系统工作包ZIP。
10. 每套项目只生成一个讲解PPT，不得额外生成独立口播稿、技术报告、事实校验表或操作说明。PPT内部包含主讲页面、逐页演讲者备注、隐藏技术附录和固定品牌结束页。
11. PPT必须先读取 `05A_方案讲解PPT生产协议.md`、`11A_本金止盈止损设计与PPT披露协议.md`、`PPT页面类型卡片.jsonl`、`PPT讲解验收测试集.jsonl` 和 `PPT压缩与精度规则.json`。
12. PPT生成前必须识别项目核心动作，标题准确优先于绝对简短；不得把“杀码验证”误写为只强调保留数量的标题。
13. PPT必须先由讲解导演建立讲解蓝图和动态模块结构，禁止把技术报告直接拆页，禁止把每条制作原则单独变成一页。
14. 每张候选页面必须通过页面独立价值门槛；简单技术主讲通常控制在7至9页，无独立价值页面必须合并、删除或转入备注/隐藏附录。
15. PPT主体默认先讲核心技术、真实缘由和可执行规则，再用案例证明观众已经理解；简单定义、缘由和规则允许合并。
16. 主讲页优先使用准确自然的人话；公式、程序变量和正式口径进入备注或隐藏附录。自然语言不得改变方向、循环、边界或冻结规则。
17. 必须检查数字、图表、标题和底部结论的视觉重复，允许留白，不得为填满页面增加无意义元素。
18. 风险和停止建议必须明确停止或暂停的具体对象；建议只保留2至4条，并与短结论一致。
19. 每个正式挂机方案必须先冻结建议本金和止盈止损模式。允许不设置、只设置止损、只设置止盈或同时设置，但不得保持未决。
20. 挂机方案PPT主讲部分必须显示建议本金金额；启用止盈或止损时必须显示对应金额，未启用项目必须明确写“不设置”。
21. 建议本金、止盈和止损必须根据单注金额、实际注数、资金路径、压力连挂、最大回撤、赔率、风险预算和字典中的已验证规则设计，不得随意填写整数金额。
22. 隐藏附录必须保存本金、止盈和止损的计算依据、模式、金额、比例、达到后动作、软件证据和替代停止条件；主讲与附录金额必须完全一致。
23. 隐藏附录必须位于结论之后、品牌页之前，正常播放自动跳过，并保存足够的样本、命中、基准、统计、版本、验证状态和执行边界证据。
24. 每页必须先识别页面类型并遵循 `PPT页面类型卡片.jsonl`；不得所有页面机械复用同一种卡片模板。
25. PPT必须通过 `PPT讲解验收测试集.jsonl`，并完成讲解审查、压缩去程序化审查、精度审查和全页渲染审查。
26. 所有正式PPT最后一张正常播放页面必须清楚显示 `www.laocaimi.org` 和 `https://t.me/laocaimi1314`；隐藏附录只能放在品牌页之前。
