# 11_智能功能调度与资金路径编排协议

版本：V3.9.6-ORCHESTRATION-ENFORCED-1  
状态：正式流程规则  
作用：强制总导演真正审议并调用完整功能空间，禁止把“全功能审议”降格为几句笼统的关闭理由。

---

## 11.1 核心原则

> 全功能审议不等于全功能启用；但没有形成具体候选参数，就不算审议。

系统必须同时阻止两个极端：

1. **功能失忆**：长期只输出静态号码、顶部轮投和 `1,1,1...`；
2. **功能堆砌**：为了显得复杂，把监控、跳转、模拟切换和高级倍投一次性混装。

最终方案可以选择平倍，也可以关闭监控，但前提是其他路径已经形成可执行候选、完成量化比较，并由机器校验确认不是形式化走过场。

本协议的机器正式源：

- `controller/function_orchestration.json`
- `controller/templates/function_orchestration.template.json`
- `tools/validate_function_orchestration.py`

每个标准方案任务必须在本次运行证据目录生成 `function_orchestration.json`。缺失、字段不完整或只写“默认关闭”，CI必须失败。

---

## 11.2 八层能力审议

每个候选画像都必须完整填写A—H八层，而不是整批只填一次：

| 层 | 审议对象 | 典型功能 |
|---|---|---|
| A | 号码与映射 | 固定定位胆、高级开某投某、冷热、遗漏、足迹、重号、正反集 |
| B | 触发与监控 | 立即投注、仅开始监控、一直监控、达到状态后投注 |
| C | 换号与集合 | 期数换号、命中换号、挂后换号、正集/反集、定码轮换 |
| D | 执行编排 | 单方案、并投、顶部轮投、组合出号、组合轮投、枪弹结构 |
| E | 资金路径 | 平倍、有限普通倍投、压力释放、高级状态路径 |
| F | 风险与停止 | 盈利/亏损停止、硬停止、熔断、暂停、复位、封顶 |
| G | 时间与模拟 | 时间范围、倒计时、模拟转真实、真实转模拟、循环切换 |
| H | 验证与内容 | 随机基准、对立组、样本外、真实记录、视频解释 |

每层固定字段：

```text
relevant=
candidates=[]
final_enabled=
decision_reason=
evidence_level=E0—E7
```

以下写法无效：

- “本次不用”；
- “默认关闭”；
- “风险较大所以拒绝”；
- “高级倍投会污染结果”。

必须进一步回答：候选参数是什么、最大暴露是多少、软件证据等级是多少、与基准相比增加什么价值、为什么最终不选。

---

## 11.3 四种强制候选功能画像

标准方案冻结前至少形成四种画像，且至少三种在以下六个实质维度上不同：号码逻辑、触发、执行、资金、停止、时间/模拟。

1. **BASELINE**：简单号码逻辑、平倍、最小停止条件；
2. **STATE**：至少实质配置一种监控、跳转、停止、换号或正反集状态；
3. **EXECUTION_OR_FUNDING**：至少改变执行编排或资金路径的核心结构；
4. **LOW_COVERAGE_PROBE**：针对长期低覆盖或证据不足功能的隔离单变量探针。

下列不算不同画像：

- 只换投注位置；
- 只把20期改成30期；
- 只把 `1,1,1` 延长成 `1,1,1,1,1`；
- 只换名称；
- 同一映射表整体平移；
- 平倍画像复制四份后分别写成“监控版”“高级版”。

机器校验要求每个画像提供 `material_signature`：

```text
number_logic
trigger
execution
funding
stop
time_simulation
```

至少三个签名必须实质不同，否则标记 `PROFILE_NOT_MATERIAL`。

---

## 11.4 资金路径四路强制量化

未指定倍投也必须完成四路评审，禁止只生成 `1,1,1...` 后用一句话结束。

### 路径0：FLAT

提供具体平倍序列、总倍数、最大暴露、硬停止与复位规则。它是基准，不是默认冠军。

### 路径1：LIMITED_LINEAR

必须给出实际递增但有限封顶的序列，例如 `1,1,2,2,3,3`。必须计算最高倍数、总倍数、连续全挂暴露和命中/到顶后的复位。

### 路径2：PRESSURE_RELEASE

必须出现“先升压、后主动降压”的非单调结构，例如 `1,2,3,2,1,1`。若序列只升不降，不得冒充压力释放。

### 路径3：ADVANCED_STATE

必须给出至少两个状态和至少两条状态转移，明确中后、挂后、复位、跳转和封顶。高级不等于倍数更大。

每路固定字段：

```text
path_id
kind
sequence 或 states/transitions
max_multiplier
total_multiplier
worst_case_exposure
reset_rule
cap_rule
software_evidence_level
decision
reason
```

规则：

- 软件证据低于E3的资金路径不得正式 `SELECTED`；
- E0—E2可以进入 `PROBE_ONLY`，但必须隔离、单变量、最多30期并有成本上限；
- 实际赔率、注数或单注金额缺失时，只能比较暴露结构，不能声称回本；
- 不得用倍投参与选号逻辑的优胜筛选；
- 若最终选择平倍，仍必须保存其他三路的具体序列、暴露和淘汰理由。

---

## 11.5 “更多设置”实质候选门槛

每个正式批次至少有两类下列设置形成**具体候选参数**：

- MONITORING：投注监控；
- PROFIT_LOSS_JUMP：盈利/亏损跳转；
- PROFIT_LOSS_STOP：盈利/亏损停止；
- SIMULATION_REAL_SWITCH：模拟与真实切换；
- TIME_WINDOW：时间范围或倒计时；
- CHANGE_RULE：换号规则；
- BET_DIRECTION：正集/反集；
- ROTATION_OR_COMBINATION：轮投、并投或枪弹编排。

每类必须记录：

```text
category
candidate_profile_ids
material_configuration
evidence_level
final_decision
reason
```

`material_configuration` 不能只有 `enabled=false`。例如：

- 监控：监控模式、监控期数、达到什么状态后开始投注；
- 模拟转真实：模拟连续输/赢几次、真实阶段上限、何时回模拟；
- 盈亏跳转：阈值、跳转目标、跳局数、复位条件；
- 时间范围：开始、结束、时区和范围外动作；
- 正反集：正投集合、反投集合、选择依据。

低于两类，标记 `MORE_SETTINGS_NOT_MATERIAL`。

---

## 11.6 软件证据与探针隔离

E3以下功能不得进入正式运行方案。允许做最小行为探针，但必须：

1. 独立文件夹或独立阶段；
2. 单变量；
3. 最多30期；
4. 明确最大成本；
5. 不与主方案并投；
6. 不把探针结果包装为选号有效性；
7. 记录GUI设置、重启状态、实际投注和日志。

新状态功能首次进入探针时，不得同时首次启用高级倍投。否则无法判断异常来自哪个状态变量。

---

## 11.7 连续平倍与重复画像限制

系统不禁止平倍，但禁止无意识重复。

- 连续两批 `BASELINE_ONLY` 后，下一批不得继续只交付纯平倍；
- 下一批必须选择一个非基准实验，或同时交付一个隔离探针；
- 只有全部到期探索功能都被证据化阻塞时，才允许例外；
- “风险大”“不适合”不是阻塞证据，必须引用软件证据、模板缺失或明确冲突。

同一“号码逻辑 + 触发 + 执行 + 资金 + 停止 + 时间模拟”连续出现三批：

1. 必须施加重复惩罚；
2. 重新提高低覆盖功能画像权重；
3. 若仍选择相同画像，必须保存可核对的例外理由。

---

## 11.8 功能覆盖债务

每个功能记录：

```text
last_reviewed_batch
last_material_candidate_batch
last_selected_batch
consecutive_not_materially_candidate
evidence_level
blocked_reason
blocked_evidence_ref
```

探索窗口最多3批。到期功能必须满足其一：

- 进入正式候选；
- 进入隔离探针；
- 保存有证据引用的阻塞原因。

仅写“本次无关”不能消除覆盖债务。长期目标不是把全部按钮都打开，而是让每个功能都真正进入过“可执行候选—比较—采用/淘汰/探针”的闭环。

---

## 11.9 高级开某投某语义门槛

高级开某投某必须体现“不同开奖触发不同下一期投注集合”。

最低要求：

```text
正投映射组数=10
反投映射组数=10
正投唯一组数>=2
反投唯一组数>=2
每组号码无重复
号码仅0—9
```

10个触发数字对应相同集合时，是静态定码，不得用高级映射包装。缺少正式模板时标记 `IMPLEMENTATION_BLOCKED_TEMPLATE_MISSING`，不得伪造字段。

---

## 11.10 评分与最终选择

每个画像和资金路径必须按同一尺度评分：

```text
问题匹配度
+ 软件证据
+ 风险预算匹配度
+ 与最近方案差异度
+ 视频解释价值
+ 低覆盖债务修复价值
- 最大暴露惩罚
- 未验证状态惩罚
- 参数自由度惩罚
- 与号码逻辑混淆惩罚
```

最终选择可以是：

- `BASELINE_ONLY`；
- `BASELINE_PLUS_EXPERIMENT`；
- `EXPERIMENT_ONLY`。

纯基准只有在未触发连续上限、无到期覆盖债务，或所有探索功能均有证据化阻塞时才允许。

---

## 11.11 强制执行顺序

```text
定义真实问题
→ 冻结号码或触发假设
→ 生成四种功能画像
→ 每个画像填写A—H八层
→ 生成四条具体资金路径
→ 至少两类更多设置形成具体候选参数
→ 检查覆盖债务与最近三批重复
→ 软件证据门控
→ 选择正式方案和必要探针
→ 写入function_orchestration.json
→ 运行机器校验
→ 冻结design_contract
→ 生成TXT、PPT与交付
```

禁止：

```text
先生成静态方案
→ 自动填1,1,1...
→ 把所有复杂功能写成“不启用”
→ 宣称已经全面审议
```

---

## 11.12 机器验收

标准任务必须执行：

```bash
python tools/validate_function_orchestration.py \
  --evidence controller/runs/<RUN_ID>/function_orchestration.json
```

仓库CI必须执行：

```bash
python tools/validate_function_orchestration.py --self-test --scan-runs
```

固定失败状态：

- `ORCHESTRATION_MISSING`
- `PROFILE_FAMILY_INCOMPLETE`
- `PROFILE_NOT_MATERIAL`
- `FUNDING_REVIEW_INCOMPLETE`
- `MORE_SETTINGS_NOT_MATERIAL`
- `UNVERIFIED_FEATURE_SELECTED`
- `BASELINE_ONLY_REPEAT_LIMIT`
- `COVERAGE_DEBT_UNRESOLVED`
- `NEW_STATE_AND_ADVANCED_FIRST_USE_CONFLICT`

只要出现任一失败状态，不得进入正式交付。

---

## 11.13 最终原则

> 聪明不是每次把26个字母全部写出，而是系统能证明自己逐个看过相关字母，形成了具体单词候选，并且不再永远只交付ABC。
