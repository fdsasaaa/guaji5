# 11_智能功能调度与资金路径编排协议

版本：V3.9.0  
状态：正式流程规则  
作用：解决“字典很大，但总导演只反复使用少数功能”的惰性问题；同时阻止为了显得聪明而乱堆功能。

---

## 11.1 核心原则

> **全功能审议，不等于全功能启用。**

总导演必须知道字典中有哪些能力，并逐层判断是否适合本次问题；但最终方案仍只启用真正有作用、软件证据足够、普通人能理解的少量功能。

两个极端都属于失败：

1. **功能失忆**：长期只使用高级开某投某、平倍和顶部轮投，其他功能从不进入候选；
2. **功能堆砌**：为了证明“会用很多功能”，把监控、换号、跳转、时间、倍投全部塞进同一方案。

V3.9.0采用“八层能力审议 + 候选功能画像 + 资金路径四路评审 + 语义防呆校验”。

---

## 11.2 八层能力审议

每个候选套装在冻结前必须审议以下八层：

| 层 | 需要回答的问题 | 典型功能 |
|---|---|---|
| A 号码与映射 | 投什么；是否依赖上一期状态 | 固定定位胆、高级开某投某、冷热、遗漏、足迹、重号 |
| B 触发与监控 | 立即投，还是先观察某种状态 | 投注监控、仅开始监控、一直监控 |
| C 换号与集合 | 什么时候换；正投还是反投 | 换号规则、正集/反集、定码轮换 |
| D 执行编排 | 单方案、轮投、并投还是枪弹 | GUI轮投、组合出号、组合轮投、并投 |
| E 资金路径 | 平倍、普通倍投还是高级状态路径 | 普通倍投、高级倍投 |
| F 风险与停止 | 何时强制停止或复位 | 盈亏停止、熔断、人工期数停止 |
| G 时间与模拟 | 是否限制时段；是否先模拟再真实 | 时间范围、倒计时、真实/模拟切换 |
| H 验证与内容 | 怎样证明不是随机或自然概率 | 随机基准、对立组、真实号码演示 |

内部审议记录至少包含：

```text
功能层=
本次是否相关=true/false
候选功能=
最终是否启用=true/false
软件证据=
启用理由=
不启用理由=
与其他功能冲突=
需要的最小运行探针=
```

“不启用”不能只写“默认关闭”；必须说明它对本次问题没有增加什么价值，或证据不足在哪里。

---

## 11.3 候选功能画像

标准批次至少生成3种不同的**功能画像**，而不仅是3种号码参数：

1. **基准画像**：简单号码逻辑 + 平倍 + 最小停止条件；
2. **状态画像**：加入一个有明确假设的监控、换号或正反集状态；
3. **执行/资金画像**：改变轮投、有限普通倍投、风险停止或高级状态路径中的一个核心维度。

可选第四画像：低覆盖功能的最小行为探针。

下列变化不算新的功能画像：

- 只把万位改成千位；
- 只把20期改成30期；
- 只把1,1,1改成1,1,1,1；
- 只更换方案名称；
- 同一高级映射表整体平移一个数字。

每批内部记录：

```text
候选功能画像数=
号码层差异数=
触发层差异数=
执行层差异数=
资金层差异数=
停止层差异数=
与最近3批重复画像=
重复惩罚=
```

同一“号码逻辑 + 执行结构 + 资金路径 + 监控状态”连续出现3批时，必须施加重复惩罚；仍要采用时，必须给出证据理由。重复惩罚不是强行换功能，而是迫使导演重新审视其他字母。

---

## 11.4 高级开某投某语义门槛

高级开某投某的核心含义是：**开出的触发数字不同，下一期投注集合会发生变化。**

因此：

1. 若10个开奖号码对应的正投号码完全相同，则该方案不是条件映射；
2. 若10个开奖号码对应的反投号码完全相同，同样属于常量映射；
3. 常量映射不得使用“高级开某投某”包装；
4. 固定五号码、固定热码、固定冷码等与上一期开奖结果无关的方案，应使用已验证的固定定位胆/定码轮换模板；
5. 当前工作包没有完整专属模板时，必须标记`IMPLEMENTATION_BLOCKED_TEMPLATE_MISSING`，不得为了交付而滥用高级映射；
6. 真正的高级映射至少应有2组不同输出；二分类逻辑允许2组，环形或相对位移逻辑通常应有更多组；
7. 每个映射必须保存自然语言关系，例如“同奇偶”“环形±2”“开n投n附近五码”，不能只保存数字表。

静态校验最低要求：

```text
正投映射组数=10
反投映射组数=10
正投唯一组数>=2
反投唯一组数>=2
每组号码无重复
号码只允许0—9
```

该校验只能识别明显常量和格式错误，不能证明映射逻辑有效。

---

## 11.5 资金路径四路评审

即使用户没有特别提出倍投，总导演也不得机械写入`1,1,1,1...`后结束。号码逻辑冻结后，必须至少评审四条路径：

### 路径0：平倍基准

作用：隔离号码逻辑，始终保留。平倍仍是基准，不代表默认冠军。

### 路径1：有限普通倍投

例如有限阶梯、分段递增或局数封顶。要求给出最大倍数、总倍数和全挂暴露。

### 路径2：压力释放型普通倍投

允许非单调序列，例如升压后主动降压；目的不是“包回本”，而是研究同总预算下的波动与回撤差异。

### 路径3：高级状态路径

使用已验证9字段结构，比较中后、挂后的状态转移。跳转、监控和多方案独立性没有E3时，只能作为测试版。

评审表：

```text
方案ID=
实际注数范围=
实际赔率=
单注金额=
本金与最大回撤=
平倍路径=
有限普通路径=
压力释放路径=
高级状态路径=
最终选择=
为什么没有选择其他三路=
是否同时交付资金对照版=
```

选择纪律：

- 实际赔率、注数和风险预算缺失时，不能声称任何倍投能回本；
- 缺少上述数据不等于永远只写平倍，可以设计“测试候选”，但必须限制倍率并标记效果未知；
- 有讲解价值且风险可控时，允许同时交付“平倍基准版 + 一个资金路径实验版”；
- 不得让倍投参与选号逻辑的优胜筛选；
- 高级不等于倍数更大，优先考虑封顶、降压、复位和熔断。

---

## 11.6 资金路径模板库使用纪律

`资金路径模板库.jsonl`是候选原料，不是默认菜单。每个模板包含：

- 复杂度等级；
- 序列或状态路径；
- 适用目的；
- 不适用条件；
- 最大倍数和总倍数；
- 是否允许正式使用；
- 所需软件证据；
- 必须对照的基线。

总导演不得每次固定选择同一模板。选择分数由以下因素组成：

```text
问题匹配度
+ 软件证据
+ 风险预算匹配度
+ 与最近方案差异度
+ 视频解释价值
- 最大暴露惩罚
- 未验证状态惩罚
- 与号码逻辑混淆惩罚
```

模板历史表现只能作为观察信息，不能直接固化为默认最优。

---

## 11.7 “更多设置”的主动审议

下列设置过去长期处于存在但未被认真调用的状态：

- 投注监控；
- 盈利/亏损跳转；
- 盈利/亏损停止；
- 真实与模拟循环切换；
- 时间范围与倒计时；
- 换号规则；
- 正集与反集；
- 顶部轮投与枪编排。

V3.9.0要求：每个正式批次至少选择其中2类进入候选审议，但不要求最终启用。

优先顺序：

1. 与真实问题直接相关；
2. 软件证据达到要求；
3. 可以做单变量对照；
4. 风险可计算；
5. 普通人能理解。

高风险未知设置不得和高级倍投同时首次启用。一个新状态功能首次进入时，优先做最小探针，不做“多功能大杂烩”。

---

## 11.8 低覆盖功能的探索机制

`功能能力卡片.jsonl`为每个功能记录适用信号、阻塞条件、搭配关系和探索权重。

低覆盖功能获得的是**候选优先权**，不是自动入选权：

- 与本次问题相关：增加探索分；
- 软件证据不足：改做行为探针；
- 与核心逻辑无关：不得为了覆盖率加入；
- 连续多批未审议：进入下一批候选提醒；
- 连续多批被审议但均不适用：降低提醒频率并记录原因。

长期目标不是把所有功能都用一遍，而是让每个功能都经过有意识的“用/不用”判断。

---

## 11.9 导演决策顺序

```text
定义真实问题
→ 冻结号码/触发假设
→ 生成3种以上功能画像
→ 八层能力审议
→ 高级映射语义检查
→ 资金路径四路评审
→ 风险与停止审议
→ 软件证据门控
→ 选择最少但最有作用的组合
→ 冻结
→ 验证与PPT
```

禁止顺序：

```text
看到软件有很多按钮
→ 随机勾几个
→ 再编一个解释
```

---

## 11.10 最终入选规则

总导演“更聪明”不以使用功能数量衡量，而以以下结果衡量：

1. 没有把固定方案误装进条件映射；
2. 没有无意识地重复最近的资金路径；
3. 至少比较过平倍、有限普通、压力释放和高级状态四路；
4. 监控、换号、停止、时间等功能被主动审议；
5. 每个启用功能都服务于同一个验证问题；
6. 能用更简单结构回答问题时，不保留复杂结构；
7. 软件证据不足时敢于阻断，而不是编字段；
8. 工作包内部能追溯“为什么用”和“为什么不用”。

固定结论：

> **聪明不是把26个字母一次写完，而是在需要时能调用正确字母，并且不再只会ABC。**
